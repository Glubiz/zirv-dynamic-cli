---
last-verified: 2026-08-23
---

# Work Journal

## How to use

- **Reading:** check the last 2–3 entries at the start of a session for recent context.
- **Writing:** entry after any non-trivial change (feature, refactor, bug fix, infra). Skip if a commit message already captures it.
- **Cap:** keep new entries to ~10 lines. If you need more, it's a spec or a [[Decision Log]] entry, not a journal note. Link out; don't inline.
- **Rotation:** when the active journal grows past ~10 entries, move the oldest ones to a quarterly file under `journal-archive/` (frontmatter `archived: true`, header stating the covered date range).

## Format

### YYYY-MM-DD: short title
**What:** one or two sentences.
**Key changes:** files/services touched.
**Follow-up:** anything unfinished (optional).

## Entries

### 2026-08-23: close all 13 open issues #83–#95 plus a malformed-`ctx.toml` fix (`feat/close-open-issues`, v2.23.0)
**What:** Two commits closing the whole open-issue backlog. `2264b55` (#85–#95): codex injection-fallback status surfacing, codex rollout-derived turn/usage events (`capabilities().events = true`), a once-only `zirv setup` memory-harvest offer, fail-safe risk re-classification outside git, a one-time codex distiller/reviewer sandbox-residual announcement plus conditional `--ignore-rules`/`--ignore-user-config`, content-based secret detection, verification-report retention on write, a `resolve_program`-derived cmd-shim trait default, `zirv setup` statusLine-wrapping, `zirv ctx send --all` fan-out, backup-run retention, and a malformed repo `ctx.toml` layer now skipped (not fatal). `499e65a` adds #83 (`[safety]` command policy + `zirv ctx safety`, wired claude `PreToolUse` hook) and #84 (`zirv ctx handover`, in-place model/harness swap under the same registry short id).
**Key changes:** `ctx/{safety,handover,config,announce,mail,status,wrap,dash/mod,adapters/mod,adapters/claude,adapters/codex}.rs`, `commands/setup.rs`, `workflow/{classify,engine,review,telemetry,verification}.rs`; new [[Command Safety]]; [[Ctx Subsystem]], [[Ctx Adapters]], [[Ctx Supervisors]], [[Usage and Pacing]], [[Rot Engine]], [[Workflows]], [[Built-in Commands]], [[Untrusted Configuration]] all updated by the implementers. `Cargo.toml` bumped to 2.23.0.
**Follow-up:** not yet pushed/PR'd as of this entry. Native 2255/2262 (7 pre-existing Windows-only); Linux `commands::ctx` 1968/1968. See [[Active Work]] and the 2026-08-23 [[Decision Log]]/[[Known Issues]] entries for residuals.

### 2026-08-23: dashboard/harness parity fix round — codex permission parity, pacing coverage, macOS usage polling (`fix/dashboard-and-harness-parity`, v2.22.0)
**What:** Seven independent fixes in one branch. macOS status-header usage via a bounded/announced Keychain fallback (`poll::anthropic_token_from_keychain`) plus `poll::usage_source_hint` explaining a missing source; `Ctrl+A v` dashboard select-mode toggle releasing zirv's own mouse reporting for panes its click-drag selection can't reach (e.g. Claude Code); `wrap`'s live mail advisory gained signal-less (codex) parity and `zirv ctx send`/`HARNESS_PROMPT` v8 now say an undirected send is claimed by exactly one session; `HARNESS_PROMPT` v6 states cross-harness delegation parity with a vendor-neutrality regression test; a new shipped-default "sandboxed, no prompts" launch posture (`AgentAdapter::policy_args`/`default_sandbox_args`, `[sandbox]`, `REPO_FORBIDDEN`); the pacing gate now fails closed on blind/stale data (`pace.blind_delay_secs`) and covers interactive launches too (`pace::InteractiveGate`, T10 — wrap/dashboard-pane spawns, previously only `exec`/`loop`); new `zirv setup restore` sharing one manifest-tracked backup mechanism with `reset`/`apply`.
**Key changes:** `ctx/{poll,pace,config,wrap,dash/mod,prompt,mail,status}.rs`, `ctx/adapters/{mod,claude,codex}.rs`, `commands/setup.rs`; [[Usage and Pacing]], [[Ctx Supervisors]], [[Ctx Adapters]], [[Untrusted Configuration]], [[Built-in Commands]]. `Cargo.toml` bumped to 2.22.0.
**Follow-up:** native tests 2153/2160 (7 pre-existing `wrap` failures, identical on main); Docker matrix 107/107 (Linux only — Windows ConPTY/cmd-shim and macOS Keychain untested). Nine issues filed for residuals (#83–#95, see [[Active Work]]). Not yet committed as of this entry.

### 2026-08-21: frontend quality-contract v2 raises the autonomous design and UX ceiling (PR #78)
**What:** Benchmarked the shipped stack against the current Impeccable, Anthropic frontend-design, and Vercel interface-guideline contracts, then made the strongest ideas enforceable without an initializer or human checkpoint. The active AI now classifies surface mode, carries the task/product/design thesis through every phase, owns the complete journey/state matrix, and must pass a bounded 13-dimension review after three-width render inspection.
**Key changes:** `workflow/{frontend,frontend_detector,frontend_render,skill,engine}.rs`; detector schema/corpus expanded to 44 inventory-covered rules and 27 cases; review schema v2 requires every UI/UX score ≥4/5; README, [[Workflows]], [[Built-in Commands]], [[Decision Log]], [[Known Issues]], [[Active Work]]. Ninth focused commit in the existing stack; version remains 2.19.0.
**Follow-up:** GitHub CI run 224 passed versioning, build, all 1,945 tests, rustfmt, Linux clippy, and Windows-target clippy. Draft PR #78 remains for maintainer review.

### 2026-08-21: autonomous frontend quality stack (issues #70–#77, v2.19.0)
**What:** Implemented an Impeccable-inspired frontend quality system inside the provider-neutral workflow engine, with the user's zero-human-initialization requirement as a hard invariant. Frontend tasks are detected from task/path evidence; Zirv derives a bounded repository profile, selects a built-in anti-slop craft floor plus phase skills, runs an offline detector, starts/cleans the app and captures narrow/wide PNG evidence, lets the active AI record a bounded visual verdict, and refuses test/review/verify advancement on stale, missing, truncated, unavailable, or failed evidence. Added Claude/Codex capability parity output, a seven-case detector benchmark, provenance links across every artifact, and frontend-only aggregate telemetry without prompts/source/diffs/model responses.
**Key changes:** `src/commands/workflow/{frontend,frontend_detector,frontend_render,classify,engine,skill,telemetry}.rs`, `StateDir::frontend`, reserved `zirv frontend` routing/help, README, [[Workflows]], [[Built-in Commands]], [[Decision Log]], [[Known Issues]], version 2.19.0. Eight dependency-ordered commits map one-to-one to issues #70–#77 on `feat/frontend-quality`.
**Follow-up:** Open the single stacked PR to `main` and let GitHub CI run Rust build/test/fmt/clippy; the local implementation environment had no Rust toolchain. Authenticated/data-dependent/dynamic routes and non-Chromium native rendering remain explicit fail-closed residuals in [[Known Issues]].

### 2026-08-21: memory, context, and workflow rework — consolidated review train, merged as v2.14.0–v2.17.0
**What:** Three parallel topic branches merged into main, each after a full-diff opus review plus two fix rounds (the original SDD session's own reviews had been cut short). Memory (issues #31–35, PR #66 collapsing #57/#58/#62/#64/#66, v2.14.0): private + shared repo-committed memory scopes, a key-addressed store, the `zirv memory` command family, a budgeted core-memory prompt layer at all six launch seams, and a deterministic recall-only retrieval engine. Context (issues #39–43, PR #65 collapsing #60/#61/#63/#65 for v2.15.0, PR #67 for v2.16.0): the generic context-surface model, AGENTS.md/native-settings ingest, the canonical `.zirv/context/` layer, deterministic drift detection, and the canonical `EffectivePolicy` model. Workflow (issues #47/#49/#50, PR #59, v2.17.0) was already journaled below in its own two entries.
**Key changes:** `src/commands/ctx/{memory,memory_cli,retrieval,surface,context,drift,policy}.rs`, `src/commands/workflow/`.
**Follow-up:** memory #36–38, context #44–46 (the context compiler that wires policy/canonical-context/memory onto a real launch, plus `zirv context status`), workflow #48/#51–56, and #68 (`get_scoped`) remain open. See the five 2026-08-21 [[Decision Log]] entries and [[Known Issues]] for the residuals recorded across all three review rounds.

### 2026-08-21: PR #59 review fixes — workflow trust gates, review integrity, evidence freshness
**What:** Implemented the confirmed opus review of the workflow subsystem: a `REPO_FORBIDDEN` `[workflow]` config section gating repo-supplied checks/skills/telemetry with independent timeout and count caps; `review run` no longer overwrites findings the reviewer recorded mid-run, no longer credits a dashboard spawn-ack as a completed review, pins the reviewer read-only through the adapter, and excludes sensitive/binary/oversized untracked bodies; verification fingerprints before running, records `--check` narrowing (which can no longer satisfy a gate), scrubs control characters out of check output, and prints results before persisting them; classification measures the tree even when scope is declared and re-measures at gated steps; artifact state paths canonicalize in one place (the macOS `/var` test failure) and the bounded server must actually accept a connection before success is reported.
**Key changes:** `src/commands/ctx/{config,announce,prompt,agent,optimize}.rs`, `src/commands/ctx/adapters/{mod,claude,codex}.rs` (new `read_only_args`), all of `src/commands/workflow/`, prompt version v7, [[Workflows]]/[[Untrusted Configuration]]/[[Ctx Adapters]].
**Follow-up:** `zirv verify`'s own gate now requires a full (non-narrowed) run, so any workflow that was advancing on `--check`-narrowed evidence needs a full run instead.

### 2026-08-21: PR #59 verifier round — deadlock, forged-summary and fail-open gaps closed
**What:** A verifier pass over the first fix round found four real defects and eleven smaller ones. The interactive artifact server could hang forever (reading a still-live child's stderr waits for EOF, which waits for exit) and could wedge a chatty server (nothing drained stderr during the lifetime) — both now handled by a continuous bounded-tail drain thread. `write_report` printed a check's repo-authored `command` unscrubbed, so an escape sequence in `verify.toml` could still forge a pass line. Both new `[workflow]` gates failed *open* or hard-errored on an unparseable repo `ctx.toml` (a file a checkout controls) — they now share `workflow::repo_gates`, which fails closed and announces. `repo_slug` canonicalizes internally, closing a slug split between `artifact register` and `workflow stats`. Report schema bumped to 2 so a pre-`narrowed_to` report cannot satisfy a gate; `--url` restricted to loopback; reviewer relay made lossy so non-UTF-8 output cannot SIGPIPE the reviewer; declared scope can no longer talk complexity down; repo-supplied checks share a 3600s budget; SSH/kube/PKCS#12 names added to the untracked denylist.
**Key changes:** `workflow/{mod,artifact,verification,review,classify,skill,telemetry}.rs`, `ctx/{state,announce,adapters/mod}.rs`, [[Workflows]], [[Untrusted Configuration]].
**Follow-up:** The verifier also proved several first-round tests inert; the C2/C3 pins now drive `run_mode`/`run_independent_review` for real and were mutation-checked (revert the fix, the test fails).

### 2026-08-20: provider-neutral workflow system (issues #47–#56, v2.14.0, PR #59)
**What:** Added compact versioned skills/capabilities and built-ins; durable feature/bugfix/refactor/spike/review workflows; deterministic intent/complexity/risk; changed-scope/final verification with fresh fingerprints; compact review packages and finding disposition; static-first artifacts with bounded server cleanup; layered custom skills; privacy-conscious local telemetry. Acceptance review then hardened sensitive-risk floors, persistent built-in-only mode, symlinked-parent/stack-size trust checks, untracked review content, `HEAD`-aware evidence, bounded output capture, open-finding gates, and per-adapter/non-duplicating stats.
**Key changes:** new `src/commands/workflow/` subsystem, raw-argv command routing, `StateDir` roots, prompt v6 current-step layer, reserved workflow commands/verification config, docs and version 2.14.0.
**Follow-up:** PR #59 is the merge/review vehicle; its checks hold the final CI and reviewer state. The future Context Compiler and canonical policy from #43–#46 consume the exposed context/capability seams rather than being duplicated here.

### 2026-08-19: dashboard click-drag selection + OSC 52 copy; Alt+Enter folding fix; mail-loss fix for issue #30 (`feat/dash-adaptive-poll-help-overlay`, uncommitted, v2.13.0)
**What:** Three changes extending PR #29 (the adaptive-poll/help-overlay branch), uncommitted. (1) The host terminal now also enables `?1002` (button-drag tracking, `?1003` stays off): a left click-drag over a pane whose child doesn't itself want mouse reporting now selects text (REVERSED highlight) and release copies it via an OSC 52 sequence to the host stdout (inline base64, 64 KiB cap, best-effort). A selection is cancelled by scrollback movement, any grid resize, or the selected pane processing new child output — three independent pure functions each check this against the selected pane specifically. (2) `encode_key`'s Enter arm now sends `ESC CR` when modifiers intersect SHIFT **or** ALT, not SHIFT alone: an empirical ConPTY probe against real claude v2.1.235 found Windows Terminal itself rewrites Shift+Enter into `ESC CR` once claude's `/terminal-setup` binding is installed, and zirv's console layer folds that back into an `Enter` keydown carrying ALT — the actual root cause of Shift+Enter submitting instead of newlining. (3) Issue #30: `build_turn_env` now sets a spawned pane's session-identity env unconditionally (not just when an adapter's turn-signal registration happens to set it), so a worker pane's report-back reply is always `--to-session`-directed instead of accidentally broadcasting and being claimable by the wrong pane; every on-behalf-of mail consumption (exec/loop drains, dashboard spawn/sweep/overlay) now logs a `mail-consumed` decision-log entry naming the file and claimant.
**Key changes:** `dash/mod.rs` (`Selection`, `pane_local_cell`, `normalize_selection`, `*_cancels_selection`, `osc52_copy_sequence`/`b64_encode`, `encode_key`, `build_turn_env`), `dash/pane.rs` (`Pane::drain` now returns `(any, more)`, `forward_mouse_button` doc), `dash/ui.rs` (selection rendering), `term.rs` (`DASH_MOUSE_ON` gains `?1002h`), `mail.rs` (`consume_and_log`), `exec.rs`/`run_loop.rs` (launch-prompt drain logging). `Cargo.toml` bumped to 2.13.0.
**Follow-up:** none open for these three changes. PR #29 pending final codex review + push — see [[Active Work]].

### 2026-08-19: adaptive dashboard input-poll wait; `Ctrl+A ?` help overlay
**What:** Two independent dashboard usability fixes, on `feat/dash-adaptive-poll-help-overlay` (PR #29, committed `ddff811`). (1) The event loop's first `event::poll` wait per tick is now adaptive (`input_poll_wait`): 10ms while within 300ms of the last operator keyboard/mouse event, else the old flat 50ms. Keystrokes always reached the pane's pty instantly, but the repaint showing the child's echo of them waited out the old flat 50ms poll, making typing feel debounced. Pane output deliberately does not refresh the hot window (a streaming/spinning pane cannot hold the loop at 10ms), so idle and streaming CPU are unchanged; `last_activity` is seeded at launch. (2) New `Ctrl+A ?` (aliases `h`/`H`) help overlay listing every binding, closed by any key. Content is a compile-time `HELP_BINDINGS` table sync-tested against the real `filter_key` dispatch and checked exhaustively against all 16 `DashAction` variants (no wildcard arm), so a future action can't be added without a help-row decision; width (49 cols) and height (fits 80x24) are both asserted by tests.
**Key changes:** `dash/mod.rs` (`DashAction::Help`, `input_poll_wait`, `INPUT_POLL_{HOT,IDLE}_WAIT`/`_WINDOW`, `last_activity`), `dash/ui.rs` (`Overlay::Help`, `HelpBinding`, `HELP_BINDINGS`, `help_lines`).
**Follow-up:** see the entry above for the three further, uncommitted changes extending this same PR. Subagent-driven/TDD, opus-reviewed (approve after one fix round); a codex cross-review was dispatched but codex is not configured on this machine and produced nothing. fmt/clippy/265 dash-scoped tests green.

Older entries: see [[journal-archive/2026-Q3|2026 Q3 archive]] (2026-08-19: role-gated worker prompt, user-layer split, deterministic pty test boundary; 2026-08-18: live inter-session messaging; token-economy hardening of `zirv chat`; per-agent code-review model config; both usage windows shown, filtered by availability, refresh gates fixed at reset boundaries; 2026-08-16: vendor usage monitoring, use_credits gating, pace-to-reset throttle; usage returns to the dashboard header; reap child process trees on every teardown path; derived harness roster + zirv-owned review policy, dashboard sidebar ownership scoping; 2026-08-15: codex enabled out of the box; dashboard scrolling, opaque overlays, and a leaner header; 2026-08-14: round-9 review — help-probe RCE, tree-kill, atomic state writes, dashboard cursor/keys; dashboard adversarial-review fixes; 2026-08-13: `zirv chat` dashboard multiplexer, handoff harvest/status/mail split; 2026-08-12: `zirv chat`/`zirv agent` aliases, Agent enable/disable gate, Obsidian vault created).
