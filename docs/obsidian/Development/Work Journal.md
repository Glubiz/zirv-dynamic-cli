---
last-verified: 2026-09-01
---

# Work Journal

## How to use

- **Reading:** check the last 2–3 entries at the start of a session for recent context.
- **Writing:** entry after any non-trivial change (feature, refactor, bug fix, infra). Skip if a commit message already captures it.
- **Cap:** keep new entries to ~10 lines. If you need more, it's a spec or a [[Decision Log]] entry, not a journal note. Link out; don't inline.
- **Rotation:** when the active journal grows past ~10 entries, move the oldest ones to a quarterly file under `journal-archive/` (frontmatter `archived: true`, header stating the covered date range).

## Format

### YYYY-MM-DD: short title
**What:** one or two sentences.
**Key changes:** files/services touched.
**Follow-up:** anything unfinished (optional).

## Entries

### 2026-09-01: release 3.8.0 -- bug batch closing four issues across two parallel worktree tracks (`release/3.8.0-bug-batch`, issues #251/#252/#253/#255)
**What:** One release PR fixing four bugs found running the workflow/agent surfaces on real work, landed as five commits across two independent worktree tracks. `0ab32d9` (#252): `CodexAdapter::extra_writable_root_args` now always names the resolved git common dir as an explicit writable root, whether it resolves inside `cwd` (a main checkout) or outside it (a linked worktree) -- the prior "outside cwd only" condition assumed a main checkout's own `.git` was already covered by the `cwd` root, which codex's own sandbox never actually grants; `agent.rs::with_headless_extra_writable_roots` now also applies the same roots to a headless `zirv agent` launch, previously dashboard-panes-only. `17bfef3`/`583ad89` (#253): `prompt::compose`'s workflow-step layer is now gated on `role == PromptRole::Orchestrator`, so a dispatched Worker/SubOrchestrator no longer inherits whatever step happens to be active in the repo at launch time. `61a174a`/`c18f338` (#251/#255, merged as `81441c1`): the frontend Test gate now always scans `verification::changed_paths_since_base` (merge-base diff, never a whole-repo fallback); Review/Verify still scan the whole repo but tag every finding `preexisting` against that same set, and `zirv workflow advance --accept-preexisting-findings` records an operator acceptance that waives pre-existing (never introduced) blocking findings for the rest of the workflow. `infer_work_domain`'s task-text score is capped below the Frontend selection threshold, so text alone (without a real frontend path signal) can no longer classify a task as Frontend; a zero-file scan is now `not_applicable` (a pass), not fail-closed missing evidence; new `workflow start --profile`/`workflow reclassify <id> --profile` let an operator force or fix a workflow's methodology overlay without losing completed steps or accepted artifacts. `collect_all`'s full-surface scan now enumerates via `git ls-files` (gitignore-aware) on a git work tree instead of a hand-rolled walk.
**Key changes:** `src/commands/workflow/{classify,engine,frontend_detector,review,verification}.rs`; `src/commands/ctx/{adapters/codex,adapters/mod,agent,prompt,compile}.rs`. `Cargo.toml` bumped to 3.8.0.
**Follow-up:** A same-day follow-up commit (`a28013d`, "fix(workflow): cached not-applicable frontend reports stay fresh") closed the one residual found after the initial fix: `latest_is_fresh_and_passing` now reuses a cached, fresh, non-truncated `not_applicable` report as passing (`latest_is_fresh_and_passing_reuses_a_cached_not_applicable_report`), so a Frontend-profile workflow that stays backend-only no longer re-runs the detector at every gate check. Gate results: nextest/serial-test failure-name lists matched this machine's known `commands::ctx::wrap::tests` baseline (see CLAUDE.md), no new failures. The review round (claude sonnet + codex seats via `zirv workflow review run`) produced three confirmed findings, fixed in `374b292`: the pre-existing-findings acceptance is saved before any later gate runs, `collect_all_via_git` falls back to the directory walk on a nonzero `git ls-files` exit instead of failing open into not-applicable, and `apply_profile` restores the kind's default Design approval when reverting to Standard. The Linux round (rust:1-bookworm, non-root, serial suite + clippy 1.98) was green on the final tree. See [[Workflows]], [[Ctx Adapters]], [[Ctx Subsystem]], [[Built-in Commands]], and the matching 2026-09-01 [[Decision Log]] entries.

### 2026-09-01: release 3.5.0 -- nine-issue batch, two parallel tracks (`feature/issue-batch-3-5-0`, issues #222/#230/#235/#236/#241/#242/#243/#244/#245)
**What:** Two independent implementation tracks closing all nine issues from the 2026-08-31 Ruflo evaluation plus a permission/capability follow-up batch. **Track A (workflow lifecycle):** #235 bounded reviewer workers (`workflow.review_worker_budget_tokens`/`review_worker_max_tool_calls`, appended to `reviewer_argv`) plus per-step wall-clock in `workflow status`; #236 `--brainstorm`/`--no-brainstorm` on `workflow start`, a rewritten interactive `brainstorm` skill vs. a new autonomous `write-intent` skill, and a headless-worker refusal (`ZIRV_CTX_HEADLESS=1`, `engine::refusal_for`); #242 `workflow.auto_spawn_on_gate` auto-spawns a detached, permit-holding `review run`/`test changed`/`verify` worker on a Review/Test/Verify gate transition; #244 `zirv ctx hook session-start` re-injects the latest handoff on Claude `resume`/`clear`, plus the published hook-surface gap analysis. **Track B (ctx/agent):** #230 item 3 `PolicyReport::degraded_capabilities` reaches the delegator via `zirv agent`'s stdout result lines, #227 report-back mail, and dashboard `SpawnAck`; #243 new pure `screen.rs` (prompt-injection markers, credential shape, high-entropy runs) wired into mail, repo context layers, shared memory, and transcript ingestion, flag-only; #241 `compile.rs::gather_memory` feeds the active workflow's task+step as the retrieval query; #222 an interactive codex `untrusted` approval posture gets a one-time advisory, never a config rewrite; #245 closed citing `fallback.rs::route_new_delegation` (#186), already usage-window-aware.
**Key changes:** `src/commands/workflow/{engine,review,mod,skill,frontend_render}.rs`; `src/commands/ctx/{config,agent,announce,compile,hook,mail,policy,prompt,score,sessions,status,wrap,exec,dash/mod,dash/spawnreq,adapters/codex}.rs`; new `src/commands/ctx/screen.rs`; `docs/design/2026-09-01-hook-surface-gap-analysis.md` (new). `Cargo.toml` bumped to 3.5.0.
**Follow-up:** Gate results: nextest/serial-test failure-name lists matched the known flaky `commands::ctx::wrap::tests`/`supervise::tests::terminate_*` baseline for this machine (see CLAUDE.md's "This Windows dev machine" section), no new failures. Residuals recorded, not fixed: `TelemetryEvent.parent_session_id` stays unpopulated for auto-spawned/agent-spawned workers (#244's telemetry-only disposition); `WorkerBudget`'s existing `HardStop` semantics mean reviewer ceilings stay strictly opt-in, not a default; `auto_spawn_on_gate` stays default-off. A codex cross-review round (`zirv agent codex`) and the standing Linux/Docker round (this diff touches `wrap.rs`/`adapters/codex.rs` argv) are still to run before merge. See [[Workflows]], [[Ctx Subsystem]], [[Ctx Adapters]], [[Ctx Supervisors]], [[Command Safety]], [[Untrusted Configuration]], [[Usage and Pacing]], [[Rot Engine]], and the matching 2026-09-01 [[Decision Log]] entries.

### 2026-09-01: waiver-blind review packages fixed, `zirv ctx status --diff` shipped, v3.4.0 (`feature/238-246-review-waiver-status-diff`, issues #238/#246)
**What:** One PR closing two issues. #238: `verification::evaluate_against_operator_baseline` is now the one seam both the test/deploy gate and `review::VerificationEvidence::from_report` call, so a review package's `passed_with_baseline_waiver`/`waived_failing_tests` fields can never disagree with what the gate already accepted — fixes the false Critical "test verification failed" finding every reviewer seat kept filing on a baseline-waived run. #246: `zirv ctx status --diff` prints only the report sections changed since a session's own previous `--diff` call, keyed per session under a new `status-snapshots/` state-dir directory; `HARNESS_PROMPT` bumped to v13, checkpoint bullet now names `zirv ctx status --brief --diff`.
**Key changes:** `src/commands/workflow/{verification,review}.rs`; `src/commands/ctx/{status,state,prompt}.rs`. `Cargo.toml` bumped to 3.4.0. See [[Workflows]], [[Ctx Subsystem]], [[Context Management]], and the matching 2026-09-01 [[Decision Log]] entries.
**Follow-up:** PR pending. Linux/Docker round not needed — no `wrap`/`announce`/`pace`/adapter argv changes on this diff. A codex cross-review round (`zirv agent codex`) is still to run before merge.

### 2026-08-31: dedupe pointer line, `.gitattributes` durability, real token-cost measurements, and the Ruflo evaluation, v3.3.0 (`feature/225-240-token-cost-ruflo`, issues #225/#240)
**What:** Two commits closing out issue #225's measurement work and delivering issue #240's evaluation. `009718b`: `compile.rs`'s canonical-context dedupe skip now appends one `context_layer_dedupe_pointer` line ("[zirv context layer omitted: identical content already loaded via CLAUDE.md/AGENTS.md]") in place of the omitted section instead of leaving pure silence in the composed prompt text — `render_measure_table` shows the deduped layer as a 0-byte row with a reason, and the real `total (session prefix)` row still reflects the small pointer line's own bytes. Same commit lands `docs/design/2026-08-31-ruflo-evaluation.md` (issue #240): a claim-by-claim check of ruvnet/ruflo against its own repo/internal docs, not its marketing copy, rejecting Raft/Byzantine consensus, the 100+ agent persona catalog, the SONA neural layer, the plugin marketplace, cross-org federation, and the build-provenance witness, while adopting five spikes re-derived in zirv's own deterministic idiom — filed as issues #241 (BM25-style deterministic memory ranking over `.zirv/memory/`), #242 (workflow gate transitions auto-spawn test/review workers), #243 (deterministic prompt-injection/PII screening pass, informed by the sibling `midstream` repo's AIDefence design), #244 (hook-surface gap analysis against Ruflo's documented 18 hooks), and #245 (usage-window-aware proactive routing for `zirv agent`, extending the existing cross-harness fallback, issue #186). `dd86901`: `.gitattributes` pins `CLAUDE.md -text`/`AGENTS.md -text` so the dedupe's byte-exact check survives a `core.autocrlf`-normalizing checkout instead of being silently and permanently suppressed (the render mixes on-disk line endings with hardcoded LF separators, so neither `eol=lf` nor `eol=crlf` can reproduce it); `docs/benchmarks/token-cost.md` §6 replaced its `bytes/4` estimates with real `tiktoken cl100k_base` counts — the dedupe saves 1,937 tokens/25.9% of the claude session prefix and 1,732/23.8% on codex (§6.6.2), the per-turn hook trim saves Δ16 tokens/38.1% (§6.6.3), `status --brief` saves a ratio-calibrated ~Δ413 tokens/52.7% (§6.6.4) — plus a new `--first-turn` mode on `token_cost_analysis.py` reading real recorded `cache_creation_input_tokens` off session transcripts (top-level median 25,742.5 tokens, n=78; subagent median 12,219.5 tokens, n=712, §6.6.5). `Cargo.toml` bumped to 3.3.0.
**Key changes:** `src/commands/ctx/compile.rs`; `.gitattributes` (new); `CLAUDE.md`/`AGENTS.md` (regenerated); `docs/benchmarks/{token-cost.md,token_cost_analysis.py}`; `docs/design/2026-08-31-ruflo-evaluation.md` (new). See [[Ctx Subsystem]]'s "The Context Compiler" section (new pointer-line and `.gitattributes` durability paragraphs) and the matching 2026-08-31 [[Decision Log]] entries.
**Follow-up:** PR pending (closes #225, #240). A measurement-only gotcha hit capturing live command output for §6.6: PowerShell's `>` redirection re-encodes stdout to UTF-16 and silently doubles the captured byte count — worked around with `cmd /c "... > file"` instead; see [[Known Issues]]. Issue #246 (a `zirv ctx status --diff` idea surfaced this session) was filed but not implemented here. The standing Linux/Docker round and a codex cross-review round (`zirv agent codex`) are still to run on this diff before merge.

### 2026-08-31: workflow adoption (detect/nudge/enforce/measure) and steady-state token reductions, v3.2.0 (`worktree-issues-223-225`, issues #223/#225)
**What:** One PR shipping two features. **#223** (workflow adoption): new pure detector `src/commands/workflow/adoption.rs` (`signals`/`AdoptionSignals { edit_like_calls, turns }`, substantial at ≥5 edit-like tool calls or ≥12 turns with ≥1 edit) and operator-only `AdoptionPolicy` (`Off < Advise < Nudge < Enforce`, default `Nudge`, `[workflow] adoption`/`ZIRV_CTX_WORKFLOW_ADOPTION`, `REPO_FORBIDDEN`). Detection rides the existing `Stop`/`Notify`/`Prompt` hooks, folding signals into a per-session `AdoptionRecord` under the state dir's new `adoption/` subdirectory; a delegated worker (`ZIRV_CTX_WORK_GROUP` set) is exempt. `Advise` delivers a one-shot Stop-hook `systemMessage`; `Nudge` an `additionalContext` line on `UserPromptSubmit` (live re-checked so an already-active workflow suppresses it); `Enforce` additionally refuses `zirv agent` delegation (exit 1) while the calling session is substantial-without-workflow, gated on a real session identity so an operator with none is never blocked. The nudge names `zirv workflow classify`'s own detected kind, falling back to `feature`. New telemetry (`AdoptionDetected`/`AdoptionRecovered`, session-keyed) backs a `zirv workflow stats` closing line: `adoption: 4/5 substantial sessions ran a workflow (80%), 1 adopted after a nudge`. **#225** (steady-state token usage): new `zirv ctx compile` verb prints the composed session prompt, or with `--measure` a deterministic per-layer byte/~token table (built off `CompiledContext`'s own provenance, never a second hand-rolled walk) — the reproducible baseline for `docs/benchmarks/token-cost.md` §6. `zirv ctx status --brief` collapses the report's unbounded sections (one line per work group, one line for sessions, decisions omitted) for cheap checkpoints, measured 2113→1188 bytes on a fixture. The per-turn `UserPromptSubmit` hook marker sentence was reworded 170→89 bytes with no change to `marker_miss_rate`'s prefix-only detection contract. `TelemetryEvent::cache_hit_ratio()` (schema v2, previously dead code) now backs `zirv workflow stats`'s `cache hit: N.N% ... (schema v2 formula)` line plus per-adapter suffixes, and `zirv ctx usage`'s default view gained its own window cache-hit line, all `n/a` rather than a manufactured 0% with no cache data.
**Extra:** v3.1.0 (the bug-batch #227/#228/#229/#232/#233 release, see the entries below) was merged in from `main` mid-flight, since it landed and released while this branch was in progress; no conflicts of substance, both feature sets are independent surfaces.
**Key changes:** `src/commands/workflow/adoption.rs` (new); `src/commands/ctx/{hook,agent,compile,status,usage}.rs`; `src/commands/workflow/telemetry.rs`; `src/commands/ctx/config.rs` (`[workflow] adoption`); `docs/benchmarks/token-cost.md` §6. `Cargo.toml` bumped to 3.2.0. See [[Ctx Subsystem]], [[Workflows]], [[Usage and Pacing]], [[Untrusted Configuration]], and the matching 2026-08-31 [[Decision Log]] entries.
**Follow-up:** PR pending. A Linux/Docker round and a codex cross-review round (`zirv agent codex`) are still to run on this diff per the standing pre-merge convention.

### 2026-08-31: bug batch — codex capacity/account exhaustion, `zirv agent --workdir`/`--headless`, lenient review ingestion, verify-check env passthrough (`fix/bug-batch-227-228-229-232-233`, v3.1.0, issues #227/#228/#229/#232/#233)
**What:** One release PR fixing five GitHub issues plus one `main`-failing test. **#227** (codex capacity/account errors): `exec.rs`'s tick now recognizes two new message classes beside the existing usage-window `LIMIT_HIT_PATTERNS` — `CAPACITY_PATTERNS` (transient per-model capacity, restart the same-repo session in place within `--max-restarts` on a 15/30/60s capped backoff, exit `EXIT_CAPACITY_EXHAUSTED`/78 on exhaustion) and `ACCOUNT_EXHAUSTED_PATTERNS` (a hard billing stop, `EXIT_ACCOUNT_EXHAUSTED`/79, no restart spent). A headless delegation exiting non-zero now mails the structured reason back to its spawning session (previously dashboard-pane-only and success-only). Codex's three billing modes (API credits/Pro/premium-weekly) are documented; `window::windows_from_rate_limits` already keys windows on their own `window_minutes`, so a weekly-only account correctly yields no 5-hour reading with no code change, only new tests. The `note_limit_wording_drift` breadcrumb now redacts the offending text (`pace::redact_for_log`, ~200 chars, tokens/keys/emails scrubbed) before logging it. **#228** (`zirv agent --workdir`/`--headless`): `--workdir <dir>` names a worker's actual run directory, canonicalised and hard-refused unless it exists, is a directory, and is inside a git repository; feeds `exec::run_with_report`'s `repo` argument via `effective_launch_repo` for a headless spawn (no adapter change). For a pane spawn, `SpawnRequest.workdir` is re-validated server-side by `resolved_spawn_cwd` under the identical rule and widens the pane's writable-root policy to that directory. `--model`/`--workdir` are now the two pane-supported trailing flags; anything else is a named hard error pointing at `--headless`, replacing the old silent demotion-to-headless. **#229/#232** (`zirv workflow review run` ingestion): `ZIRV_REVIEW_RESULT` parsing is now lenient per finding (unknown fields ignored, severity/disposition synonyms accepted case-insensitively, an unrecognized value degrades to `major`/`open` with the raw value appended rather than rejecting the finding); any ingestion failure persists the reviewer's raw output to `.zirv/work/<id>/review/raw-<ts>-<agent>.txt` and names that path in the error; staleness is judged against the current run's own starting fingerprint; `.zirv/work/**` is excluded from the review package and its fingerprint (`classify::is_workflow_work_path`); package truncation now orders `src`/code hunks first (tests, config, docs, renames after), reading the raw diff up to 8 MiB before ordering. **#233** (verify-check env passthrough): `run_check`'s child process gets an explicit `DEFAULT_CHECK_ENV_PASSTHROUGH` (`SSH_AUTH_SOCK`/`SSH_AGENT_PID`/`SSH_ASKPASS`/`GPG_TTY`/`GNUPGHOME`) instead of an unscrubbed full inherit; `[workflow] check_env_passthrough` (`REPO_FORBIDDEN`) lets an operator add names, `ZIRV_CTX_WORKFLOW_CHECK_ENV_PASSTHROUGH` replaces the merged set; a macOS-only `launchctl getenv` fallback covers a login-session variable zirv's own process lacks.
**Extra:** `.zirv/context/common.md` had drifted to 4121 bytes against its own 4096-byte shipped budget, failing `this_repositorys_canonical_common_context_fits_the_shipped_budget` on `main` and silently truncating 25 bytes every session — trimmed to 4080 bytes, `CLAUDE.md`/`AGENTS.md` regenerated.
**Key changes:** `src/commands/ctx/{pace,exec,agent,window}.rs`; `src/commands/ctx/dash/{mod,spawnreq}.rs`; `src/commands/workflow/{review,classify,verification}.rs`; `src/commands/ctx/config.rs`; `tests/fixtures/fake-agent.sh`/`fake-codex-agent.sh`; `.zirv/context/common.md`. `Cargo.toml` bumped to 3.1.0. See [[Ctx Supervisors]], [[Usage and Pacing]], [[Ctx Adapters]], [[Ctx Subsystem]], [[Workflows]], [[Untrusted Configuration]], [[Command Safety]], [[Known Issues]], and the matching 2026-08-31 [[Decision Log]] entries.
**Follow-up:** PR pending. Residuals recorded, not fixed: `zirv ctx loop` still only recognises the original `LIMIT_HIT_PATTERNS` class, not the two new codex classes (#227); a forged dashboard spawn request can now point a fresh worker pane's writes at any git repo the dashboard user can reach, not only the dashboard's own repo family, widening the accepted #179 trust boundary (#228). A Linux/Docker round and a codex cross-review round (`zirv agent codex`) are still to run on this diff per the standing pre-merge convention. Machine gotcha hit while verifying: this Windows box's Git Bash has no `git`/`cargo` on `PATH` inside Claude Code, worked around with a `-File run.ps1` PowerShell wrapper that rebuilds `PATH` from Machine+User before running each gate — see [[Known Issues]].

### 2026-08-31: bug batch review round — pane `--workdir` confinement, `CAPACITY_PATTERNS` final list, reviewer `--headless`/stdout-only contract (`fix/bug-batch-227-228-229-232-233`, v3.1.0, issues #227/#228/#229/#232/#233)
**What:** Three follow-up commits addressing the review round on the batch above. **#228 narrowed:** a security review found the first `--workdir` cut ("any git repository") too wide for a pane spawn; `dash::resolved_spawn_cwd` now also confines it to `workdir_roots(cfg, repo)` (default: the dashboard's own canonicalised repo root plus its parent directory; widened by the operator via `[dash] workdir_roots` in `~/.zirv/ctx.toml` or `ZIRV_CTX_DASH_WORKDIR_ROOTS`, both `REPO_FORBIDDEN`), containment checked component-wise on canonical paths so `D:\GitHub\zirv` never matches a sibling `D:\GitHub\zirv-other`; the headless `zirv agent --workdir` path stays unrestricted beyond exists/dir/git-repo, since it runs as the operator's own command. **#227 pattern list finalized:** `CAPACITY_PATTERNS` dropped a seventh candidate, `"503 Service Unavailable"`, after review concluded a non-provider process (a test hitting `localhost`) could print that generic HTTP line too — the final six needles and pinned non-matches are in `pace.rs`'s own tests. **#229/#232 exercised live:** `zirv workflow review run`'s own reviewer spawn now passes an explicit `--headless` (`reviewer_argv`), because this branch's own #228 loud pane-flag refusal would otherwise block the workflow's own reviewer from inside a dashboard — found by this branch's review gate hitting it directly; the reviewer prompt now also states the `ZIRV_REVIEW_RESULT` line must be printed as plain stdout text, never through a harness findings tool, after a real miss: a claude sonnet reviewer delivered findings through Claude Code's `ReportFindings` tool instead, zirv's relay saw no envelope, and the #232 raw-output salvage path recovered the findings via `zirv workflow review add` — a working demonstration of that fix, not a lost round. A codex reviewer in the same round ingested cleanly via the new lenient parser (2 findings). **#233 clarified:** a codex review finding that `check_env_passthrough` "violates the operator-only trust boundary" was dismissed — the allowlist is a guarantee that named variables are present (with a macOS `launchctl getenv` fallback), never an isolation/scrubbing boundary; the check child already inherits zirv's whole environment unscrubbed, by design, the same posture `repo_checks_enabled` already grants it.
**Key changes:** `src/commands/ctx/dash/mod.rs` (`default_workdir_roots`, `workdir_roots`, `workdir_within_roots`); `src/commands/ctx/config.rs` (`DashConfig::workdir_roots`); `src/commands/ctx/pace.rs` (`CAPACITY_PATTERNS`); `src/commands/workflow/review.rs` (`reviewer_argv`, the prompt's stdout-only instruction). See [[Ctx Supervisors]], [[Usage and Pacing]], [[Ctx Adapters]], [[Workflows]], [[Command Safety]], [[Untrusted Configuration]], [[Known Issues]], and the matching 2026-08-31 [[Decision Log]] entries.
**Follow-up:** Same as the entry above — PR still pending, Linux/Docker and codex cross-review rounds still to run before merge.

### 2026-08-31: PR #234 conflict resolution — addressed mail from any cwd plus safe built-in auto-allow (`fix/224-226-bug-batch`, issues #224/#226, v3.0.1)
**What:** Issue #224 replaced the broad `Bash(zirv *)` shipped allow with the reviewed structured-built-in partition derived from `utils::RESERVED_COMMANDS`; repo scripts/payloads and dangerous posture-pinning forms remain gated, and Claude launch settings mirror only the safe patterns. Issue #226 makes session/role-directed mail follow the recipient's delivery-envelope path from any cwd. Rebasing onto v3.0.0 exposed an overlap with issue #219's registered-mailbox read widening: retaining both mechanisms duplicated directed results in inbox/unread counts and widened undirected mail across mailbox slugs. The merge keeps envelope-addressed lookup as the single mechanism, preserves worktree/`--peek`/receipt-status coverage from #219, and keeps undirected, claim-once, and fan-out mail mailbox-scoped.
**Key changes:** `src/commands/ctx/mail.rs` (`directed_paths_for`, path-derived `consume`, peek union/dedup, combined #219/#226 regressions); `src/commands/ctx/{safety.rs,adapters/mod.rs,adapters/claude.rs}`; `src/commands/ctx/dash/pane.rs` (deterministic long-lived child for the real-PTY write smoke test merged from main); `Cargo.toml`/`Cargo.lock` 3.0.1; [[Command Safety]], [[Ctx Adapters]], [[Ctx Subsystem]], [[Untrusted Configuration]], and [[Decision Log]].
**Verification / follow-up:** All five local gates pass on the resolved tree: `cargo build`; `cargo nextest run --no-fail-fast` (3259/3259); `cargo test --verbose -- --test-threads=1` (3259/3259); `cargo fmt -- --check`; `cargo clippy --all-targets -- -D warnings`. Commit/push the merge resolution and confirm PR #234's GitHub pipeline. Codex's adapter posture remains unchanged because it has no symmetric verified per-command settings projection.

### 2026-08-31: bug batch, widened to a breaking release — headless argv budget, worktree mail read-side, frontend target root, bracketed-paste ownership, `.zirv/commands/` relayout (`fix/issue-batch-220-219-206-214`, v3.0.0, issues #220/#219/#206/#214/#212)
**What:** Started as four bug fixes; scope grew mid-batch when the operator folded #212 into the same PR, so it now ships as **v3.0.0** (breaking) instead of v2.40.0. #220: `exec.rs::build_headless` — the chokepoint every headless launch/relaunch/nudge/park (`zirv agent`, `zirv workflow review run --agent`) reuses — now measures the WHOLE assembled argv (`headless_argv_len`: program plus every argument, including the #213 system-prompt layer) and routes the prompt to stdin (`adapter.headless_cmd_stdin`) whenever it exceeds the same `INLINE_ARGV_PROMPT_BUDGET_BYTES` (24KB) #213 already established, or the launch goes through a shim — closing the `os error 206` gap #213 left open for a large task prompt riding beside a near-budget system prompt. #219: `zirv ctx inbox`/`unread_counts` used to derive their mailbox only from `repo_slug(current_dir())`, so a session running from a git worktree never looked where its own mail was actually filed (delivery already used the target's registered slug); `run_inbox_with`/`unread_counts` now also scan the caller's registered slug when it differs, merging both directories by `sent` time. #214: new `--frontend-root <path>` on `workflow start`/`workflow advance` sets `WorkflowState.frontend_target_root`, which the Frontend-profile detector/render evidence gate now scans instead of `state.repo` when set (persisted before the gate runs, so a fail-closed advance still records it); `zirv workflow status` prints it when set. A same-issue fix: `classify.rs::git_change_input` now excludes untracked `.zirv`-owned paths (`is_zirv_owned_path`) from change-surface evidence — a stale `.zirv/work/<old-id>/dash-v3-mock.html` left over from issue #209 was making every new workflow in this repo classify as Frontend domain. #206: a multiline paste into a wrapped interactive session used to submit one line per CR instead of one paste, because `wrap` only ever relayed the wrapped child's own bracketed-paste request (`ESC[?2004h`) rather than owning the mode itself — so bracketing only held once some child had asked and the request had landed, never before an agent finished starting or during a relaunch gap. `term.rs`'s `RawGuard` now owns DECSET 2004 for the whole session regardless of the child, and a new `PasteGuard` filter reassembles an `ESC[200~ … ESC[201~` span across arbitrary stdin read boundaries and forwards it as one write (1 MiB cap, 5s deadline). Reproduced/verified against the real probe-pty → `zirv ctx wrap` → ConPTY → `claude.exe` stack.
**Key changes:** `src/commands/ctx/exec.rs` (`headless_argv_len`, `headless_prompt_via_stdin`, `build_headless`); `src/commands/ctx/mail.rs` (`registered_slug_if_different`, `registered_slug_for_short`, `run_inbox_with`, `unread_counts`); `src/commands/workflow/engine.rs` (`WorkflowState.frontend_target_root`, `--frontend-root` on `StartArgs`/`AdvanceArgs`, `resolve_frontend_root`); `src/commands/workflow/classify.rs` (`is_zirv_owned_path`, `git_change_input`); `src/commands/ctx/term.rs` (`BRACKETED_PASTE`, DECSET 2004 in `RawGuard`); `src/commands/ctx/wrap.rs` (`PasteGuard`). See [[Ctx Subsystem]]'s "Chat, Agent and Mail" section, [[Ctx Adapters]], [[Ctx Supervisors]]'s "wrap" section, [[Built-in Commands]]'s "AI-native workflow lifecycle" section, [[Known Issues]], and the matching 2026-08-31 [[Decision Log]] entries.
**#212 landed same day (`feat(scripts)!: move zirv scripts to .zirv/commands/`):** scripts now resolve ONLY from `<repo>/.zirv/commands/` then `~/.zirv/commands/` (same local-shadows-global precedence) — a hard cutover, no root fallback, no auto-migrate. `zirv init` creates `.zirv/commands/` (local and global); `zirv create`/`--global` writes there; `.shortcuts.yaml` and every config file (`ctx.toml`/`.settings.toml`/`verify.toml`) stay at the `.zirv` root; `zirv help` lists scripts from `commands/`, shortcuts from the root; the not-found error now names stray root-level script files (up to 10, config never listed) with a move instruction. This repo's own 11 `.zirv/*.yaml` scripts were `git mv`'d into `.zirv/commands/`. Key changes: `src/utils.rs` (`COMMANDS_DIR_NAME`, `candidate_names_in_dir`, `script_like_files_at_root`), `src/input.rs` (`find_script_in_dir`, `not_found_error`), `src/commands/{create,init,help}.rs`. README/Architecture/Concepts/Modules vault pages and `_system-context.md` were updated directly by its implementer, not here. **#179 was pulled back out same day** (its implementer produced nothing in 90 minutes) and stays open, excluded from this PR — needs its own PR, still requiring unix-socket `SO_PEERCRED` plus pid-ancestry attribution for dash spawn requests; the filesystem intake boundary documented in `dash/mod.rs` still stands unchanged.
**Follow-up:** PR not yet opened as of this entry. One residual on #206: no real human paste in Windows Terminal was exercised in the agent environment that built this — a manual paste on the built binary is recommended before merge. New residual from #212: an installed pre-3.0 `zirv` binary can no longer find this repo's scripts (it still looks in the `.zirv` root) — install the 3.0 binary before relying on `zirv <script>` here.
**Review round:** Three independent reviews ran against `git diff main...HEAD -- src/` (Sonnet, codex `gpt-5.6-terra` cross-review via `zirv agent codex`, and the workflow's own codex `review run`); they converged on three defects, all fixed in-branch. (1) `exec.rs::headless_argv_len` now estimates Windows command-line quoting per argument (`len + count('"') + count('\\') + 2`), so a quote-and-backslash-heavy prompt that sits under the raw 24KB budget but would expand past it once quoted still routes to stdin (`a_quote_and_backslash_heavy_prompt_under_the_raw_budget_still_routes_to_stdin`). (2) `input.rs` now confines every `.shortcuts.yaml` target to `.zirv/commands/`, so a `../legacy.yaml` or an absolute-path mapping could no longer bypass the #212 cutover. (3) A separate codex `review run` found `PasteGuard` (issue #206) forwarded a lone `ESC`/`ESC[` immediately for interrupt/arrow responsiveness, but that meant a start marker split right after those bytes across a read boundary was never recognised as opening a span at all; `PasteGuard` now remembers the forwarded 1–2 byte prefix (`forwarded_start_prefix`) and opens the span when the next read begins with the remainder, gathering the body as one write (`a_start_marker_split_after_the_escape_still_opens_one_span`, `a_start_marker_split_after_the_csi_introducer_still_opens_one_span`, `an_end_marker_split_after_the_escape_is_reassembled_inside_the_span`). A tooling gap surfaced during review was filed as **issue #229**, not fixed here: `zirv workflow review run`'s compact package dropped every `src/` hunk for this ~2.2k-line diff, its `ZIRV_REVIEW_RESULT` parser rejected a reviewer payload carrying an extra `failure_scenario` field, and editing the untracked `.zirv/work/<id>/plan.md` mid-run invalidated the evidence ("change set changed during review"). Separately, the workflow's own test-step gate needed one `zirv test baseline` waiver (`wrap::tests::win::a_supervised_wrap_binds_a_turn_signal_transport`) before `zirv test changed` would pass.

### 2026-08-30: bug batch — baseline-waivable test gate, codex argv overflow, budget-kill exit-code fix, flaky sessions test (`worktree-fix-bug-batch-213-215-218-203`, issues #215/#213/#203/#218)
**What:** Four fixes landed on one worktree/branch, one PR to come. #215: `zirv workflow advance`'s test/deploy gate — previously hard-failing on any test failure — now passes when a failing report's failing test names are all a subset of a new operator-owned `~/.zirv/test-baseline/<repo_slug>.json`, recorded only by the new `zirv test baseline [--repo <path>] [--check <id>]... [--dry-run] [--json]` command; waived names print loudly. #213: `injection_args_for_session` now shrinks an oversized codex inline-argv composed prompt to a 24KB budget (memory → context → workflow strip order, tail-truncation backstop) instead of hard-failing the launch with Windows' `os error 206`. #203: the mid-poll over-budget `HardStop` check now gives one tick of grace before killing a child, so a naturally-exiting child's real exit code survives instead of being clobbered by `EXIT_BUDGET_EXHAUSTED`. #218: `sessions.rs`'s `adopt_child_pid` test now tolerates ≤2s clock-boundary drift between two `process_start_secs` readings (test-only).
**Key changes:** `src/commands/workflow/verification.rs` (`TestBaseline`, `save_baseline`/`load_baseline`/`evaluate_against_baseline`, `run_baseline`, `TestCommand::Baseline`); `src/commands/ctx/prompt.rs` (`INLINE_ARGV_PROMPT_BUDGET_BYTES`, `shrink_for_inline_argv`, `INLINE_TRUNCATION_LAYERS`); `src/commands/ctx/exec.rs` (budget-grace tick in `supervise_run`'s tick closure); `src/commands/ctx/sessions.rs` (test tolerance only). See [[Workflows]]'s "Baseline-waivable test gate" section, [[Utilities]]'s "Inline-argv delivery is budgeted" section, [[Ctx Adapters]], [[Ctx Supervisors]]'s "A mid-poll budget kill got one tick of grace" note, and the matching 2026-08-30 [[Decision Log]] entries.
**Follow-up:** PR not yet opened as of this entry. Issue #209's stale workflow `31794d36` (parked at its test gate in the main checkout since the dash TUI v3 session) is unblockable once this branch's #215 baseline mechanism ships. Remaining open deferred issues: #206 (multiline-paste input bug), #214, #212, #179 (spawn-request auth).

### 2026-08-30: install experience unified and hardened (`worktree-feat-210-install-experience`, issue #210, v2.39.1)
**What:** `cd.yaml`'s Linux release asset now cross-compiles to a static `x86_64-unknown-linux-musl` binary instead of the `ubuntu-24.04` glibc build, which had linked GLIBC_2.39 and failed to run on Debian 12/Ubuntu 22.04 (verified in Docker before and after). `install.sh` gained an arch guard (Linux is x86_64-only; other arches get a `cargo install --git` pointer instead of a broken download), a `ZIRV_INSTALL_DIR` override, `mkdir -p` for a missing install dir, and a download-failure message naming the failed URL — all verified in Docker (default install, pinned version, override, and an arm64 guard trip via qemu). README's Installation/Upgrading sections were rewritten to canonical per-OS instructions, added a Homebrew `brew trust glubiz/tap` note (new Homebrew refuses untrusted third-party taps, verified live), and replaced the misleading `cargo build` fallback with `cargo install --git`.
**Key changes:** `.github/workflows/cd.yaml`, `install.sh`, `README.md`; `Cargo.toml` repository URL fixed (`Glubiz/zirv` → `Glubiz/zirv-dynamic-cli`) and bumped to 2.39.1 (rebased over v2.39.0/#216).
**Follow-up:** Already-published releases (<= 2.39.0) remain glibc-2.39-linked, so pinning an old version via `install.sh` still fails on older distros — no retrofit planned. The `cli.zirv.io` landing page (separate repo `zirv-generic-frontend`, `src/content/channels.ts`) still advertises a 404 install-script URL and an unpublished `cargo install zirv`; being fixed by a separate PR on that repo. See the 2026-08-30 [[Decision Log]] entry and [[Known Issues]].
### 2026-08-30: dash TUI v3 -- fidelity fixes, rot column, footer signal row (`feat/209-dash-v3`, issue #209, v2.39.0)
**What:** Full lifecycle for the issue #209 v3 redesign: an operator-approved design artifact (`.zirv/work/31794d36-4ba3-4dbc-8fad-1d5890fa127e/dash-v3-mock.html`) reviewed and approved before any implementation, then landed as four commits -- `tui::accent()` gains bold weight (`style.rs`, §A1); a read-only `workflow::active_workflow_summary()` accessor (reuses `engine::load_active`); the main dash change (dialog borders dim-cyan with `title · n` counts, the sidebar's box replaced by a flat divider plus full-width rules mirroring the header, the sidebar's rot column restored after issue #202 dropped it, a new footer signal row for the focused pane, a selected-row REVERSED-over-coloured-glyph bug fix); then a codex cross-review round (`zirv agent codex`) that returned 5 findings, 4 confirmed and fixed (the dead-pane footer fallback was unreachable in the live reap loop; the footer's mail count read the dashboard's own launch identity instead of the focused pane's; the width-degradation tiers dropped the workflow segment before the harness label, never matching the mock's 44-column example; the supervision segment hardcoded `true` instead of reading `Pane::reachable()`) and 1 refuted (workflow resolution staying keyed to the dashboard's repo rather than per-pane is correct, not a bug -- see the matching [[Decision Log]] entries).
**Key changes:** `src/style.rs`, `src/commands/ctx/dash/{mod,ui,pane}.rs`, `src/commands/workflow/mod.rs`; `Cargo.toml` 2.38.1 -> 2.39.0. See [[Ctx Supervisors]]'s "The dashboard header, sidebar and footer", [[Workflows]]'s new `active_workflow_summary` note, and four 2026-08-30 [[Decision Log]] entries (footer grammar, workflow-repo-keying refutation, supervision signal, plus the pre-existing #202 style-system entry's corrected cross-link).
**Follow-up:** A gate limitation surfaced mid-session and was filed as issue #215, not fixed here: `zirv workflow advance`'s test step demands fresh `zirv test changed` evidence (the full serial suite, hard-fails on any failure), which this machine's ~6 documented pre-existing baseline wrap/exec failures make structurally unpassable even with zero new failure names -- worked around manually for this workflow, the gate itself still needs a baseline/waiver mechanism. Also: the footer's supervision segment has no "degraded" state yet (a mid-session supervision failure analogous to `wrap`'s own `BarState::degraded`, as opposed to a spawn-time bind failure) -- recorded as future work in `FooterAliveFacts`'s own doc comment, not filed as a separate issue.

### 2026-08-30: README refresh for the v2.38 feature set (`docs/198-readme-refresh`, issue #198 part 2)
**What:** Closed the audited README gap list against source: new "Permission auditing and safe-list proposals" section (`zirv ctx permissions audit|compile|propose`, issue #178's gh/glab api mutating-vs-GET gating, `SAFE_COLLABORATION_VERBS`, auto-proposal to GitHub issues, disabled-by-default `[permissions] propose_enabled`); expanded Development Workflows with the full `zirv workflow` verb set, the intent→spec→plan→implement→test→review→verify→deploy lifecycle, `.zirv/work/<id>/` artifact acceptance, deploy tiers, the agent registry seats, and the maintain loop; a new dashboard subsection under `zirv chat` (multi-pane ConPTY multiplexer, `Ctrl+A` keybindings, `dash.*` config); a new "Cross-harness fallback and handover" section (`zirv ctx handover`, `[fallback]` order/thresholds as shown in `zirv ctx status`); fixed Reserved Command Names (`context`/`setup`/`report` were missing); added `zirv frontend capabilities` plus a cross-reference to `Glubiz/zirv-generic-frontend`; and replaced the stale `2.5.0` install-script version example.
**Key changes:** `README.md` (all edits above, TOC synced); `Cargo.toml` 2.38.0 → 2.38.1.
**Follow-up:** Issue #198 part 1 (refreshing the `zirv-generic-frontend` template repository itself) is untouched by this PR and not yet started.

### 2026-08-30: TUI redesign — style.rs foundation, wrap chrome, dashboard chrome, CLI surfaces (`feat/202-tui-redesign`, issue #202, v2.38.0)
**What:** Audited the terminal chrome across `wrap`'s banner/status bar, the dashboard header/sidebar/dialogs, and `zirv help`/`ctx status`/`context status`, produced an approved design artifact, then landed it in three implementation waves plus a version bump: (1) `src/style.rs`, a single terminal design system (semantic `Tone` palette, display-width-aware truncation via new dep `unicode-width`, `format_age`/`format_pct`/`PLACEHOLDER`, `style::tui` ratatui tokens); (2) `chrome.rs`'s launch banner (rounded-box/compact/legacy degradation ladder) and status bar (chip + glyph-tagged priority-truncated segments); (3) the dashboard (`dash/`) header/sidebar redesign, one shared `render_list_dialog` primitive across six overlays, and the new `Ctrl+A e` errors overlay, plus styled `zirv help`/`ctx status`/`context status`.
**Key changes:** `src/style.rs` (new); `src/commands/ctx/chrome.rs`, `dash/mod.rs`, `dash/ui.rs`, `status.rs`, `context_status.rs`, `help.rs`; `output.rs` gained `success()`; `Cargo.toml` → 2.38.0. See [[Ctx Supervisors]]'s "Terminal chrome"/"The dashboard header and sidebar" sections, [[Ctx Subsystem]]'s `status` verb entry, [[Built-in Commands]]'s `help` entry, [[Technology Stack]]'s `unicode-width` entry, and the same-day [[Decision Log]] entry.
**Follow-up:** Verification now complete — full nextest 3086/3087 (single failure = the known `wrap::win::` baseline), serial `cargo test` the same, `fmt`/`clippy -D warnings` clean on both Windows and the Linux Docker round (`rust:1-bookworm`: 126/126 `wrap::` real-PTY tests + clippy), and a native sonnet review round approved with zero findings. The codex cross-review round returned three confirmed findings (dangling-ZWJ truncation, dash header hints clipped by a long model name, list-dialog u16 height overflow), all fixed with regression tests in `d6cfce5` and re-verified on all gates incl. the Linux Docker round; a final full-PR sonnet review found no defects.

### 2026-08-30: HARNESS_PROMPT v11 — design-review gate and autonomous lifecycle engagement (issues #204/#205, `feat/202-tui-redesign`, commit `8b196f3`)
**What:** Bumped the injected meta-harness prompt layer (`prompt.rs::HARNESS_PROMPT`) to v11 with two new orchestrator bullets. #204: an operator-handed design/UX-shaped task (a UI redesign, a visual or interaction overhaul) now requires an audit of the current state and representative target designs put to the operator for explicit approval before implementation is dispatched — scoped to operator-facing design direction only, so an autonomous frontend baseline with no design dimension still proceeds without asking. #205: with no active `zirv workflow` and a substantial incoming task, the orchestrator seat now starts the lifecycle itself (`zirv workflow start <kind> --task "..."`), trusting `classify.rs`'s size-adaptive gating to right-size the gates, and is told to consult `zirv workflow status` mid-session since the injected prompt does not refresh after launch.
**Key changes:** `src/commands/ctx/prompt.rs` (`HARNESS_PROMPT` v10 → v11). See [[Context Management]]'s "zirv as a meta-harness" section (new v11 paragraph).
**Follow-up:** None recorded — implemented and documented same day as the #202 verification pass above.

### 2026-08-30: Issue-tracker cleanup — closures and new filings
**What:** Closed #187 and #190–#194 (all shipped in merged PR #200) and #180 (duplicate of #203). Filed #204 and #205 (both implemented same day, see the entry above) and #206 (a multiline-paste input bug, not yet triaged).
**Key changes:** Issue tracker only — no code or vault-page changes beyond this entry and [[Active Work]]'s matching note.
**Follow-up:** Remaining open priorities: #179 (spawn-request auth, security), #203 (budget-kill exit-code clobber flake), #198 (frontend template refresh), #206.

### 2026-08-29: PR #200 five-leg review round and fix wave (`release/187-ai-native-sdlc`)
**What:** Full review of the SDLC release PR: spec conformance (20/22 implemented), superpowers parity/token analysis (~10–16x cheaper per cycle, enforcement is code not prose), Windows gates + CLI smoke, Docker/Linux wrap verification, and a correctness pass; then one fix wave.
**Key changes:** `handover.rs` implicit-model quality assumption restored; `exec.rs` segment accounting (no-prompt exit records once, exhausted budget records at all); `fallback.rs` reset-tie keeps the requested harness; `engine.rs` symlink defense on `.zirv/work`, unknown-workflow domain error, gitignore warning; cross-adapter `dispatch_agent` conformance test; `common.md` trimmed under budget; spec Dispatch amendment (see [[Decision Log]]).
**Follow-up:** issue #203 (pre-existing over-budget exit-code clobber flake, see [[Known Issues]]); loop-session cross-harness handover stays parked per `82567c2`'s own framing.
**What:** Wrote the full design for extending `zirv workflow` across Intent → Design → Plan → Implement/Test/Review/Verify → Deploy, plus maintain-cycle re-entry, without a second engine.
**Key changes:** New `docs/superpowers/specs/2026-08-28-ai-native-sdlc-design.md`; committed-work-product hash gates, adaptive skill stages, provider-neutral agents, deploy tiers, maintain scan, and telemetry/docs are phased into #190–#194. The design branch was reconciled onto current main before implementation validation.
**Follow-up:** Implement and validate the five stacked phases.

### 2026-08-28: cross-harness token fallback (issue #186, v2.36.0, PR #189)
**What:** Connected usage windows, the enabled/capacity roster, model-tier mapping, delegation routing, and `exec` handoffs so exhausted/low-headroom new work can land on another harness and a vendor-limit-blocked supervised prompt can resume there instead of idling.
**Key changes:** new `commands/ctx/fallback.rs`; trusted repo-narrow-only `[fallback]` config; `pace::spawn_headroom`; exact cross-vendor tier mapping; `agent` predictive/exhausted rerouting; `exec` handoff + remaining-budget continuation + visited-harness loop guard; `ctx status` policy/headroom disclosure; decision-log actions `harness-reroute`/`harness-handover`; v2.36.0 bump and regression tests for trust/model/bounded-capacity rules.
**Follow-up:** Finish CI (build/tests/fmt/clippy/Windows target) and move draft PR #189 to review-ready once all gates are green.


### 2026-08-28: Reliable messaging — completed and review-hardened (`release/2.35.0`, issue #177, draft PR #185)
**What:** Added a compiling, additive delivery-sidecar implementation around the legacy Markdown mailbox: UUID/thread/reply metadata, per-recipient receipts, TTL/dead-letter state, explicit claim-once, role fan-out, sender status, automatic post-write wake markers, a cross-harness information-not-instruction envelope, and status metrics.
**Verification at handoff (`68be11a` WIP checkpoint):** `cargo check`, rustfmt, and `git diff --check` passed; the 56 pre-existing `mail::tests` passed before six new focused tests were appended, unexecuted at that point.
**Completed (`93553b1`) and review-hardened (`b831dc1`):** a task review of `fe9734b..93553b1` returned 1 critical + 2 important findings, all fixed same-day with one focused test each — `render_delivery_message` interpolated `envelope.from.session`/`.harness` (and every other field) without the `header_value` sanitization the legacy `Message::to_markdown` path already applied, so a newline-bearing `SESSION_ENV`/`AGENT_ENV` could forge an extra bullet line inside the trusted envelope block (now sanitized at render time); `mark_delivery` fell back to the literal `"unknown-reader"` for every unidentified caller, so two concurrent anonymous readers of an undirected claim-once message could both pass `claim_once`'s `AlreadyExists` check (unidentified claimants now mint a unique pid+UUID id via `anonymous_claimant_id`); a crash between `claim_once` succeeding and `consume_reading`'s physical rename left a `Read` receipt whose file never moved, invisible to `expire_deliveries` forever (now cross-checked against each target's actual file presence and corrected to `Expired`).
**Follow-up:** #177 is complete and durable on `release/2.35.0`; #178 followed. See [[Active Work]], [[Ctx Subsystem]]'s "Reliable delivery" section, and the 2026-08-28 [[Decision Log]] entries.

### 2026-08-28: approved-prompt review and safe-list proposals (`release/2.35.0`, issue #178, draft PR #185)
**What:** Added `zirv ctx permissions propose --agent <codex|claude> --sessions <N> [--dry-run]`: post-hoc, transcript-side capture of operator-APPROVED permission prompts correlated with subsequent execution (claude via `correlate_safety_decision`'s `ask`-then-clean-`tool_result`; codex via matching `custom_tool_call`/`custom_tool_call_output` `call_id`s), a conservative classifier that only ever flags documented `gh`/`glab` create/edit/comment collaboration verbs (never merge/close/delete/release/auth/api/redirect/shell-composed) as safe-list-eligible, and a deduplicated `safe-list-proposal` GitHub issue per family. Disabled by default: `crate::settings::operator_propose_enabled` mirrors `operator_github_token` exactly, so a repository `.settings.toml` is never even read for this decision.
**Review round 2 (`9570d41`):** added a persisted per-family reported-evidence watermark (`<state>/approvals/reported.json`) so a repeat run makes zero HTTP calls when nothing changed and delta-marks (not repeats) a comment on an already-open issue; `classify_approval` now returns `Eligible`/`Excluded(reason)`/`OutOfScope` and prints a concise non-escalating-alternative guidance line for every excluded-but-in-scope family, even when nothing else is eligible that run. Also fixed a bug found while implementing this: `family_depth` never listed `"glab"`, so every glab command collapsed to the single-token family `"glab"`, misclassifying every collaboration verb as too-generic.
**Fix (`affb399`):** `family_depth("glab") == 2` (from the round-2 fix above) made `"glab api"` a reachable two-token family, but `is_protected_family` had no protection arm for it — a mutating `glab api -X DELETE`/`POST` call could fall through to the catch-all and compile into a standing `[safety] allow`. Generalized `gh_api_call_is_mutating` to `api_call_is_mutating` and added a `"gh api" | "glab api"` match arm, plus a compile-eligibility end-to-end test.
**Follow-up:** #178 is the last of the three release/2.35.0 issues; closeout (`91dc8f5`) regenerated managed `CLAUDE.md`/`AGENTS.md` and ticked the plan checklist. See [[Active Work]], [[Command Safety]]'s "Approved-prompt review and safe-list proposals" section, and the 2026-08-28 [[Decision Log]] entries.

### 2026-08-28: CLI-native Zirv issue reporting (`release/2.35.0`, issue #176, draft PR #185)
**What:** Added `zirv report bug|feature <title> [--body|--body-file]`, posting to the fixed Zirv repository with the matching label, a bounded environment footer, and only the created issue URL on stdout. Session context includes harness/model but never a session id.
**Key changes:** new `commands/report.rs`; raw-argv dispatch, reserved-name and built-in help wiring; operator-only `[github].token` parsing in `settings.rs`; canonical instructions plus [[Built-in Commands]]/[[Untrusted Configuration]]. Credential order is GitHub CLI, `GH_TOKEN`, `GITHUB_TOKEN`, then home settings; repository settings never supply credentials. HTTP and auth are injected in tests, which remain network- and real-token-free.
**Follow-up:** #176 is the next PR #185 checkpoint; begin #177 only after that commit is durable on the remote branch.

Older entries: see [[journal-archive/2026-Q3|2026 Q3 archive]] (2026-08-27: PR #171 independent review round, `release/2.32.0`, v2.32.0, issue #155 Phases 4-6; 2026-08-27: repo-portable memories, `feat/172-repo-portable-memories`, v2.32.0, issue #172; 2026-08-27: token-cost reduction Phase 2, `feat/token-cost-p2-telemetry`, v2.34.0, PR #157, issue #155; 2026-08-27: token-cost reduction Phase 3, `feat/token-cost-p3-context-dedupe`, v2.35.0, issue #155; 2026-08-26: token-cost reduction Phase 1, `feat/token-cost-p1-quick-wins`, v2.33.0, issue #155; 2026-08-26: dashboard discovery fallback + EPERM-blind pid liveness fix, `fix/145-146-dash-discovery-pane-send`, v2.30.1, issues #145/#146; 2026-08-26: sandbox-escape allowlist + durable interactive-mode pinning, `fix/147-sandbox-escape-allowlist`, v2.31.0, issue #147, PR #154; 2026-08-26: machine-wide heavy-worker budget + permission-compile writer, `feat/133-132-governance-and-compile`, v2.30.0, issues #133/#132; 2026-08-25: safety-round three-issue fix, `fix/134-136-139-safety-round`, v2.29.1, issues #134/#136/#139; 2026-08-25: codex/claude permission-request audit, `feat/132-codex-permission-audit`, v2.29.0, issue #132; 2026-08-25: production profile migration and optimizer audit fixes, `fix/121-123-setup-optimize`, v2.28.0, issues #121–#130; 2026-08-25: adapter-aware injection deferral, dashboard overlay newlines, and a `gh` sandbox carve-out, `fix/118-injection-deferral-and-dash-newlines`, v2.27.0, issue #118; 2026-08-24: cross-platform effect guard, launch evidence, and audit, `feat/cross-harness-permissions`, v2.26.0; 2026-08-24: cross-harness low-noise permissions and defense-in-depth, `feat/cross-harness-permissions`, v2.25.2; 2026-08-24: two-phase pane injection + report-back reminder sweep, then a review round deferring the settle gap, `fix/codex-pane-messaging`, v2.25.2, issues #114/#115, PR #116; 2026-08-24: test-suite speedup via nextest + supervise final-drain race fix, `perf/test-suite-speed`, v2.25.1, PR #113; 2026-08-24: guided first-run setup wizard, `feature/first-run-setup`, v2.25.0; 2026-08-23: production-readiness pass + full zirv AI-setup migration, `fix/shipped-posture-allows-zirv`, v2.24.0; close all 13 open issues #83–#95 plus a malformed-`ctx.toml` fix, `feat/close-open-issues`, v2.23.0; dashboard/harness parity fix round — codex permission parity, pacing coverage, macOS usage polling, `fix/dashboard-and-harness-parity`, v2.22.0; 2026-08-21: frontend quality-contract v2, PR #78; autonomous frontend quality stack, issues #70–#77; memory/context/workflow rework merged as v2.14.0–v2.17.0; PR #59 review fixes — workflow trust gates, review integrity, evidence freshness; PR #59 verifier round — deadlock, forged-summary and fail-open gaps closed; 2026-08-20: provider-neutral workflow system, issues #47–#56; 2026-08-19: dashboard click-drag selection + OSC 52 copy, Alt+Enter folding fix, mail-loss fix for issue #30; adaptive dashboard input-poll wait, `Ctrl+A ?` help overlay; role-gated worker prompt, user-layer split, deterministic pty test boundary; 2026-08-18: live inter-session messaging; token-economy hardening of `zirv chat`; per-agent code-review model config; both usage windows shown, filtered by availability, refresh gates fixed at reset boundaries; 2026-08-16: vendor usage monitoring, use_credits gating, pace-to-reset throttle; usage returns to the dashboard header; reap child process trees on every teardown path; derived harness roster + zirv-owned review policy, dashboard sidebar ownership scoping; 2026-08-15: codex enabled out of the box; dashboard scrolling, opaque overlays, and a leaner header; 2026-08-14: round-9 review — help-probe RCE, tree-kill, atomic state writes, dashboard cursor/keys; dashboard adversarial-review fixes; 2026-08-13: `zirv chat` dashboard multiplexer, handoff harvest/status/mail split; 2026-08-12: `zirv chat`/`zirv agent` aliases, Agent enable/disable gate, Obsidian vault created).
