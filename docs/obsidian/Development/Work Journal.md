---
last-verified: 2026-09-04
---

# Work Journal

## How to use

- **Reading:** check the last 2–3 entries at the start of a session for recent context.
- **Writing:** entry after any non-trivial change (feature, refactor, bug fix, infra). Skip if a commit message already captures it.
- **Cap:** keep new entries to ~10 lines. If you need more, it's a spec or a [[Decision Log]] entry, not a journal note. Link out; don't inline.
- **Rotation:** when the active journal grows past ~10 entries, move the oldest ones to a quarterly file under `journal-archive/` (frontmatter `archived: true`, header stating the covered date range).

## Format

### YYYY-MM-DD: short title
**What:** one or two sentences.
**Key changes:** files/services touched.
**Follow-up:** anything unfinished (optional).

## Entries

### 2026-09-04: release 3.20.0 harness batch 6 -- worktree reclaim, durable task cards, objective completion judge, session attention model, operator skill (`release/3.20.0-harness-batch-6`, issues #319/#317/#314/#349/#355)
**What:** Five issues on one release branch, following batch 5. #319: `worktree.rs`'s proof-required reclaim (ahead-count/porcelain-dirt/cherry-unmatched probes feed a pure `decide`) refuses to remove anything a probe can't prove clean, archives untracked-only content before removal, records an explicit base commit plus an ownership row (`<state>/worktrees/<repo-slug>.jsonl`), and adds a dead-owner startup GC plus `zirv ctx worktree list|finalize|prune`; a worker tree with unpushed commits is now kept rather than removed. #317: `task.rs`'s durable task cards (an append-only per-repo event log materialized into `todo`/`ready`/`running`/`blocked`/`review`/`done`/`archived` cards) add claim/heartbeat/TTL, dependency gating, silent-exit-0 auto-block, and respawn refusal past an auth blocker, plus `zirv ctx task <verb>`, the atomic `zirv ctx swarm`, and `zirv ctx agent --task <id>`. #314: `judge.rs` plus `run_loop.rs` run `[objective] gates` deterministically before ever asking a cheap-model judge for `done`/`blocked`/`continue`/`wait`; `blocked` stops with a `Handoff.blocked` and parent mail, the judge fails open on any unavailable/non-conforming answer, and `max_cycles_without_progress` stops the loop independently of the judge. #349: `attention.rs`'s composed per-session status (lifecycle/attention/visibility, one authority per axis) is wired from hooks, `exec`'s stall latch and clean exit, writer-permit and spawn-gate-quota refusals, workflow gate/verification-failure transitions, and the dashboard's pane-quiescence tick; `zirv ctx explain-status`/`wait` read it back. #355: `skill.rs`/`command_schema.rs` back `zirv --skill`/`zirv skill` (the operator skill, distinct from `skill list|show`'s pre-existing engineering-skill tree) and `zirv commands [--json]` (every clap leaf classified read-only/mutating, completeness-tested); harness prompt v17 points agents at both.
**Key changes:** `src/commands/ctx/{worktree,task,judge,attention}.rs` (all new), `src/skill.rs` (new), `src/commands/command_schema.rs` (new), `assets/zirv-skill.md` (new), `src/commands/ctx/{run_loop,exec,hook,agent,config,mod,dash/mod}.rs`, `src/commands/workflow/engine.rs`, `src/{main,utils}.rs`. `Cargo.toml` bumped to 3.20.0.
**Follow-up:** Run the five verification gates, then open the PR against `main`. Residuals: no per-tick heartbeat from the supervisor loop for a claimed task card; silent-zero-exit detection for a task-claimed worker only fires with `--result-schema`; `Attention::Question`/`Attention::Permission` have no live source yet, no `ScreenManifest` authority source, and no `ctx status`/`explain-status --json`. Built 3.19.0 from the unmerged PR #347 branch into `~/bin` since the installed chocolatey 3.18.0 still carries #346 (native subagents denied repo writes); codex had no usage left, so every track ran on Claude sonnet. See [[Active Work]]'s matching entry and the 2026-09-04 [[Decision Log]] entries.

### 2026-09-04: release 3.17.0 harness batch 5 -- stall/restart-chain reliability, zero-model transcript search, window attribution, redacted snapshot, delegation envelopes (`release/3.17.0-harness-batch-5`, issues #310/#315/#312/#320/#262)
**What:** Five issues on one release branch, following batch 4. #310: `stall.rs` (pure progress-clock/latch) plugs into `exec`'s tick for stall detection (`supervise.idle_no_tool_secs`/`in_tool_secs`/`stall_grace_secs`, `EXIT_STALLED`=81); `chain.rs` persists a cross-process restart-chain breaker with six independent failure-class counters, `zirv ctx loop` cycles excluded by construction; dashboard footer gains a `◆ stalled` badge, `zirv ctx status` a `restart chain:` line. #315: `zirv ctx search` is zero-model BM25 recall over transcripts/handoffs/work artifacts/mail, persisted per-repo index, lineage dedupe, loop/exec demotion, screened and capped by operator-only `[search] max_output_bytes`. #312: `breakdown.rs` buckets a transcript's window (largest-remainder token apportioning) feeding `status --breakdown`/`score` JSON's `window_breakdown` (`Score`'s own field stays `None` by design); a reclaim-gated `/compact` Stop-hook advisory (`compact_advisory.min_reclaim_tokens`/`window_fraction`, narrow-only fold) rearms only after a full runway regrows. #320: `zirv ctx snapshot` is a redacted 64 KB-capped state summary; `zirv report --snapshot`/`--snapshot-file` attach it to an issue. #262: `envelope.rs` (`WorkerEnvelope`, may only narrow) plus `zirv ctx agent --path-scope`/`--no-network`/`--depth`, `ZIRV_ENVELOPE`/`ZIRV_PRINCIPAL` on every spawn, a locked fallback on a malformed envelope, depth-0 refusal, and safety-hook enforcement independent of the ordinary `[safety]` verdict.
**Key changes:** `src/commands/ctx/{stall,chain,search,search_index,breakdown,envelope}.rs` (all new), `src/commands/ctx/{agent,safety,log,config,hook,score,event,rot,status,dash/mod,dash/spawnreq,prompt}.rs`, `src/commands/report.rs`. `Cargo.toml` bumped to 3.17.0.
**Follow-up:** Run the five verification gates, then open the PR against `main`. Residual: the interactive `wrap` path has no stall detection yet (#310); `zirv ctx ask` was not built this batch. See [[Active Work]]'s matching entry and the 2026-09-04 [[Decision Log]] entries.

### 2026-09-04: release 3.16.0 harness batch 4 -- same-harness routing hint, baseline lock, post-edit diagnostics, worker result contracts, two safety loop breakers (`release/3.16.0-harness-batch-4`, issues #328/#302/#308/#318/#313/#331)
**What:** Six issues on one release branch. #328: `claude::ORCHESTRATOR_PROMPT` states the same-harness-vs-`zirv agent` routing rule outright and outranks a contradicting operator/repo layer; `agent::same_harness_hint` prints a one-line advisory (never a refusal) when a delegation targets the harness already running it. #302: `verification.rs` holds one per-repository `BaselineLock` (same shape as `group::GroupLock`) across baseline streak updates and `--prune`, closing a race two concurrent writers could hit. #308 stage 1: a new off-by-default `[diagnostics]` section lets the Stop hook run `cargo check`/`tsc --noEmit` after an edited turn, diff against a per-session baseline, and append only new diagnostics as a third advisory line, using its own `CARGO_TARGET_DIR` under the state dir so it never blocks on the repo's own build lock; codex is a permanent no-op (no `files_modified`). #318: `zirv agent --result-schema`/`--result-kind` declares a structural contract a worker's report-back must satisfy, validated on headless exit with one bounded resume-retry (claude only) before a `contract_failed` report-back, and enforced identically on `zirv ctx send --to-session`; a dashboard-restored pane loses the contract env (new Known Issues entry). #313 (landed pre-branch, folded into this release): two additive PreToolUse loop breakers (consecutive-denial, identical-failing-command), three `[safety]` thresholds via a zero-means-disabled `narrow_threshold`. #331: doc-comment fix pointing `is_reserved_zirv_escape_safe`'s scope note at the real #222 acceptor.
**Key changes:** `src/commands/ctx/{adapters/claude,agent,safety,log,config,diagnostics(new),hook,mail,dash/{mod,spawnreq},exec,result_schema(new),schemas/*(new)}.rs`, `src/commands/workflow/verification.rs`, `.zirv/ctx.toml`.
**Follow-up:** Run the five verification gates, then open the PR against `main`. See [[Active Work]]'s matching entry and the 2026-09-04 [[Decision Log]] entries.

### 2026-09-03: release 3.15.0 -- mail heading truncation, safety hardening, verify-on-stop nudge, permission-prompt audit, workdir visibility hint

**What:** Five-issue batch (`release/3.14.0-harness-batch-3`, rebuilt on top of main's own #325). #326: `mail::parse_markdown`'s `## Message` heading recognition now gates on a `header_seen` flag that fires exactly once, so a body's own `## Summary`/`## Findings` heading no longer re-closes the header and silently truncates the stored payload. #327/#321 item 2: `git push --mirror`/`--prune` and `git checkout -f`/`--force`/`-B` now ask unconditionally; a new `is_mixed_confined_write_and_read_only_escape_safe` combinator allows an unsandboxed retry mixing a scratchpad-confined write segment (e.g. `mkdir -p <scratch>/x`) with a read-only escape segment (e.g. a `gh` call redirecting into that same scratchpad). #309: a new `[verify_on_stop]` section (narrow-only, default on, cap 2/session) makes the Stop hook name the exact stale-gate command (`zirv test changed`/`zirv verify`) when this session modified code after the last passing verification run; `zirv ctx status` gains a `gates: fresh`/`stale` line off the same check. #321/#307/#320: `zirv ctx permissions audit` gains `--session`/`--repo`/`--all-repos` scoping (default: the current repo's own claude project directory) plus a `## Sampled transcripts` section, and a `## Permission-prompt log` section reading `logs/permission-prompts.jsonl` (populated by the pre-existing `zirv ctx hook permission` observer), grouping Read/Edit-family prompts by directory. #307: `zirv test`/`zirv verify --dry-run` are now escape-safe on an unsandboxed retry (a real run stays Ask/Deny); a Claude delegator joining a dashboard with `--workdir` outside its own repo/worktrees gets a `hint: run /add-dir <path> ...` line on the spawn ack.

**Key changes:** `src/commands/ctx/{mail,safety,hook,config,status,log,permissions,optimize,agent}.rs`. `Cargo.toml` bumped to 3.15.0.

**Follow-up:** PR pending. See [[Active Work]]'s matching entry and the 2026-09-03 [[Decision Log]] entry.

### 2026-09-02/03: release 3.12.0 -- writer-permit governance, context lint, cost ledger, speed telemetry, prefix-stability harness, narrowed VCS classifier

**What:** Six-issue batch (`release/3.12.0-harness-batch-2`). #267: `zirv ctx agent --mode read-only|writing` (default writing) plus `--worktree` gate a new writer-permit pool (`<state>/permits/writers/`, `supervise.max_writers` default 1, `REPO_FORBIDDEN`), refusing with a new retryable `EXIT_WRITER_BUSY` (80); auto-spawned review/test/verify workers are classified read-only; `zirv ctx status` gains `writers: N of M slots in use`. #275: `zirv context lint [--json] [--budget] [--fix-plan]` adds five pure findings (CTX001-CTX005: budget headroom, cross-layer duplicates, contradiction candidates, proportionality, dedupe leak), discovered as a verify-gate `CheckKind::ContextLint` whenever `.zirv/context/` exists and folded into `zirv context sync --report`. #264: `zirv ctx spend` aggregates `delegations.jsonl` by harness/model/task-class/worker, priced by a new operator-only `price.rs` table (`~/.zirv/prices.toml` override, both `price.table_path`/`stale_after_days` `REPO_FORBIDDEN`); cost lines land on `status`, `workflow stats`, and the dashboard's new aggregate row. #293: `NormalizedEvent` gains `at_ms` timestamps plus two inert sibling variants (`AssistantFirstText`/`ToolResultTimestamp`); `score::derive_speed_metrics` (turn p50/max latency, TTFT p50, tool-error rate) feeds a `workflow stats` speed line -- verified never to move a rot verdict. #299: a prompt-prefix stability test harness (`FAKE_AGENT_PROMPT_LOG`, `compile::layers_of`/`emitted_layers`) asserts a state change perturbs only its own declared prompt layer, protecting the provider cache-hit rate this release's cost/speed telemetry now makes visible. #306: the built-in VCS safety classifier narrows four ask rules (`checkout`/`restore`, `clean -f`, `worktree remove --force`, `rebase`) to fire only outside agent-scoped local work (a concrete path, a confined scratch worktree, non-interactive), removing their old blanket `Bash(git rebase *)`/`Bash(git clean *)` glob entries.

**Key changes:** `src/commands/ctx/{agent,exec,permit,config,status,log,context_lint (new),context_cli,mod,price (new),spend (new),event,window,score,safety,adapters/claude,adapters/codex,dash/mod,dash/spawnreq,dash/ui}.rs`, `src/commands/workflow/{engine,telemetry,verification}.rs`, `tests/fixtures/fake-agent.sh`. `Cargo.toml` bumped to 3.12.0.

**Follow-up:** A same-release residual was closed in-branch: a dashboard pane spawn did not originally enforce the writer-permit pool the headless fork already did (`SpawnRequest.mode` was data-parity only) -- `fulfill_spawn_request` now calls the identical `acquire_writer` gate. Still-open, not fixed here: `workflow::engine::auto_spawn_decision`'s `AutoSpawn.mode` correctly classifies Review/Test/Verify as read-only but isn't yet threaded into an actual permit acquisition, since `spawn_auto_worker` launches those as a direct subprocess rather than through `zirv ctx agent`. See [[Active Work]]'s matching entry and the 2026-09-02/03 [[Decision Log]] entries.

### 2026-09-02: release 3.11.0 -- handoff v3, resume trust manifest, verification no-progress guard, group reservation, durable objective

**What:** Five-issue batch (`release/3.11.0-harness-batch`). #280: handoff `SECTIONS` grows 7 -> 11 (Constraints/Blocked/Key decisions added, Files touched split into Files read/Files modified), `DISTILL_PROMPT_VERSION` v2 -> v3, distillation now takes the repo's own previous handoff so a restart chain carries decisions/constraints forward instead of dropping them once they age out of the transcript tail. #281: `resume`/`SessionStart` append a host-verified working-set manifest (existing `.zirv/work/<id>/` artifacts, git-changed paths) beside the handoff; `sessions::Record.in_flight` (stamped by `wrap` per keystroke, `exec` per cycle) lets a dead-pid record surface a mid-turn crash as one `<zirv_interrupted>` block on the next start; `resume_prompt_preview` keeps `zirv ctx status` from consuming the one-shot marker. #287: `GateOutcome::Unchanged` stops `--run-checks` from re-executing checks against a worktree byte-identical to the last failing attempt, still burning a step attempt. #301: `WorkGroup.reserved_tokens` + `settle_reservation` (replacing `add_spent_tokens`) make token-budget admission atomic, closing a race where two concurrent children could both see the full remaining budget. #285: new `zirv ctx objective set|show|close` plus `--objective` on `exec`/`loop` persists a durable per-repo objective as its own `PromptSource::Objective` prompt layer (`DEFAULT_PROMPT_VERSION` v9 -> v10); `objective close` refuses without fresh passing verification, so completion is never model-asserted. Rejected: #303 (`codex exec resume <id> "/compact"` as a compaction trigger) -- verified on codex-cli 0.147.0 that it sends the text as a plain user message and records no compaction at all.

**Key changes:** `src/commands/ctx/{handoff,event,adapters/claude,resume,hook,sessions,wrap,exec,group,agent,dash/mod,prompt,compile,config,mod,state}.rs`, `src/commands/ctx/objective.rs` (new), `src/commands/workflow/{verification,engine,telemetry}.rs`. `Cargo.toml` bumped to 3.11.0.

**Follow-up:** PR pending. See [[Active Work]]'s matching entry and the 2026-09-02 [[Decision Log]] entries.

### 2026-09-02: harness iteration round 3 -- review model enforced, review dispositions to memory, loop stops
**What:** A fourth audit round against workflow af4ed8ec's own deferred list shipped two narrow items: `reviewer_argv` now enforces the roster's resolved review model on the reviewer's own launch argv instead of only advising it (opus becomes the real default with `chat.model` unset); a settled `Fixed`/`Residual` review finding now writes one durable memory entry under its own `review-finding-<id>` bank. The round rejected size-aware review count, an active-pointer lock, artifact re-injection dedupe, and a codex idle heuristic, each for a concrete structural reason, and the iteration loop stops here.
**Key changes:** `src/commands/ctx/adapters/mod.rs`, `src/commands/workflow/review.rs`, `src/commands/ctx/status.rs` (unrelated test hermeticity fix, same branch).
**Follow-up:** Backlog moved to [[Active Work]]'s wrapper-redesign entry: active-pointer lock design, codex idle research spike, artifact dedupe if measured, size-aware review count if `Classification.reasons` becomes structured.

### 2026-09-02: harness iteration round 1 -- cache-stable workflow layer, single-pass session start, review deltas, verify reuse, same-error signal
**What:** Three audits (cost, speed, intelligence) of the wrapper's own hot paths, workflow 263cb162, same PR #261. Shipped: `corrections_in` is now incremental (`CorrectionCheckpoint`); session start reads memory/context files once instead of twice (`load_both_scopes`, `read_context_layer_text`); the workflow-step prompt layer moves after Context and before Memory (`DEFAULT_PROMPT_VERSION` v8 -> v9); review packages send only new/changed findings on a delta round plus an accepted-spec excerpt (schema 2 -> 4); `zirv verify` reuses fresh `zirv test` evidence for an unchanged fingerprint; `workflow advance --run-checks` and `workflow review dispose --apply-recommended`; a same-error rot signal (`same_error_weight` default 0, but its own Stop-hook advisory clause fires once `same_error_repeats` crosses `same_error_threshold`); a handoff `Verification` section.
**Key changes:** `src/commands/ctx/{hook,compile,memory,retrieval,prompt,rot,event,config,score,handoff,adapters/claude}.rs`, `src/commands/workflow/{review,verification,engine}.rs`, `.zirv/ctx.toml`.
**Follow-up:** Deferred items (reviewer model by risk, codex idle heuristic, `--attach-artifact`, harvest from failures/findings, artifact re-injection dedupe) tracked in `docs/superpowers/specs/2026-09-01-wrapper-behaviour-redesign.md`'s "Round 3" section and [[Active Work]].

### 2026-09-02: harness iteration round 2 -- close verb, verification tri-state, --attach-artifact, tool-error harvest
**What:** Four of round 3's own deferred items landed: `zirv workflow close <id> [--reason]` gives a review-capped workflow an explicit terminal exit (`WorkflowStatus::Closed`, replacing the never-constructed `Cancelled`) and fixes a `resume` bug that silently revived closed/failed workflows; `VerificationOutcome.status` is now `Passed|Failed|Unknown`, closing round 3's residual finding F1 (a compound command like `cargo test || true` no longer reads as a pass), plus F2 (`write_list` now normalizes every handoff list item); `zirv ctx agent --attach-artifact <intent|spec|plan>` appends a repo's accepted artifact to a delegated worker's task prompt, capped and labeled untrusted; `durable_harvest_prompt` gains a deduped "Repeated tool errors" block at session end.
**Key changes:** `src/commands/workflow/{engine,telemetry}.rs`, `src/commands/ctx/{event,handoff,agent,memory}.rs`.
**Follow-up:** Still deferred (reviewer model by risk, codex idle heuristic, memory-bank harvest from failures/findings, artifact re-injection dedupe across steps) — see `docs/superpowers/specs/2026-09-01-wrapper-behaviour-redesign.md`'s "Round 4" section and [[Active Work]].

### 2026-09-01: wrapper behaviour redesign -- proportionality-first prompts, tiered docs/verify
**What:** Non-Rust half of the wrapper redesign (`docs/superpowers/specs/2026-09-01-wrapper-behaviour-redesign.md`): `.zirv/context/{common,claude,codex}.md` gained a tiered verify/review/vault-keeper structure and the codex honesty clause moved to common; `CLAUDE.md`/`AGENTS.md` regenerated via `zirv context sync --generate`.
**Key changes:** `.zirv/context/*.md`, `CLAUDE.md`, `AGENTS.md`, `Cargo.toml`/`Cargo.lock` (3.9.0), vault pages below.
**Follow-up:** src/** side (prompt.rs, adapters, adoption.rs, engine.rs) owned by a parallel worker on the same branch.

Older entries: see [[journal-archive/2026-Q3|2026 Q3 archive]] (2026-09-01: wrapper behaviour redesign round 2 -- content gaps, codex parity, over-verification rot signal (`prompt.rs` `DEFAULT_PROMPT` v3 -> v4, `adapters/codex.rs` new worker/sub-orchestrator role prompts, interleave-aware `rot.rs` repetition signal); 2026-09-01: release 3.8.0 -- bug batch closing four issues across two parallel worktree tracks, v3.8.0, `release/3.8.0-bug-batch`, issues #251/#252/#253/#255; 2026-09-01: release 3.5.0 -- nine-issue batch across two parallel tracks (workflow-lifecycle + ctx/agent), v3.5.0, `feature/issue-batch-3-5-0`, issues #222/#230/#235/#236/#241-#245; 2026-09-01: waiver-blind review packages fixed + `zirv ctx status --diff` shipped, v3.4.0, `feature/238-246-review-waiver-status-diff`, issues #238/#246; 2026-08-31: dedupe pointer line/`.gitattributes` durability/real token-cost measurements/Ruflo evaluation, v3.3.0, `feature/225-240-token-cost-ruflo`, issues #225/#240; 2026-08-31: workflow adoption (detect/nudge/enforce/measure) and steady-state token reductions, v3.2.0, `worktree-issues-223-225`, issues #223/#225; 2026-08-31: bug batch -- codex capacity/account exhaustion, `zirv agent --workdir`/`--headless`, lenient review ingestion, verify-check env passthrough (plus its own review round), v3.1.0, `fix/bug-batch-227-228-229-232-233`, issues #227/#228/#229/#232/#233; 2026-08-31: PR #234 conflict resolution -- addressed mail from any cwd plus safe built-in auto-allow, v3.0.1, `fix/224-226-bug-batch`, issues #224/#226; 2026-08-31: bug batch widened to a breaking release -- headless argv budget, worktree mail read-side, frontend target root, bracketed-paste ownership, `.zirv/commands/` relayout, v3.0.0, `fix/issue-batch-220-219-206-214`, issues #220/#219/#206/#214/#212; 2026-08-30: bug batch -- baseline-waivable test gate, codex argv overflow, budget-kill exit-code fix, flaky sessions test, `worktree-fix-bug-batch-213-215-218-203`, issues #215/#213/#203/#218; 2026-08-30: install experience unified and hardened, v2.39.1, `worktree-feat-210-install-experience`, issue #210; 2026-08-30: dash TUI v3 -- fidelity fixes, rot column, footer signal row, v2.39.0, `feat/209-dash-v3`, issue #209; 2026-08-30: README refresh for the v2.38 feature set, `docs/198-readme-refresh`, issue #198 part 2; 2026-08-30: TUI redesign -- style.rs foundation, wrap chrome, dashboard chrome, CLI surfaces, v2.38.0, `feat/202-tui-redesign`, issue #202; 2026-08-30: HARNESS_PROMPT v11 -- design-review gate and autonomous lifecycle engagement, issues #204/#205, `feat/202-tui-redesign`; 2026-08-30: issue-tracker cleanup -- closures and new filings; 2026-08-29: PR #200 five-leg review round and fix wave, `release/187-ai-native-sdlc`; 2026-08-28: cross-harness token fallback, v2.36.0, issue #186, PR #189; 2026-08-28: reliable messaging -- completed and review-hardened, `release/2.35.0`, issue #177, draft PR #185; 2026-08-28: approved-prompt review and safe-list proposals, `release/2.35.0`, issue #178, draft PR #185; 2026-08-28: CLI-native Zirv issue reporting, `release/2.35.0`, issue #176, draft PR #185; 2026-08-27: PR #171 independent review round, `release/2.32.0`, v2.32.0, issue #155 Phases 4-6; 2026-08-27: repo-portable memories, `feat/172-repo-portable-memories`, v2.32.0, issue #172; 2026-08-27: token-cost reduction Phase 2, `feat/token-cost-p2-telemetry`, v2.34.0, PR #157, issue #155; 2026-08-27: token-cost reduction Phase 3, `feat/token-cost-p3-context-dedupe`, v2.35.0, issue #155; 2026-08-26: token-cost reduction Phase 1, `feat/token-cost-p1-quick-wins`, v2.33.0, issue #155; 2026-08-26: dashboard discovery fallback + EPERM-blind pid liveness fix, `fix/145-146-dash-discovery-pane-send`, v2.30.1, issues #145/#146; 2026-08-26: sandbox-escape allowlist + durable interactive-mode pinning, `fix/147-sandbox-escape-allowlist`, v2.31.0, issue #147, PR #154; 2026-08-26: machine-wide heavy-worker budget + permission-compile writer, `feat/133-132-governance-and-compile`, v2.30.0, issues #133/#132; 2026-08-25: safety-round three-issue fix, `fix/134-136-139-safety-round`, v2.29.1, issues #134/#136/#139; 2026-08-25: codex/claude permission-request audit, `feat/132-codex-permission-audit`, v2.29.0, issue #132; 2026-08-25: production profile migration and optimizer audit fixes, `fix/121-123-setup-optimize`, v2.28.0, issues #121–#130; 2026-08-25: adapter-aware injection deferral, dashboard overlay newlines, and a `gh` sandbox carve-out, `fix/118-injection-deferral-and-dash-newlines`, v2.27.0, issue #118; 2026-08-24: cross-platform effect guard, launch evidence, and audit, `feat/cross-harness-permissions`, v2.26.0; 2026-08-24: cross-harness low-noise permissions and defense-in-depth, `feat/cross-harness-permissions`, v2.25.2; 2026-08-24: two-phase pane injection + report-back reminder sweep, then a review round deferring the settle gap, `fix/codex-pane-messaging`, v2.25.2, issues #114/#115, PR #116; 2026-08-24: test-suite speedup via nextest + supervise final-drain race fix, `perf/test-suite-speed`, v2.25.1, PR #113; 2026-08-24: guided first-run setup wizard, `feature/first-run-setup`, v2.25.0; 2026-08-23: production-readiness pass + full zirv AI-setup migration, `fix/shipped-posture-allows-zirv`, v2.24.0; close all 13 open issues #83–#95 plus a malformed-`ctx.toml` fix, `feat/close-open-issues`, v2.23.0; dashboard/harness parity fix round — codex permission parity, pacing coverage, macOS usage polling, `fix/dashboard-and-harness-parity`, v2.22.0; 2026-08-21: frontend quality-contract v2, PR #78; autonomous frontend quality stack, issues #70–#77; memory/context/workflow rework merged as v2.14.0–v2.17.0; PR #59 review fixes — workflow trust gates, review integrity, evidence freshness; PR #59 verifier round — deadlock, forged-summary and fail-open gaps closed; 2026-08-20: provider-neutral workflow system, issues #47–#56; 2026-08-19: dashboard click-drag selection + OSC 52 copy, Alt+Enter folding fix, mail-loss fix for issue #30; adaptive dashboard input-poll wait, `Ctrl+A ?` help overlay; role-gated worker prompt, user-layer split, deterministic pty test boundary; 2026-08-18: live inter-session messaging; token-economy hardening of `zirv chat`; per-agent code-review model config; both usage windows shown, filtered by availability, refresh gates fixed at reset boundaries; 2026-08-16: vendor usage monitoring, use_credits gating, pace-to-reset throttle; usage returns to the dashboard header; reap child process trees on every teardown path; derived harness roster + zirv-owned review policy, dashboard sidebar ownership scoping; 2026-08-15: codex enabled out of the box; dashboard scrolling, opaque overlays, and a leaner header; 2026-08-14: round-9 review — help-probe RCE, tree-kill, atomic state writes, dashboard cursor/keys; dashboard adversarial-review fixes; 2026-08-13: `zirv chat` dashboard multiplexer, handoff harvest/status/mail split; 2026-08-12: `zirv chat`/`zirv agent` aliases, Agent enable/disable gate, Obsidian vault created).
