---
last-verified: 2026-08-27
---

# Work Journal

## How to use

- **Reading:** check the last 2–3 entries at the start of a session for recent context.
- **Writing:** entry after any non-trivial change (feature, refactor, bug fix, infra). Skip if a commit message already captures it.
- **Cap:** keep new entries to ~10 lines. If you need more, it's a spec or a [[Decision Log]] entry, not a journal note. Link out; don't inline.
- **Rotation:** when the active journal grows past ~10 entries, move the oldest ones to a quarterly file under `journal-archive/` (frontmatter `archived: true`, header stating the covered date range).

## Format

### YYYY-MM-DD: short title
**What:** one or two sentences.
**Key changes:** files/services touched.
**Follow-up:** anything unfinished (optional).

## Entries

### 2026-08-27: token-cost reduction Phase 2 — raw four-category telemetry with delegation lineage (`feat/token-cost-p2-telemetry`, v2.34.0, PR #157, issue #155)
**What:** P0 measurement work for issue #155: telemetry (`TelemetryEvent` schema v2) and `event::TranscriptUsage` now record the four raw token classes (`input`, `cache_creation`, `cache_read`, `output`) instead of folding cache creation/read into one pre-summed `input_tokens`, plus the same four classes for subagent (`isSidechain`) spend, which used to be dropped entirely. `TranscriptUsage::context_total()` recombines the three input-side classes into the same "real context size" figure every existing caller (rot's token gate, status, this page's own estimator) already expected, so nothing downstream changed behavior. `zirv ctx agent` now appends a `Delegation` cost record (`log.rs`, `delegations.jsonl`) per completed one-shot delegation — worker/parent session id, resolved model, the four raw classes off the worker's own transcript, wall time, exit code, and an outcome (`ok`/`rot-exhausted`/`timeout`/`failed`) — plus a one-line `delegation-complete` marker in the main decision log. `zirv ctx usage --sessions` (new flag) prints a per-transcript breakdown of the same four classes over a trailing 24h window (`window::session_spend`), with a cache-hit ratio and event count per session. Merged `origin/feat/token-cost-p1-quick-wins` (4 commits landed on the base since this branch forked) into this branch first — only `Cargo.toml`/`Cargo.lock`'s version field conflicted (kept 2.34.0); `prompt.rs`/`compile.rs` auto-merged clean, so both phases' changes coexist.
**Key changes:** `commands/workflow/telemetry.rs` (schema v2, six new `Option` fields, all `#[serde(default)]`), `commands/workflow/engine.rs` (`sidechain_usage_since`, `UsageCheckpoint`'s two new cumulative fields, `TransitionEvidence` threading), `commands/ctx/event.rs` (`TranscriptUsage::context_total()`), `commands/ctx/log.rs` (`Delegation`, `append_delegation`/`tail_delegations`, `DELEGATION_ACTION`), `commands/ctx/agent.rs` (`delegation_outcome`, the post-run checkpoint write), `commands/ctx/window.rs` (`SessionSpend`, `session_spend_of`/`session_spend`), `commands/ctx/usage.rs` (`--sessions` flag, `render_sessions`). `Cargo.toml` bumped to 2.34.0.
**Follow-up:** `parent_session_id`/`work_group_id` are on `TelemetryEvent`'s schema already but unpopulated by the engine as of this phase (only `session_id` is) — grouping several delegations under one parent task is deferred to a later phase. No CLI surface reads `delegations.jsonl` back yet. All five gates pass clean on the merged tree (build; nextest 2,651/2,651 outside the sandbox, 77 sandbox-induced pty/socket-openpty failures inside it that vanish outside — no introduced regressions; serial `cargo test --test-threads=1` 2,651/2,651; `cargo fmt --check` clean; `cargo clippy --all-targets -D warnings` clean). See [[Active Work]], [[Usage and Pacing]]'s new "`zirv ctx usage --sessions`" and "Four raw token classes" sections, [[Ctx Subsystem]]'s new "One-shot delegation cost accounting" note, and [[Workflows]]'s updated Telemetry section.

### 2026-08-27: token-cost reduction Phase 3 — canonical-content hash dedupe (`feat/token-cost-p3-context-dedupe`, v2.35.0, issue #155)
**What:** Task 3.1 (already landed, `d825b45`): `context_cli::render_generated` stamps a `<!-- zirv:canonical-sha256:... -->` provenance line into every generated `CLAUDE.md`/`AGENTS.md`, inside the managed prefix so an older zirv still reads the file as managed. Task 3.2 wires that stamp into compose-time injection: a session now skips re-injecting the canonical `.zirv/context/` layer when the harness's own native file provably already holds the current canonical bytes — proven, not assumed, by `compile.rs`'s `native_file_already_carries_canonical`. First pushed version (`90523d3`) compared only the native file's SELF-DECLARED header hash against a hash of the current sources, never the file's actual body; independent review caught it same-day with a working probe (tampered body, untouched header, dedupe still fired, zero canonical text delivered) and it was fixed before merge: the header hash is now only a cheap pre-filter, and the real proof is `native_text == render_generated(common, harness)`, byte-exact against a fresh render, no whitespace/CRLF normalization. A skip still reports every surface in `ContextProvenance` at `delivered_bytes: 0` (so `zirv context status` keeps seeing it, now flagged `[DEDUPED]`, checked after `[TRUNCATED]` so a genuine zero-budget truncation is never misread as a dedupe) and writes one `context-dedup-skip` decision-log entry, gated by the same `log_truncation` parameter Phase 1 added. New `context.dedupe_native` config key (default `true`), deliberately NOT `REPO_FORBIDDEN` — folded narrowing-only via new `narrow_dedupe_bool`, the `pace.enabled` T9 pattern with inverted polarity.
**Key changes:** `commands/ctx/compile.rs` (`DEDUP_SKIP_ACTION`, `native_context_path`, `native_file_already_carries_canonical`, `log_dedup_skip_decision`, `with_canonical_context_layer` gained `state`/`now` params), `commands/ctx/config.rs` (`ContextConfig::dedupe_native`, `narrow_dedupe_bool`), `commands/ctx/context_cli.rs` (`render_generated` widened to `pub(super)`, `embedded_canonical_sha256`'s `#[allow(dead_code)]` removed), `commands/ctx/context_status.rs` (`[DEDUPED]` marker in `render_per_harness_section`), `.zirv/ctx.toml` (sample `dedupe_native` line). `Cargo.toml` bumped to 2.35.0. All five verification gates clean: 2646+/2646+ tests, zero clippy warnings, fmt clean.
**Follow-up:** Phase 3 of issue #155 is now fully done pending PR review/merge. Phase 4 ("review-train convergence") is next per `docs/superpowers/plans/2026-08-26-token-cost-reduction.md`. See [[Active Work]] and the 2026-08-27 [[Decision Log]] entries.

### 2026-08-26: token-cost reduction Phase 1 — loud truncation, tighter common.md, merged memory tail (`feat/token-cost-p1-quick-wins`, v2.33.0, issue #155)
**What:** Three quick-win fixes for issue #155 (token-cost reduction), landed as one branch. Task 1.1: a canonical `.zirv/context/` layer cut by its byte budget used to vanish with no trace — `compile.rs` now writes a `context-truncated` decision-log entry (gated by a new `log_truncation` parameter, since a read-only report compiles context too) plus an unconditional stderr warning naming the surface and the config key to raise. Task 1.2: `.zirv/context/common.md` tightened from 4917 to 3898 bytes, back under its 4096-byte cap, with pin tests guarding the trim. Task 1.3: memory composition moved from two early layers (core + retrieval — double-counting a dual-selected entry's text, and invalidating the provider's prompt cache on every working-tree change, since retrieval ranks off changed paths) to one deduped layer composed at the tail, right after the canonical context layer — prompt shape v7 -> v8.
**Key changes:** `commands/ctx/compile.rs` (`TRUNCATED_ACTION`, `log_truncation_decisions`, `merge_memory_layers`, a `log_truncation` parameter threaded through `compile`/`compile_with_harness_roster` and all six launch-path call sites), `commands/ctx/prompt.rs` (`compose` no longer builds a memory layer at all), `.zirv/context/common.md`. `Cargo.toml` bumped to 2.33.0.
**Follow-up:** Phase 2 (telemetry) is next and is P0 for measuring Phases 3–6. See [[Active Work]] and the 2026-08-26 [[Decision Log]] entry.

### 2026-08-26: dashboard discovery fallback + EPERM-blind pid liveness fix (`fix/145-146-dash-discovery-pane-send`, v2.30.1, issues #145/#146)
**What:** #146: `sessions::is_alive` now treats `kill(pid,0) == EPERM` as alive, not dead — a sandboxed caller (e.g. `zirv ctx send`/`nudge` inside a dashboard pane) cannot signal even live sessions, and the old check swept every live record as stale, producing "no sessions are registered" with live sessions in the registry. #145: `agent::try_join_dashboard` now scans every other `<state>/dash/*` token directory when the inherited `DASH_REQUESTS_ENV` directory is stale/dead, joining the newest live dashboard instead of falling back to headless.
**Key changes:** `commands/ctx/sessions.rs` (`is_alive` now `pub(crate)` and EPERM-aware, `resolve_error_with_diagnostics`), `commands/ctx/mail.rs` (send-failure diagnostics), `commands/ctx/agent.rs` (`live_join_target`), `commands/ctx/dash/mod.rs` (`discover_live_dash_dirs`, `select_live_dash_dir`, `CandidateStatus`, `DashCandidate`; deleted the duplicate `pid_alive`). `Cargo.toml` bumped to 2.30.1.
**Follow-up:** EPERM-as-alive is a documented trade-off (pid recycling to a foreign uid keeps a stale record alive with no start-time disambiguator) — a follow-up issue is pending. See [[Active Work]] and the 2026-08-26 [[Decision Log]] entry.

### 2026-08-26: sandbox-escape allowlist + durable interactive-mode pinning (`fix/147-sandbox-escape-allowlist`, v2.31.0, issue #147, PR #154)
**What:** New `[safety] escape_allow` policy layer (operator-home-only, `REPO_FORBIDDEN`, seeded with a built-in read-only-utility subset of `SHIPPED_POSTURE_ALLOW`) narrows the sandbox-escape gate: every executable segment of a `--dangerously-disable-sandbox` retry must match `escape_allow` AND clear a new credential-path/root-scan screen (`escape_denied_by_screen`) before the escalation branch leaves an already-`Allow` verdict alone (matched `<sandbox: escape_allow>`), alongside the existing `gh` carve-out. The native claude settings `permissions.ask = ["Bash(dangerouslyDisableSandbox:true)"]` rule is removed outright — documented Claude behavior makes a native rule independent of the `PreToolUse` hook's own decision, so it silently defeated every hook-side escape `allow`. `sandbox.filesystem.allowWrite` now scopes the mail directory so `zirv ctx send` works sandboxed. `permissions.rs`'s claude extractor now correlates against zirv's own safety-decision log instead of guessing cause from denial text, and surfaces accepted/pending/denied interactive sandbox-escape asks for the first time; a new `permissions compile --escape` flag (claude only) writes eligible accepted asks into `[safety] escape_allow`, and both audit/compile surfaces now warn when a recommended family would still be blocked by the operator's own native claude rule.

Finishing round (PR #154) added two more fixes on top: (1) CI's "Run Tests" failure (`commands::workflow::artifact::tests::a_server_command_that_exits_immediately_is_a_failure`, ubuntu, cargo-nextest exit 100) was a genuine race — `run_interactive_with`'s failure branches read `spawn_stderr_drain`'s tail mutex the instant `try_wait`/`terminate_process_tree` observed the child dead, with no synchronization against the reader thread having actually drained the pipe yet, so a child that printed to stderr and exited in the same instant could lose that line under CI's scheduling. `spawn_stderr_drain` now also returns its `JoinHandle`, and both failure branches call a new bounded `wait_for_stderr_drain` (`STDERR_DRAIN_BUDGET` = 500ms, mirrors `supervise::FINAL_DRAIN_BUDGET`'s identical fix for the analogous `OutputTap` race) before reading the tail. (2) Durable interactive-mode pinning: the hook (`run_check_hook_mode_with_env`) used to classify `LaunchMode` from Claude's self-reported `permission_mode` alone, so an operator whose native `defaultMode` is `"auto"` (not `"default"`/`"plan"`/`"acceptEdits"`) had every genuinely interactive session fail closed to `Headless`, asking on everything. A new `adapters::LAUNCH_MODE_ENV` (`ZIRV_CTX_LAUNCH_MODE=interactive`) is pushed into `turn_env` at every real interactive-launch seam — `wrap.rs::run_with` (covers `zirv ctx wrap` and, via `chat.rs`, `zirv chat`'s wrap fallback), `dash/mod.rs::run_dashboard`'s first/orchestrator pane (always interactive), and `dash/mod.rs::fulfill_spawn_request`'s worker panes (gated on the identical `req.interactive` signal `worker_pane_extra_args` already uses) — reusing each site's existing `LaunchMode`/interactivity signal rather than re-deriving it. The hook's own `safety::launch_mode_pinned_interactive(env)` trusts the pin over an unproven self-report; listed in `sessions::SUPERVISION_ENV` so a nested headless launch (`exec`/`loop`, or a non-interactive spawn request) never inherits its parent's proof.
**Key changes:** `commands/ctx/safety.rs` (`escape_allow`, `builtin_escape_allow`, `escape_denied_by_screen`, `escape_allow_matches`, `command_fails_escape_screen`, `SANDBOX_DENY_READ_HOME_PATHS`, `launch_mode_pinned_interactive`), `commands/ctx/config.rs` (`REPO_FORBIDDEN` entry), `commands/ctx/adapters/claude.rs` (`launch_settings_value`'s third `mail_write_dir` param, native ask rule removed), `commands/ctx/adapters/mod.rs` (`LAUNCH_MODE_ENV`, `LAUNCH_MODE_INTERACTIVE_VALUE`, `launch_mode_pin_env`), `commands/ctx/sessions.rs` (`SUPERVISION_ENV` grown to 5), `commands/ctx/wrap.rs` (`turn_env` pin push), `commands/ctx/dash/mod.rs` (pin push at both pane-spawn seams), `commands/ctx/log.rs` (`SafetyDecisionRecord`/`read_safety_decisions`), `commands/ctx/permissions.rs` (`permission_mode`/`is_sidechain` fields, `correlate_safety_decision`, `AuditReport.requests`, `CompileArgs.escape`, `escape_eligibility`, `read_native_claude_rules`/`native_conflict`), `commands/setup.rs` (`union_home_safety_escape_allow`/`read_home_safety_escape_allow`), `commands/workflow/artifact.rs` (`spawn_stderr_drain`/`wait_for_stderr_drain` race fix). `Cargo.toml` bumped to 2.31.0.

**Review round 1 (2026-08-27) fixed two escalations found in the above, both landed in the same PR #154 before merge.** (1) CRITICAL: `escape_denied_by_screen` never checked for unquoted output/input redirection, so a seeded read-only family's own pattern (e.g. `"echo *"`) matched straight through a trailing ` > ~/.claude/settings.json` — an unsandboxed *write* via the built-in escape seed, in both interactive and headless mode. Now routed through `contains_unquoted_redirection` (the primitive the `gh` carve-out already trusted): any unquoted `>`/`>>`/`<` denies the escape screen outright, seeded family or not; applied identically to `permissions.rs`'s `--escape` compile eligibility. (2) IMPORTANT: the paragraph above describes the durable interactive-launch pin for a dashboard worker pane as "gated on the identical `req.interactive` signal `worker_pane_extra_args` already uses" — that was the bug. `SpawnRequest.interactive` is untrusted JSON any process able to reach the requests directory (capability-protected, not authenticated) can forge, so a forged `req-*.json` claiming `"interactive": true` could win the durable pin with nobody actually watching a dashboard. `fulfill_spawn_request` now takes an explicit `trusted_interactive: bool` parameter supplied by the CALLER, never derived from `req` itself: only the dashboard's own in-process Spawn overlay passes `true`; the file-backed drop directory's consumer (`handle_spawn_requests`) always passes a named `FILE_DROP_TRUSTED_INTERACTIVE = false` constant, regardless of what a forged request file claims. `req.interactive` still drives the pre-existing, out-of-scope `worker_pane_extra_args` posture choice, unchanged. Pinned by `spawn_launch_mode_pin_ignores_req_interactive_and_only_trusts_the_caller` (pure decision-function test) and `a_forged_interactive_spawn_request_still_spawns_normally` (the forged request must still spawn — forging denies only the pin, not the spawn itself).
**Key changes (review round 1):** `commands/ctx/safety.rs` (`escape_denied_by_screen` redirection check), `commands/ctx/permissions.rs` (`command_fails_escape_screen` same fix), `commands/ctx/dash/mod.rs` (`fulfill_spawn_request`'s `trusted_interactive` param, `spawn_launch_mode_pin`, `FILE_DROP_TRUSTED_INTERACTIVE`).
**Follow-up:** All five gates pass; failures are exactly the documented pre-existing baseline (name-for-name), none introduced. A CI-only fix landed on top after review: `a_forged_interactive_spawn_request_still_spawns_normally` did a real pty spawn without pinning `cfg.agent_bin` to a guaranteed-real binary (the ABSOLUTE rule every other real-pty-spawn test in `dash/mod.rs` already follows), so it passed by accident on any machine with a real `claude` binary on `PATH` and failed on a clean CI runner — fixed by adding the same `sleep 3`/`ping -n 3 127.0.0.1` override the sibling tests use; no production code changed. See the 2026-08-26/27 [[Decision Log]] entries and [[Command Safety]]'s "The `escape_allow` sandbox-escape gate"/"The `--escape` flag" sections.

### 2026-08-26: machine-wide heavy-worker budget + permission-compile writer (`feat/133-132-governance-and-compile`, v2.30.0, issues #133/#132)
**What:** Two features stacked on `fix/134-136-139-safety-round`. #133 (the BSOD incident: four kernel bugchecks in 12 minutes under two concurrent unbounded build/test workloads under zirv supervision): a machine-wide `supervise.max_heavy_workers` budget (default 1, `REPO_FORBIDDEN`), refuse-not-queue gates at `exec.rs` and `dash::fulfill_spawn_request`, and a `heavy workers: N of M` status line. #132 residual: `zirv ctx permissions compile` writes eligible audit-report families into `[safety] allow` in the operator's home config, `audit` staying strictly read-only.
**Key changes:** `commands/ctx/config.rs` (`SuperviseConfig::max_heavy_workers`), `commands/ctx/sessions.rs` (`count_heavy_workers`/`count_heavy_workers_among`), `commands/ctx/exec.rs`, `commands/ctx/dash/mod.rs`, `commands/ctx/status.rs` (refactored to fetch `sessions::list` once and share it), `commands/ctx/permissions.rs` (`FamilyGroup::protected`, `PermissionsVerb::Compile`, `run_compile`, `compile_eligibility`), `commands/setup.rs` (`union_home_safety_allow`/`union_allow_patterns`/`read_home_safety_allow`). `Cargo.toml` bumped to 2.30.0.
**Follow-up:** A real regression was caught and fixed during this work, not just a design risk: `status.rs`'s first draft called `sessions::list` twice per request, and `list`'s stale-record sweep (a documented side effect of being called at all) meant the second call silently lost a dead-pid record the `sessions:` block should have still shown — caught by the pre-existing `status_marks_a_session_whose_process_is_gone_as_stale` test. Fixed by fetching the registry once and threading it through. See the 2026-08-25 [[Decision Log]] entries (refuse-not-queue; compile-writes-home-only) and [[Ctx Supervisors]]/[[Command Safety]]'s new sections.

### 2026-08-25: safety-round three-issue fix (`fix/134-136-139-safety-round`, v2.29.1, issues #134/#136/#139)
**What:** Closed three independent safety/adapter bugs on one branch, TDD per fix. #134: `zirv agent codex` (headless) failed outright because `codex exec` on current codex-cli (0.149.x) rejects `--ask-for-approval` — a new exec-scoped probe (`EXEC_ASK_FOR_APPROVAL_SUPPORT`, mirroring `IGNORE_FLAGS_SUPPORT`'s shape) falls back to `-c approval_policy=<value>` on a headless launch when unsupported; interactive is unaffected. `AgentAdapter::policy_args` gained a `LaunchMode` parameter to know which surface it targets. #136: the command-safety scanner glob-matched deny patterns against a `git commit -m` argument's own quoted prose, denying commits that merely *documented* dangerous primitives by name. `redact_opaque_message` (blunt value-blanking, never touching `command_substitutions`' own unredacted-text extraction) and `redact_single_quoted_heredocs` (POSIX-literal heredoc bodies, applied once at `normalize_segments`'s top) close it — a live `$(...)` inside a message still escalates. #139: a widened operator policy silently degraded a live session to ask-on-everything with a misleading hook explanation. `evaluate_with_attestation_evidence` now computes a `SnapshotDivergence`, `explain_text` names it, `run_explain` is routed through the same evidence function the hook uses (agreeing with it, unchanged without the attestation env vars), and `sessions::Record::safety_policy_sha256` + `status.rs`'s stale-line check surface it in `zirv ctx status`.
**Key changes:** `commands/ctx/adapters/{codex,claude,mod}.rs`, `commands/ctx/safety.rs`, `commands/ctx/sessions.rs`, `commands/ctx/status.rs`, `commands/ctx/{wrap,exec,run_loop}.rs` (fingerprint stamped at registration), `commands/ctx/dash/mod.rs` (test fixture field only — `Pane::spawn` does not yet thread a `SafetyPolicy` through, documented residual). `Cargo.toml` bumped to 2.29.1. All Windows gates pass with only the established `wrap::win::a_supervised_wrap_binds_a_turn_signal_transport` baseline failure; Docker (`rust:1-bookworm`, non-root) build/`wrap::` tests (125/125)/clippy all exit 0.
**Follow-up:** commits not pushed, no PR opened per instruction. `docs/obsidian/Modules/{Command Safety,Ctx Adapters,Ctx Subsystem}.md` updated.

### 2026-08-25: codex/claude permission-request audit (`feat/132-codex-permission-audit`, v2.29.0, issue #132)
**What:** Added `zirv ctx permissions audit --agent <codex|claude> --sessions <N> [--json]`, a transcript-backed audit answering issue #132's production evidence (33 escalated Codex requests in one session, many one-off or downstream-`jq`-only approvals): groups requests by a normalized command family and reports whether a saved approval would match the next equivalent invocation.
**Key changes:** new `commands/ctx/permissions.rs` (codex `require_escalated` `exec` extractor; claude headless `dontAsk` `PreToolUse`-denial extractor, grounded in real transcripts inspected under `~/.claude/projects/`); widened six `safety.rs` helpers (`pipeline_stages`, `unwrap_env_prefix`, `unwrap_shell_wrapper`, `strip_program_dir`, `sql_program_name`, `collapse_whitespace`) from private to `pub(crate)` for reuse, no behavior change; `optimize.rs`'s new `permission_noise_findings` folds a noisy audit into the standard report; `mod.rs` wires the new `CtxVerb::Permissions`; two new fixtures under `tests/fixtures/`; `docs/obsidian/Modules/{Ctx Subsystem,Command Safety}.md` updated. `Cargo.toml` bumped to 2.29.0. A same-day review round (`3c2adf8`) added a `is_protected_family` gate (destructive git/publish-release/credential/global-install families never recommended a standing allow regardless of reusability — see the 2026-08-25 [[Decision Log]] entry), `env -C`/`--chdir`/`-S`/`--split-string` handling in `unwrap_env_prefix` (live `evaluate()` behavior, not just the audit), and a decorated-pipe-target unwrap for `is_reusable`'s filter check.
**Follow-up:** the audit reports and recommends but does not yet compile a recommendation into an applied `[safety]`/`[policy]` edit — see [[Known Issues]] and [[Command Safety]]'s "Known gap" note.

### 2026-08-25: production profile migration and optimizer audit fixes (`fix/121-123-setup-optimize`, v2.28.0, issues #121–#130)
**What:** Exercised `zirv setup apply`, `zirv setup profile`, both deterministic/model-backed optimization paths, and the repository's commit workflow against the real global Claude/Codex setup, previewing every write-capable Zirv command first. Filed and fixed ten production gaps: dry-runnable complete profile management; accurate optional-path and model-evidence optimization; operator-profile-independent tests; deterministic fake-agent readiness; portable Codex wrap expectations; and a commit workflow that now refuses protected/detached branches before pushing `HEAD` to its same-named upstream.
**Key changes:** `commands/setup.rs`, `commands/ctx/{handoff,optimize,testenv,wrap}.rs` and test launch seams, fake-agent/model fixtures, `.zirv/context/common.md` plus generated `CLAUDE.md`/`AGENTS.md`, `.zirv/commit.yaml`, setup/ctx adapter documentation, and focused regressions. `Cargo.toml` bumped to 2.28.0. The verified release binary and dry-run-validated global profile are installed: Claude/Fable orchestration, Codex `gpt-5.6-sol` workers, Claude Sonnet review with Opus reserved for high-risk review in the managed global prompt, sandbox enabled, 8 KiB common-context budget, complete hooks, and tee-wrapped custom statusline. The final model optimizer rejected three ungrounded findings and retained five informational duplicates in legacy `~/CLAUDE.md`; shared-memory optimization found no applicable consolidation and reported one core-regeneration opportunity. All five required gates pass on the final tree: build, nextest 2,518/2,518, serial test 2,518/2,518, rustfmt, and clippy with warnings denied.
**Follow-up:** Push the branch and open the closing PR; optionally remove the five legacy global duplicates by hand from the report.

### 2026-08-25: adapter-aware injection deferral, dashboard overlay newlines, and a `gh` sandbox carve-out (`fix/118-injection-deferral-and-dash-newlines`, v2.27.0, issue #118)
**What:** Closed issue #118 (wrap's own codex composer paste-fold residual) and, on the same branch, gave the dashboard's own compose-style overlays multi-line input and exempted read-only `gh` from the mandatory sandbox-escalation ask/deny. New `Capabilities::defer_injection_submit` (codex: `true`, claude: `false`) makes `wrap`'s T13 mail-advisory injection adapter-aware: a defer adapter gets the advisory text written alone, then its submitting `\r` drained by the pump loop after the shared `INJECTION_SUBMIT_DELAY` (50ms, now defined once in `ctx/mod.rs`, re-exported by `dash::pane`). `inject_compact`/`Action::Compact` deliberately stays single-burst — unreachable for codex, since `may_inject` needs a turn signal codex's adapter never posts, documented directly in code rather than left as an unexplained residual. The dashboard now negotiates kitty keyboard enhancement (`DISAMBIGUATE_ESCAPE_CODES`, popped on teardown) so Shift+Enter carries its modifier on the wire, and all four zirv-owned overlays (mail compose, spawn, memory, nudge) insert a newline on Shift+Enter/Alt+Enter instead of submitting, rendered through a new `draft_lines` helper; `nudge_overlay_reduce` was extracted out of `run_dashboard`'s inline match for parity with the other three reducers. Separately, `is_sandbox_bypass_safe_gh_command` carves a single, simple, already-`Allow` read-only `gh` invocation (`issue view/list`, `pr view/list/diff/checks`, `repo view`, `run view/list`) out of the mandatory unsandboxed-retry escalation, since `gh` always needs to read credential config the sandbox denies outright.
**Key changes:** `commands/ctx/event.rs` (`Capabilities::defer_injection_submit`), `commands/ctx/mod.rs` (`INJECTION_SUBMIT_DELAY`, moved here), `commands/ctx/adapters/{claude,codex,mod}.rs`, `commands/ctx/wrap.rs` (`write_mail_advisory`/`write_mail_advisory_phase1`, `MailWatch::pending_submit`, the `ChunkReader`-bounded `read_until` test helper), `commands/ctx/dash/pane.rs` (`INJECTION_SUBMIT_DELAY` re-export), `commands/ctx/dash/mod.rs` (`push_keyboard_enhancement`, `teardown_terminal(bool)`, `nudge_overlay_reduce`, `NudgeSubmit`, Shift+Enter/Alt+Enter arms on all four overlay reducers), `commands/ctx/dash/ui.rs` (`draft_lines`), `commands/ctx/safety.rs` (`is_sandbox_bypass_safe_gh_command`, `SANDBOX_BYPASS_SAFE_GH_FORMS`, `contains_unquoted_redirection`). `Cargo.toml` bumped to 2.27.0.
**Follow-up:** [[Ctx Adapters]], [[Ctx Supervisors]], [[Command Safety]], [[Known Issues]], and the 2026-08-25 [[Decision Log]] entries updated. See [[Active Work]].

### 2026-08-24: cross-platform effect guard, launch evidence, and audit (`feat/cross-harness-permissions`, v2.26.0)
**What:** Closed the final native-shell gaps without changing the low-noise product rule. Supervised and setup-installed Claude hooks now match `Bash|PowerShell`; quote-aware structural analysis understands lone `&` while preserving redirections; semantic floors classify generated cleanup, remote HTTP mutation, direct credential access/upload, destructive Git effects, infrastructure/service destruction, and irreversible package/release operations through Unix, PowerShell, `cmd.exe`, executable paths and wrapper extensions.
**Key changes:** Each Claude launch writes a SHA-256-addressed immutable policy snapshot/settings pair; the hook validates it, evaluates launch and current policy, keeps the stricter verdict, and fails closed on tampering. Daily safety-decision JSONL stores command hash, policy fingerprints, attestation, verdict and rule evidence—never raw command text. `sha2` 0.11.0 added and version raised to 2.28.0. Landstrip, Arapuca and MXC were evaluated but not adopted as a silent mandatory broker; [[Known Issues]] keeps native-Windows Claude's kernel-containment gap explicit.
**Follow-up:** Build, rustfmt, clippy and `cargo test --bin zirv --no-run` pass. Full serial: 2,409/2,414 pass; the five failures are exactly the established Windows `wrap` names. All 88 safety tests pass inside that run. `cargo nextest run --no-fail-fast` remains unavailable (`cargo-nextest` is not installed). A corrupted old MSVC incremental/PDB artifact and one Rust 1.96.1 dependency ICE were bypassed with a clean worktree-local target and one build job; the clean verification then completed. Orchestrator PR review remains, with Codex review skipped per operator instruction.

### 2026-08-24: cross-harness low-noise permissions and defense-in-depth (`feat/cross-harness-permissions`, v2.25.2)
**What:** Executed the approved twelve-task plan: (1) compiler-enforced `LaunchMode` at all seven real-launch seams; (2) narrow reversible-danger ask set, zirv-self-destruction denies, and curl/wget rebalance; (3) inverted interactive Claude posture with mode-specific unmatched defaults and explicit hook allow; (4) product acceptance corpus; (5) pure conservative SQL classifier; (6) SQL config/wiring; (7) probe-gated Codex `on-request`; (8) mode-aware policy support/reports and reported interactive baseline; (9) mode-aware safety explain plus the issue-#102 `dontAsk` re-scope; (10) v2.26.0; (11) vault/README update; (12) full local and cross-platform verification gates.
**Key changes:** Interactive Claude now uses `default` with conservative Design B (no blanket native `Bash(*)`; explicit hook allow carries everyday/unknown commands), while headless stays `dontAsk`; interactive Codex uses `on-request` only when a bounded cached `--help` probe proves it and otherwise falls back to `never`. `SHIPPED_POSTURE_ALLOW`/`_ASK`/`_DENY`, two launch defaults, conservative read-only SQL recognition, and mode-aware policy/explain output make the low-noise/fail-closed split explicit. The mandatory Claude Code 2.1.241 hook probe reached `permissionMode: default` but the account rate-limited before Bash, so the ask-over-native-allow contract remains honestly unverified rather than assumed.
**Hardening:** The post-plan audit fixed SQL's raw-command-only bypass, then adopted Dippy's most-restrictive structural fold for quote-aware compound commands, recursive inline shells, `$()` and backticks. Claude launches now attest a private hook/sandbox/credential settings layer every time and guard `dangerouslyDisableSandbox` in both native settings and hook logic; current Codex CLIs gain capability-probed `--approve-for-me` automatic boundary review. Automatic approval leases were rejected because PostToolUse cannot distinguish “allow once” from “allow for session.”
**Follow-up:** Final foreground verification on v2.27.0: build, rustfmt, clippy and cfg(unix) no-run compilation pass; focused safety 74/74; serial 2,393/2,398 with only the five established native-Windows `wrap` failures. Nextest was unavailable because `cargo-nextest` is not installed. Operator-executed gates remain: rerun the real Claude TTY hook-contract probe after quota resets; run the Linux/Docker tests and Docker AI-feature matrix. Native Windows Claude has no supported OS sandbox and remains an honestly recorded containment gap. See [[Active Work]], [[Command Safety]], [[Ctx Adapters]], [[Known Issues]], and the 2026-08-24 [[Decision Log]] entries.
Older entries: see [[journal-archive/2026-Q3|2026 Q3 archive]] (2026-08-24: two-phase pane injection + report-back reminder sweep, then a review round deferring the settle gap, `fix/codex-pane-messaging`, v2.25.2, issues #114/#115, PR #116; 2026-08-24: test-suite speedup via nextest + supervise final-drain race fix, `perf/test-suite-speed`, v2.25.1, PR #113; 2026-08-24: guided first-run setup wizard, `feature/first-run-setup`, v2.25.0; 2026-08-23: production-readiness pass + full zirv AI-setup migration, `fix/shipped-posture-allows-zirv`, v2.24.0; close all 13 open issues #83–#95 plus a malformed-`ctx.toml` fix, `feat/close-open-issues`, v2.23.0; dashboard/harness parity fix round — codex permission parity, pacing coverage, macOS usage polling, `fix/dashboard-and-harness-parity`, v2.22.0; 2026-08-21: frontend quality-contract v2, PR #78; autonomous frontend quality stack, issues #70–#77; memory/context/workflow rework merged as v2.14.0–v2.17.0; PR #59 review fixes — workflow trust gates, review integrity, evidence freshness; PR #59 verifier round — deadlock, forged-summary and fail-open gaps closed; 2026-08-20: provider-neutral workflow system, issues #47–#56; 2026-08-19: dashboard click-drag selection + OSC 52 copy, Alt+Enter folding fix, mail-loss fix for issue #30; adaptive dashboard input-poll wait, `Ctrl+A ?` help overlay; role-gated worker prompt, user-layer split, deterministic pty test boundary; 2026-08-18: live inter-session messaging; token-economy hardening of `zirv chat`; per-agent code-review model config; both usage windows shown, filtered by availability, refresh gates fixed at reset boundaries; 2026-08-16: vendor usage monitoring, use_credits gating, pace-to-reset throttle; usage returns to the dashboard header; reap child process trees on every teardown path; derived harness roster + zirv-owned review policy, dashboard sidebar ownership scoping; 2026-08-15: codex enabled out of the box; dashboard scrolling, opaque overlays, and a leaner header; 2026-08-14: round-9 review — help-probe RCE, tree-kill, atomic state writes, dashboard cursor/keys; dashboard adversarial-review fixes; 2026-08-13: `zirv chat` dashboard multiplexer, handoff harvest/status/mail split; 2026-08-12: `zirv chat`/`zirv agent` aliases, Agent enable/disable gate, Obsidian vault created).
