---
last-verified: 2026-08-27
---

# Active Work

> Claude: update at session start and end for seamless handoffs.

Entries use the format:

```
### <title> — <state> (`branch-name`, YYYY-MM-DD)
**Status (date):** what's true right now.
**Next:** the next concrete step.
```

## In Progress

### Machine-wide heavy-worker budget + permission-compile writer — implemented, not yet pushed (`feat/133-132-governance-and-compile`, 2026-08-25/26, v2.30.0, issues #133/#132)
**Status (2026-08-26):** Two features on one branch, stacked on `fix/134-136-139-safety-round`. **#133** (BSOD incident — four kernel bugchecks in 12 minutes under two concurrent unbounded build/test workloads): new `supervise.max_heavy_workers` (`config.rs`, default 1, `REPO_FORBIDDEN`, `ZIRV_CTX_SUPERVISE_MAX_HEAVY_WORKERS`), a machine-wide enumeration (`sessions::count_heavy_workers`/`count_heavy_workers_among`, counting live `Verb::Exec`/`Verb::Dash` only — never `Wrap`/`Chat`, operator-attended, and deliberately not `Loop`, out of scope), refuse-not-queue gates at `exec.rs` (immediately before `SessionGuard::register`, covering `zirv agent`'s headless fallback too) and `dash::fulfill_spawn_request` (a non-retryable `SpawnRefusal::policy`), and an unconditional `heavy workers: N of M slots in use` line in `zirv ctx status` that degrades silently on a config load error. A real regression surfaced and was fixed during this work: `status.rs` used to call `sessions::list` twice per request (once for the new count, once inside `sessions_lines`) — `list` sweeps a stale record's file from disk as a side effect of being called at all, so the second call found the first call's swept record already gone and silently dropped it from the `sessions:` listing (`status_marks_a_session_whose_process_is_gone_as_stale` caught it). Fixed by fetching `list` exactly once in `run_with` and threading the result into both `count_heavy_workers_among` and a refactored `sessions_lines` that now takes records as a parameter instead of calling `list` itself. **#132 residual:** new `zirv ctx permissions compile --agent <codex|claude> --sessions <N> [--dry-run]` — `audit` stays read-only; `compile` runs the identical pipeline and WRITES eligible (`!protected && reusable`, re-verified independently at write time, never trusting the cached `FamilyGroup.protected` field — new) families as `"<family> *"` patterns into `[safety] allow` in `~/.zirv/ctx.toml` only, via a new `setup.rs` union writer (`union_home_safety_allow`/`union_allow_patterns`/`read_home_safety_allow`) that unions rather than clobbers, backs the pre-write file up to `ctx.toml.bak`, and warns about comment loss. A regression test resolves the real `[safety]` policy from the written file and confirms the compiled pattern matches a *different* command in the same family, not just the audit's own sample. `Cargo.toml` bumped to 2.30.0.
**Next:** Run the remaining verification gates (fmt/clippy/serial-test) and open the PR against `fix/134-136-139-safety-round`. See [[Ctx Supervisors]]'s new "Machine-wide heavy-worker budget" section, [[Command Safety]]'s new "Compiling recommendations into approvals" section, [[Ctx Subsystem]], [[Untrusted Configuration]], and the 2026-08-25 [[Decision Log]] entries (refuse-not-queue; compile-writes-home-only).

### Codex/claude permission-request audit — implemented, PR pending (`feat/132-codex-permission-audit`, 2026-08-25, v2.29.0, issue #132)
**Status (2026-08-25):** New `zirv ctx permissions audit --agent <codex|claude> --sessions <N> [--json]` verb (`permissions.rs`) groups escalated (codex `sandbox_permissions: "require_escalated"` `exec` requests) and denied (claude headless `dontAsk` `PreToolUse` denials) requests by a normalized command family, reusing `safety.rs`'s wrapper-normalization helpers (widened to `pub(crate)`) so `env`/shell wrappers collapse to the same family as the bare command. Each family gets a reusability verdict (false on a long quoted literal payload, or a pipe into `jq`/`grep`/`awk`/`sed`) and a recommendation — a same-day review round (`3c2adf8`) added a separate `is_protected_family` gate so destructive git/publish-release/credential/global-install families are never recommended a standing allow regardless of reusability, plus `env -C`/`--chdir`/`-S`/`--split-string` handling in `unwrap_env_prefix` (the last bails out rather than misparse) and a decorated-pipe-target unwrap so `... | env NO_COLOR=1 jq .` is still recognised as a `jq` filter. `optimize.rs`'s new `permission_noise_findings` folds a noisy audit (>5 total requests) into the standard findings report as one `"permission-noise"` finding. Two regression fixtures (`tests/fixtures/codex-rollout-permission-requests.jsonl`, `tests/fixtures/claude-transcript-permission-requests.jsonl`, the claude one grounded in real transcript shapes inspected on the maintainer's machine) cover the command shapes from the issue's own production evidence. `Cargo.toml` bumped to 2.29.0. See [[Command Safety]]'s "Permission audit" section for the full model.
**Next:** Run the five verification gates and open the PR. The "compile operator-owned managed policy into reusable approvals" acceptance row is now implemented on `feat/133-132-governance-and-compile` (see the entry above) rather than left as a filed gap.

### Cross-harness command permission policy — design approved, spec review pending (2026-08-24, target v2.26.0)
**Status (2026-08-24):** `docs/superpowers/specs/2026-08-24-cross-harness-permissions-design.md` lays out a design (approach "C — keep both models, sharpen the seam") for making read-only work run silently, mutating/destructive commands prompt, currently-denied commands ask instead of dying, and every zirv-wrapped harness behave the same, all from one zirv-owned policy — without the operator configuring Claude or Codex directly. Keeps `safety.rs` (issue #83, harness-neutral command classifier) and `policy.rs` (issue #43, capability-stance model) as separate layers rather than merging them: `policy.rs` governs the whole tool surface coarsely per capability, `safety.rs` classifies shell command strings inside `ShellExec` more finely (gaining destructive-command `ask` built-ins and a SQL statement classifier), and new work concentrates in the projection seam that compiles both into each harness's native launch mechanism.
**Next:** spec review, then implementation against target v2.26.0. Not yet a branch/PR as of this entry.

### Frontend quality workflow — nine-commit draft PR green (`feat/frontend-quality`, 2026-08-21, v2.19.0, PR #78)
**Status (2026-08-21):** Issues #70–#77 plus the focused quality-contract v2 enhancement are implemented as nine dependency-ordered commits in draft PR #78 targeting `main`. Surface modes, product/design thesis and whole-journey reasoning, a 44-rule detector with complete inventory coverage, narrow/intermediate/wide evidence, a mandatory 13-dimension ≥4/5 AI-review gate, and durable task context all remain zero-touch. GitHub CI run 224 passed versioning, build, all 1,945 tests, rustfmt, Linux clippy, and Windows-target clippy.
**Next:** maintainer review; mark PR #78 ready and merge the top stack PR when desired.

## Recently Completed

### Dashboard discovery fallback + EPERM-blind pid liveness fix — implemented, PR pending (`fix/145-146-dash-discovery-pane-send`, 2026-08-26, v2.30.1, issues #145/#146)
**Status (2026-08-26):** Two commits on top of `2b24467`. #146: `sessions::is_alive` used to read any `kill(pid, 0)` failure as "dead," conflating `ESRCH` (genuinely gone) with `EPERM` (exists, this caller just lacks permission to signal it) — exactly what a sandboxed `zirv ctx send`/`nudge` running inside a dashboard pane gets probing its own dashboard's sessions, so `list` swept every live record as `Stale` and every send/nudge failed with "no sessions are registered" despite live sessions in the registry. `is_alive` is now `pub(crate)`, EPERM reads as alive, and it is the single liveness primitive the whole module (and, since this fix, `dash/mod.rs` too — its own duplicated `pid_alive` was deleted) shares. Documented trade-off, not fixed here: a pid the kernel recycled to a foreign-uid process keeps a stale record alive until that pid frees, with no start-time disambiguator yet — a follow-up issue is pending. `mail`/`sessions` resolve-failure messages now name the state dir actually checked and whether `ZIRV_CTX_STATE_DIR` was set (`resolve_error_with_diagnostics`). #145: `agent::try_join_dashboard` now falls back to scanning every other `<state>/dash/*` token directory (`dash::discover_live_dash_dirs`/`select_live_dash_dir`) when the inherited `DASH_REQUESTS_ENV` directory is stale or its owner dead — newest `owner.pid` mtime wins, lexicographic tie-break, every candidate logged with its reason (dead owner / no pidfile / live-not-selected / joined); no scan when the env var was never set. Cross-repo join stays safe — an accepted pane always spawns at the request's own cwd, never the joined dashboard's. `Cargo.toml` bumped to 2.30.1.
**Next:** open the PR. Follow-up issue to file: a start-time disambiguator for the EPERM-as-alive pid-recycling trade-off. See [[Ctx Subsystem]], [[Ctx Supervisors]], and the 2026-08-26 [[Work Journal]]/[[Decision Log]] entries.

### Sandbox-escape allowlist + durable interactive-mode pinning — implemented, PR #154 (`fix/147-sandbox-escape-allowlist`, 2026-08-26, v2.31.0, issue #147)
**Status (2026-08-26):** New `[safety] escape_allow` policy layer (operator-home-only, `REPO_FORBIDDEN`, seeded with a built-in read-only-utility subset of `SHIPPED_POSTURE_ALLOW`) narrows the `--dangerously-disable-sandbox` escalation: every executable segment of a retry must match `escape_allow` AND clear a new credential-path/root-scan screen (`escape_denied_by_screen`) before the hook lets an already-`Allow` verdict through (matched `<sandbox: escape_allow>`), alongside the existing `gh` carve-out. The native claude settings `permissions.ask = ["Bash(dangerouslyDisableSandbox:true)"]` rule is removed outright — documented Claude behavior makes a native rule independent of the `PreToolUse` hook's own decision, so it silently defeated every hook-side escape `allow`. `sandbox.filesystem.allowWrite` now scopes the mail directory so `zirv ctx send` works inside a sandboxed session. `permissions.rs`'s claude extractor now correlates against zirv's own safety-decision log (`log::read_safety_decisions`) instead of guessing cause from denial text, surfaces accepted/pending/denied interactive sandbox-escape asks for the first time (invisible before, since an accepted ask never produces an errored `tool_result`), and a new `permissions compile --escape` flag (claude only) writes eligible ones into `[safety] escape_allow`; both audit/compile surfaces now print a WARNING when a recommended family would still be blocked by the operator's own native `~/.claude/settings.json` rule.

Finishing round added durable interactive-mode pinning: the hook used to trust only Claude's self-reported `permission_mode`, so an operator's native `defaultMode: "auto"` made every genuinely interactive session fail closed to `Headless`. `adapters::LAUNCH_MODE_ENV` is now set on every real zirv interactive launch (`wrap::run_with`, the dashboard's first pane, and worker panes spawned from an interactive request) and read by `safety::launch_mode_pinned_interactive`, overriding an unproven self-report; scrubbed via `SUPERVISION_ENV` so a nested headless launch never inherits it. Also fixed the red CI: `artifact.rs`'s `run_interactive_with` read its stderr-drain tail before the reader thread had synchronized, losing a fast-exiting child's last stderr line under CI scheduling (`a_server_command_that_exits_immediately_is_a_failure`) — fixed with a bounded `wait_for_stderr_drain`, mirroring `supervise::FINAL_DRAIN_BUDGET`'s identical fix elsewhere.

**Review round 1 (2026-08-27) closed two escalations before merge.** CRITICAL: `escape_denied_by_screen` never checked for unquoted redirection, so a seeded read-only family's own pattern (e.g. `"echo *"`) matched straight through a trailing ` > ~/.claude/settings.json` — an unsandboxed write via the built-in escape seed. Now routed through the existing `contains_unquoted_redirection` primitive, applied identically to `permissions --escape` compile eligibility. IMPORTANT: the durable interactive-launch pin for a dashboard worker pane was gated on `SpawnRequest.interactive` — untrusted JSON any process able to reach the requests directory can forge — so a forged request could win the pin with nobody watching a dashboard. `fulfill_spawn_request` now takes an explicit `trusted_interactive: bool` from the CALLER, never derived from `req`: the dashboard's own in-process Spawn overlay passes `true`, the file-backed drop directory's consumer always passes a named `FILE_DROP_TRUSTED_INTERACTIVE = false`. A CI-only fix landed on top: the round-1 regression test did a real pty spawn without pinning `cfg.agent_bin` to a guaranteed-real binary (the rule every other real-pty-spawn test in the module follows), so it passed only on a machine with a real `claude` on `PATH` and failed on CI — fixed test-side, no production code touched. All five gates pass; failures are exactly the documented pre-existing baseline (name-for-name), none introduced.
**Next:** Merge once reviewed. See [[Command Safety]]'s new "The `escape_allow` sandbox-escape gate"/"The `--escape` flag" sections (including the review-round-1 redirection-screen addendum), [[Ctx Adapters]]'s "Attested `--settings` layer" bullet, [[Ctx Subsystem]]'s `permissions compile` entry, and the 2026-08-26/27 [[Decision Log]]/[[Work Journal]] entries.

### Codex resume-flag collision and dead-dashboard-timeout fix round — implemented, not yet pushed (`fix/143-144-agent-spawn`, 2026-08-26, v2.29.2, issues #143/#144)
**Status (2026-08-26):** Two commits plus a version bump, TDD per fix, closing the long-standing "codex never delivers on this machine" — the root cause was these two bugs, not codex auth. #143: `exec.rs`'s claude-only resume-flag stripping (`-c`/`--continue`/`--fork-session`, `--session-id`/`--resume`) used to run for every adapter, so codex's own `-c, --config <key>=<value>` had its bare `-c` stripped as claude's valueless `--continue` shorthand on every restart, orphaning the value token (e.g. `approval_policy=never`) — real codex-cli rejected the relaunch outright, exit 2. New `adapter_has_resume_flags(name)` (true only for `"claude"`) gates the strip; `extra_launch_flags`/`pins_an_existing_conversation` both take the adapter's name now, threaded through `exec.rs`, `wrap.rs`'s `restart_launch_flags`, and `chat.rs`'s `dash_orchestrator_pane`. #144: `agent::try_join_dashboard` checked only `dir.is_dir()`, so a crashed or force-quit dashboard's leftover requests directory burned the caller's entire ack timeout as "dashboard did not answer" even while some other dashboard ran fine elsewhere; it now calls `sessions::dashboard_owner_is_live` (widened to `pub(crate)`) right after and refuses a dead owner's directory immediately, and all three "dashboard did not answer" messages now name the request path (the timeout one also names the ack duration) instead of being identical strings. `docs/obsidian/{Modules/Ctx Subsystem,Modules/Ctx Supervisors,Development/Known Issues,Development/Decision Log,Development/Work Journal}.md` updated.
**Next:** push the branch and open the PR (neither done yet, per instruction). Residual recorded in [[Known Issues]], not fixed: a sandboxed caller that cannot write into a *live* dashboard's requests directory at all (e.g. a Claude Code session's own sandbox denying `~/Library/Application Support/zirv/ctx`) still degrades to headless with only a stderr line naming the write error — diagnosable now, not eliminated. See [[Ctx Subsystem]], [[Ctx Supervisors]], and the 2026-08-26 [[Work Journal]]/[[Decision Log]] entries.

### Three-issue safety/adapter fix round — implemented, not yet pushed (`fix/134-136-139-safety-round`, 2026-08-25, v2.29.1, issues #134/#136/#139)
**Status (2026-08-25):** Three commits plus a version bump, TDD per fix. #134: `zirv agent codex` (headless) broke outright on current codex-cli (0.149.x) because `codex exec` rejects `--ask-for-approval` — a new exec-scoped `--help` probe (`EXEC_ASK_FOR_APPROVAL_SUPPORT`) falls back to `-c approval_policy=<value>` on an unsupporting headless launch; interactive is byte-for-byte unchanged. `AgentAdapter::policy_args` gained a `LaunchMode` parameter. #136: `git commit -m "..."` messages that merely *documented* dangerous primitives by name were denied because the message's own prose reached the deny matcher verbatim — `redact_opaque_message`/`redact_single_quoted_heredocs` (both pure, both leaving `command_substitutions`'s own unredacted-text extraction untouched, so a live `$(...)` inside a message still escalates) close it. #139: a widened operator policy silently degraded a live session to ask-on-everything with a misleading hook explanation — `evaluate_with_attestation_evidence` now carries a `SnapshotDivergence`, `explain_text` names it, `run_explain` agrees with the hook (routed through the same evidence function), and `sessions::Record::safety_policy_sha256` + a new `status.rs` check surface "policy snapshot stale" in `zirv ctx status`. All Windows gates (build/nextest/serial-test/fmt/clippy) pass with only the established `wrap::win` baseline failure; Docker (`rust:1-bookworm`, non-root) verification of the adapter-argv change — build, `wrap::` tests (125/125), clippy — all exit 0.
**Next:** push the branch and open the PR (neither done yet, per instruction). Documented residual: dashboard worker panes (`dash/pane.rs::Pane::spawn`) don't yet thread a `SafetyPolicy` through to stamp `safety_policy_sha256` — their sessions never trigger the stale-snapshot line (degrades silently, not a false claim). See [[Command Safety]], [[Ctx Adapters]], [[Ctx Subsystem]], and the 2026-08-25 [[Work Journal]] entry.

### Dashboard spawn requests now accept linked git worktrees of the dashboard's own repo — implemented (`fix/119-worktree-pane-spawn`, 2026-08-25, issue #119)
**Status (2026-08-25):** Three commits (`a51ec18`, `d698fc3`, `523834a`). `dash::mod`'s repo gate (`fulfill_spawn_request`) used to refuse any `SpawnRequest.cwd` that wasn't the dashboard's own repo directory outright. A new `accepted_spawn_cwd`/`git_common_dir` pair now also accepts a `cwd` that is a linked `git worktree add` sibling of the dashboard's repo, decided by comparing each side's canonicalised `git rev-parse --git-common-dir` (env-scrubbed of `GIT_DIR`/`GIT_COMMON_DIR`/`GIT_WORK_TREE`/`GIT_INDEX_FILE` so an inherited override can't make two unrelated repos compare equal) — a genuinely unrelated repo still refuses. `Pane::spawn` now takes `cwd` and `repo` as separate parameters: the accepted pane's child process runs at `cwd` (the worktree), but its `sessions::Record` — and therefore `repo_slug`/mailbox routing/`zirv ctx nudge --to-session` — stays keyed to the dashboard's own `repo`, never the worktree.
**Next:** PR review pending. See [[Ctx Supervisors]] and the 2026-08-25 [[Decision Log]]/[[Work Journal]] entries.

### Zirv-managed global profile migration and optimizer audit — production-applied (`fix/121-123-setup-optimize`, 2026-08-25, v2.28.0)
**Status (2026-08-25):** Production migration exposed and filed issues #121–#130. The branch adds dry-runnable, backup-backed `zirv setup profile` routing/statusline/global-prompt/context-budget management; removes Zirv's own optional-path false positives including legacy managed wording; captures bounded sanitized model stderr; rejects findings grounded in ambient native memory rather than supplied optimizer surfaces; isolates subprocess tests from the operator profile; and makes the repository commit workflow refuse protected/detached branches before publishing the current branch to its same-named upstream. The verified 2.28.0 binary is installed globally and the dry-run-validated profile is applied: Claude/Fable orchestration, Codex `gpt-5.6-sol` workers, Claude Sonnet routine review with Opus reserved by the managed prompt for high-risk review, sandbox enabled, 8 KiB common-context budget, 5/5 Claude hooks, 4/4 Codex hooks, and tee-wrapped custom statusline. Deterministic and model-backed optimization now report only five informational duplicates in the legacy global `~/CLAUDE.md`; the model pass rejected three ungrounded findings. Shared-memory optimization found no applicable duplicate consolidation and reported one core-regeneration opportunity. All five required gates pass on the final tree: build, nextest 2,518/2,518, serial test 2,518/2,518, rustfmt, and clippy with warnings denied.
**Next:** Open and merge the closing PR; optionally deduplicate the five legacy global `~/CLAUDE.md` instructions by hand from the report.

### Injection-deferral capability, dashboard multi-line overlays, and a `gh` sandbox carve-out — implemented (`fix/118-injection-deferral-and-dash-newlines`, 2026-08-25, v2.27.0, issue #118)
**Status (2026-08-25):** Closes issue #118. `wrap`'s own T13 mail-advisory injection is now adapter-aware via new `Capabilities::defer_injection_submit` (codex: `true`, claude: `false`): a defer adapter gets the advisory text written alone, then its submitting `\r` drained by the pump loop after the shared `INJECTION_SUBMIT_DELAY` (50ms, now defined once in `mod.rs`, re-exported by `dash::pane`) — closing the codex composer paste-fold (#114/PR #116) for wrap's own advisory, not just the dashboard's visible injections. `inject_compact`/`Action::Compact` stays single-burst deliberately: unreachable for codex since `may_inject` requires a turn signal codex's adapter never posts. Separately, on the same branch: the dashboard now negotiates kitty keyboard enhancement (`DISAMBIGUATE_ESCAPE_CODES`, popped on teardown) so Shift+Enter is visible on the wire, and all four zirv-owned overlays (mail compose, spawn, memory, nudge) insert a newline on Shift+Enter/Alt+Enter instead of submitting, rendered via a new `draft_lines` helper; `nudge_overlay_reduce` was extracted for parity/testability with the other three reducers. A `gh` read-only sandbox-bypass carve-out (`is_sandbox_bypass_safe_gh_command`) exempts a single, simple, already-`Allow` read-only `gh` invocation from the mandatory unsandboxed-retry escalation. `read_until`'s wrap test helper is now bounded by a background-thread `ChunkReader` + `recv_timeout`, closing the previously-recorded blocking-read residual.
**Next:** PR review pending. See [[Ctx Adapters]], [[Ctx Supervisors]], [[Command Safety]], [[Known Issues]], and the 2026-08-25 [[Decision Log]]/[[Work Journal]] entries.

### Cross-harness low-noise permissions — implementation plus market hardening (`feat/cross-harness-permissions`, 2026-08-24, v2.28.0)
**Status (2026-08-24):** The approved twelve-task v2.26.0 implementation and v2.27.0 containment/structural pass are complete. The final v2.28.0 round gives `Bash`, native PowerShell and `cmd.exe` one effect classifier: single-ampersand nodes, recovery-aware generated cleanup, remote-versus-loopback HTTP mutation, credential-file access/upload, destructive version-control effects, infrastructure/service destruction, and irreversible registry/package/release operations are normalized across executable paths and `.exe`/`.cmd` wrappers. Everyday, read-only, staging-only, dry-run, download, loopback and novel commands stay silent. Every Claude launch now carries a SHA-256-addressed immutable policy snapshot; the hook keeps the stricter launch/current verdict, fails closed on tampering, and writes a privacy-preserving daily audit containing command hash rather than raw shell text. Landstrip, Arapuca and Microsoft MXC were evaluated and rejected as invisible mandatory dependencies pending a mature, adversarially verified three-OS containment contract. Final foreground verification: build, rustfmt, clippy, and cfg test-binary no-run pass; 2,409/2,414 serial tests pass and the five failures are exactly the established native-Windows `wrap` baseline; all 88 safety tests pass within that run. `cargo nextest run --no-fail-fast` is unavailable because `cargo-nextest` is not installed.
**Next:** Orchestrator review on the PR (Codex review explicitly skipped by operator request). Operator-executed follow-up remains the real Claude TTY hook-contract probe after quota resets plus Linux/Docker and the Docker AI-feature matrix; native Windows Claude's missing kernel sandbox remains explicitly recorded in [[Known Issues]] rather than being misrepresented as containment parity.
### Two-phase pane injection + report-back reminder sweep, deferred-submit review round — implemented (`fix/codex-pane-messaging`, 2026-08-24, v2.25.2, issues #114/#115, PR #116)
**Status (2026-08-24):** Six commits. #114: `inject_visible` used to write an injected line and its submitting `\r` in one `write_all`; codex's TUI paste-burst heuristic folded that `\r` into the pasted text as a newline, so a nudge/mail/report-back message sat typed but unsubmitted until a human pressed Enter. #115: a dashboard-spawned worker's report-back contract was silent and unverified — `compose_worker_prompt` now logs a `"report-back-omitted"` decision-log entry when mail is enabled but the requester is unaddressable, and a new `report_back_reminder_sweep` (same cadence as `mail_sweep`) injects one unconditional, once-only reminder naming the exact `zirv ctx send --to-session <id>` command into an idle worker pane. A same-day review round (F1-F7) redesigned the #114 mechanism: the first cut blocked the dashboard's single UI thread for `INJECTION_SUBMIT_DELAY` (50ms, still a hardcoded const, not a config key) between its two writes, freezing redraw/input for up to ~1.35s across nine panes/three sweeps and risking a double-typed line on a failed second write. It is now a deferred, tick-drained submit (`Pane::pending_submit`/`drain_pending_submits`/`write_submit_cr`, no sleeping); `wrap.rs`'s `Action::Compact` and T13 mail advisory share the identical shape; a restored worker pane's `report_to`/`report_reminder_sent` now survive a dashboard restart (roster persistence). `Cargo.toml` bumped to 2.25.2.
**Next:** neither fix has been verified against a real, running codex-cli TUI yet — pending the Docker matrix. See the 2026-08-24 [[Work Journal]] entry, [[Ctx Supervisors]], [[Known Issues]], and the 2026-08-24 [[Decision Log]] entries.

### Test-suite speedup via nextest + supervise final-drain race fix — implemented, PR #113 (`perf/test-suite-speed`, 2026-08-24, v2.25.1)
**Status (2026-08-24):** Primary local/CI test loop switched from serial `cargo test -- --test-threads=1` (~11–12 min) to `cargo nextest run --no-fail-fast` (~23–54s), one process per test; `.config/nextest.toml` added (including an `exec-nudge-restart` test-group serializing the writer-thread-driven nudge family against host contention), CI installs a pinned `cargo-nextest@0.9.143` via `taiki-e/install-action`, serial `cargo test` kept as a documented compatibility fallback (`.zirv/context/common.md`'s "all five" verify commands). One production fix: `OutputTap::drain_to_eof(budget)` (`supervise.rs`, `FINAL_DRAIN_BUDGET` = 500ms) replaces `try_lines()` at `exec.rs`'s/`run_loop.rs`'s post-`supervise_child` "final drain" — `try_lines` was itself non-blocking with no synchronization against the reader threads reaching EOF, so it could still lose the fast-exit race it existed to close. The rest is test hermeticity surfaced by per-process isolation: a real-`$HOME` leak in three test modules (`context_cli.rs`, `handoff.rs`, `handover.rs`), `review.rs`'s `dash_channel_active` gained an injectable `EnvLookup`, a 24-name `DASH_REQUESTS_ENV` ambient-failure baseline (when the suite runs under a zirv dash session) scrubbed at all four real-spawn sites in `wrap.rs`/`resume.rs` plus a new `wait_bounded` helper so a wedged pty child fails fast, and `fake-codex-agent.sh`'s `--help` capability probe no longer steals a fixture mode line (fixed same-day in `e589cb3`, the sole deterministic failure on CI run 32723969751). `Cargo.toml` bumped to 2.25.1.
**Next:** PR #113 open against `main`. Residual recorded in [[Known Issues]], not fixed: `wrap.rs`'s `read_until` test helper is a genuinely blocking read with no per-read timeout and can stall past its nominal budget under extreme host load. See the 2026-08-24 [[Work Journal]] entry and [[Ctx Supervisors]]/[[Technology Stack]].

### Guided first-run setup wizard — implemented, PR not yet opened (`feature/first-run-setup`, 2026-08-24, v2.25.0)
**Status (2026-08-24):** Three commits (`cd9dd48` feature, `272a173` fix round 1, `0751357` fix round 2). Bare `zirv` and a literal `zirv chat` (no further arguments) now auto-launch a guided setup wizard the first time both stdin and stdout are real terminals, `~/.zirv` has neither `ctx.toml` nor `.settings.toml`, and (for `chat`) the session would not be refused by the nesting guard; `--help`/`--allow-nested`/any other flag never arms it. Collect-then-apply design (`collect_first_run_answers`/`apply_first_run_answers`) so a Ctrl-C abort writes nothing and the wizard simply re-offers next time. Steps, all HOME-layer only: scaffolds `~/.zirv` (new `init::scaffold_global_zirv`/`scaffold_local_zirv`, extracted from `init_zirv_with` for reuse); per-harness enable/disable via new `settings::set_operator_agent_enabled`; default agent + optional chat model; memory enabled/harvest; optional local `.zirv` scaffold; optional hook install (`run_apply` when a local `.zirv` exists, else the two home-scoped install functions directly, so declining the local-`.zirv` step can't have one appear as a side effect of hooks). `zirv setup`'s guided menu gained a re-runnable "Run first-run wizard" entry. Bare-`zirv` help text corrected: chat needs a *local* `.zirv` plus both TTYs, not a global `~/.zirv` too. `Cargo.toml` bumped to 2.25.0. Two review rounds closed 7 Important findings (nesting-guard ordering, hook-install's unrequested `.zirv` side effect, a double-asked harvest question, an abort permanently suppressing the wizard, `--help` arming it, an index-panic risk, a harvest-decline silently not taking effect) plus several minors. All four gates clean; full suite 27 pre-existing failures only, byte-identical to unpatched `main` (this machine's documented `wrap::tests`/env-isolation baseline). See the 2026-08-24 [[Work Journal]] entry and [[Decision Log]].
**Next:** PR not yet opened as of this entry. Residuals in [[Known Issues]], not fixed: the hook-install fallback installs both harnesses' hooks regardless of the per-harness enable answers; a nesting-guard skip is silent rather than hinting at `zirv setup`; `zirv chat --allow-nested` never arms the wizard (accepted consequence of the `argv.len() == 2` gate, not a bug); three `commands::setup` tests assert `cfg.memory.*` without clearing `ZIRV_CTX_MEMORY_HARVEST`.

<!-- Cap at ~10 entries; drop the oldest when adding a new one. -->

<!-- Rotated out 2026-08-26: "Production-readiness pass + full zirv AI-setup migration -- implemented, not yet merged (fix/shipped-posture-allows-zirv, 2026-08-23, v2.24.0)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-26: "Close all 13 open issues (#83–#95) plus a malformed-`ctx.toml` fix -- implemented, not yet merged (feat/close-open-issues, 2026-08-23, v2.23.0)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-26: "Memory, context, and workflow subsystems -- merged (2026-08-21, v2.14.0–v2.17.0, PRs #59/#65/#66/#67)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-26: "Dashboard/harness parity fix round -- codex permission parity, pacing coverage, macOS usage polling, dashboard select mode (fix/dashboard-and-harness-parity, 2026-08-22/23, v2.22.0)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-25: "feat/dash-adaptive-poll-help-overlay -- adaptive input-poll wait, Ctrl+A ? help overlay, click-drag selection, Alt+Enter fold fix, mail-loss fix (2026-08-19, v2.13.0, PR #29)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-25: "feat/chat-token-economy -- worker model defaults, PreToolUse seat guard, token-economy prompts, capacity marker, live inter-session messaging (2026-08-18, v2.12.0, PR #28)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-25: "feat/review-model-config -- per-agent code-review model config (2026-08-18, v2.11.0)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-24: "feat/usage-two-window-display -- both usage windows shown, staleness-filtered (2026-08-17/18)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-24: "feat/usage-credits-throttle -- vendor usage monitoring, use_credits gating, pace-to-reset throttle (2026-08-16/17)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-24: "fix/process-lifecycle -- reap child process trees on every teardown path (stacked on feat/harness-roster-prompt, 2026-08-16)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-23: "Derived harness roster + zirv-owned review policy; dashboard sidebar ownership scoping (feat/harness-roster-prompt, 2026-08-16)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-23: "zirv meta-harness (chat entry, coordination, dashboard) — merged (PR #21, 2026-08-14)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-23: "Agent enable/disable settings and Obsidian vault setup — both merged (2026-08-12)" — oldest entry, cap held at 10. -->
