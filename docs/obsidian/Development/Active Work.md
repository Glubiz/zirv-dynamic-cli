---
last-verified: 2026-09-04
---

# Active Work

> Claude: update at session start and end for seamless handoffs.

Entries use the format:

```
### <title> — <state> (`branch-name`, YYYY-MM-DD)
**Status (date):** what's true right now.
**Next:** the next concrete step.
```

## In Progress

### Issue #313 — consecutive-denial breaker + identical-failing-command guard — implemented, not yet committed (`feature/issue-313-safety-loop-breakers`, 2026-09-04)
**Status (2026-09-04):** Additive-only change to `zirv ctx`'s command safety hook (`src/commands/ctx/safety.rs`); no existing verdict family changed for any command. `SafetyPolicy` gains three thresholds (`denial_breaker_threshold`/`identical_command_warn_after`/`identical_command_refuse_after`, defaults 3/2/5), folded across layers by a new `narrow_threshold` (a `0`-means-disabled variant of `config.rs`'s `narrow_max_nudges` pattern — not `REPO_FORBIDDEN`). The PreToolUse hook now: (1) prefixes its reason text with an explicit "stop retrying" note once a session's own trailing run of consecutive Ask/Deny verdicts (`log::read_recent_safety_decisions`, a new bounded reader) crosses the threshold; (2) parses the session transcript (`trailing_same_command_failure_run`, mirroring `adapters::claude::structural_context`'s tool_use/tool_result pairing) for a trailing run of the exact same failing Bash command and warns via `additionalContext` at the warn threshold, refusing outright (headless only) at the refuse threshold. `.zirv/ctx.toml` documents the three new sample-config rows.
**Next:** Run the five verification gates, then commit and open a PR against `release/3.16.0-harness-batch-4`. See [[Command Safety]]'s new "Loop breakers" section, [[Untrusted Configuration]]'s `[safety]` fold section, and the 2026-09-04 [[Decision Log]]/[[Work Journal]] entries.

### Issue #198 — two parts, one in flight (2026-08-30)
**Status (2026-08-30):** Issue #198 covers two related refreshes tracked together: **part 1**, refreshing the `Glubiz/zirv-generic-frontend` template repository itself (dependency bump, a real README, `.zirv/` script audit against the current schema) — not yet started, no branch/PR in either repo; **part 2**, this repo's `README.md` (`docs/198-readme-refresh`) — PR open, see the matching 2026-08-30 [[Work Journal]] entry for what it closed.
**Next:** Start part 1 in `zirv-generic-frontend` once part 2 merges, so the template's own README can link back at a README that already documents `zirv frontend capabilities` and the workflow lifecycle.

### TUI redesign — implemented, verified, and cross-reviewed; release PR open (`feat/202-tui-redesign`, 2026-08-30, v2.38.0, issue #202)
**Status (2026-08-30):** Five commits landing a centralized terminal design system (`src/style.rs`: semantic `Tone`/`paint`, display-width-aware truncation via new dep `unicode-width`, `format_age`/`format_pct`/`PLACEHOLDER`, `style::tui` ratatui tokens) and applying it across every terminal-facing surface: `chrome.rs`'s launch banner (rounded-box/compact/legacy degradation ladder, `banner()` gained a `cols: Option<u16>` parameter, `chrome::right_truncate` removed) and status bar (brand chip + glyph-tagged, priority-truncated segments); the dashboard (`dash/`) header (chip + harness/model + live count + sticky error line + right-aligned hints — rot score/mail/mem/sessions/usage all dropped from it) and sidebar (braille-spinner/●/✗/· state glyphs, rot score and the old `*`/`~` row markers dropped), one shared `render_list_dialog` primitive across mail/memory/restore/handover/help/quit-confirm, and a new `Ctrl+A e` errors overlay; styled `zirv help`, `ctx status` (liveness glyphs, tri-coloured liveness, `mail: ✉ N unread`, en-dash placeholders), and `context status` (coloured severity tags). Colour is gated by `console::colors_enabled()` throughout, so piped output stays plain. `Cargo.toml` bumped to 2.38.0. **Verification is now complete**: full nextest 3086/3087 (the single failure is the known `wrap::win::` baseline), serial `cargo test` the same, `fmt`/`clippy -D warnings` clean on both Windows and Linux (Docker `rust:1-bookworm`: 126/126 `wrap::` real-PTY tests plus clippy clean), and a native sonnet review round returned approve with zero findings.
**Next:** Codex cross-review completed with three confirmed findings, all fixed and re-verified in `d6cfce5` (ZWJ-safe truncation, header hint priority, dialog height saturation); a final full-PR sonnet review found no defects. Release PR open, awaiting merge. See [[Ctx Supervisors]]'s "Terminal chrome"/"The dashboard header and sidebar" sections, [[Ctx Subsystem]]'s `status` verb entry, [[Built-in Commands]]'s `help` entry, [[Technology Stack]]'s `unicode-width` entry, [[Known Issues]]' corrected sidebar-glyph entry, [[Context Management]]'s `HARNESS_PROMPT` v11 note (issues #204/#205, same day), and the 2026-08-30 [[Decision Log]]/[[Work Journal]] entries.

### Issue-tracker cleanup and #204/#205/#206 filed (2026-08-30)
**Status (2026-08-30):** Closed #187 and #190–#194 (all shipped in merged PR #200) and #180 (duplicate of #203). Filed #204 (`HARNESS_PROMPT` design-review gate) and #205 (autonomous lifecycle engagement), both implemented same day on `feat/202-tui-redesign` at `8b196f3` — see [[Context Management]]'s meta-harness section. Also filed #206 (a multiline-paste input bug), not yet triaged or started.
**Next:** Remaining open priorities, roughly in order: #179 (spawn-request auth — security), #203 (the over-budget mid-poll exit-code clobber flake, see [[Known Issues]]), #198 (frontend template refresh), #206 (multiline-paste input bug).

### AI-native SDLC flows — design approved, phased implementation active (`docs/187-ai-native-sdlc-spec`, 2026-08-28/29, issue #187, PR #188)
**Status (2026-08-29):** `docs/superpowers/specs/2026-08-28-ai-native-sdlc-design.md` extends the existing `zirv workflow` engine in place with a committed intent/spec/plan chain, adaptive ceremony, provider-neutral process skills and agent registry, deploy tiers, and an invoked maintain loop. Implementation is tracked in #190–#194 and stacked from this design branch.
**Next:** Land the phased implementation stack, preserving the spec's trust boundaries and deterministic gates. See the spec, [[Ctx Subsystem]], and the 2026-08-28 [[Decision Log]]/[[Work Journal]] entries.

### Machine-wide heavy-worker budget + permission-compile writer — implemented, not yet pushed (`feat/133-132-governance-and-compile`, 2026-08-25/26, v2.30.0, issues #133/#132)
**Status (2026-08-26):** Two features on one branch, stacked on `fix/134-136-139-safety-round`. **#133** (BSOD incident — four kernel bugchecks in 12 minutes under two concurrent unbounded build/test workloads): new `supervise.max_heavy_workers` (`config.rs`, default 1, `REPO_FORBIDDEN`, `ZIRV_CTX_SUPERVISE_MAX_HEAVY_WORKERS`), a machine-wide enumeration (`sessions::count_heavy_workers`/`count_heavy_workers_among`, counting live `Verb::Exec`/`Verb::Dash` only — never `Wrap`/`Chat`, operator-attended, and deliberately not `Loop`, out of scope), refuse-not-queue gates at `exec.rs` (immediately before `SessionGuard::register`, covering `zirv agent`'s headless fallback too) and `dash::fulfill_spawn_request` (a non-retryable `SpawnRefusal::policy`), and an unconditional `heavy workers: N of M slots in use` line in `zirv ctx status` that degrades silently on a config load error. A real regression surfaced and was fixed during this work: `status.rs` used to call `sessions::list` twice per request (once for the new count, once inside `sessions_lines`) — `list` sweeps a stale record's file from disk as a side effect of being called at all, so the second call found the first call's swept record already gone and silently dropped it from the `sessions:` listing (`status_marks_a_session_whose_process_is_gone_as_stale` caught it). Fixed by fetching `list` exactly once in `run_with` and threading the result into both `count_heavy_workers_among` and a refactored `sessions_lines` that now takes records as a parameter instead of calling `list` itself. **#132 residual:** new `zirv ctx permissions compile --agent <codex|claude> --sessions <N> [--dry-run]` — `audit` stays read-only; `compile` runs the identical pipeline and WRITES eligible (`!protected && reusable`, re-verified independently at write time, never trusting the cached `FamilyGroup.protected` field — new) families as `"<family> *"` patterns into `[safety] allow` in `~/.zirv/ctx.toml` only, via a new `setup.rs` union writer (`union_home_safety_allow`/`union_allow_patterns`/`read_home_safety_allow`) that unions rather than clobbers, backs the pre-write file up to `ctx.toml.bak`, and warns about comment loss. A regression test resolves the real `[safety]` policy from the written file and confirms the compiled pattern matches a *different* command in the same family, not just the audit's own sample. `Cargo.toml` bumped to 2.30.0.
**Next:** Run the remaining verification gates (fmt/clippy/serial-test) and open the PR against `fix/134-136-139-safety-round`. See [[Ctx Supervisors]]'s new "Machine-wide heavy-worker budget" section, [[Command Safety]]'s new "Compiling recommendations into approvals" section, [[Ctx Subsystem]], [[Untrusted Configuration]], and the 2026-08-25 [[Decision Log]] entries (refuse-not-queue; compile-writes-home-only).

### Codex/claude permission-request audit — implemented, PR pending (`feat/132-codex-permission-audit`, 2026-08-25, v2.29.0, issue #132)
**Status (2026-08-25):** New `zirv ctx permissions audit --agent <codex|claude> --sessions <N> [--json]` verb (`permissions.rs`) groups escalated (codex `sandbox_permissions: "require_escalated"` `exec` requests) and denied (claude headless `dontAsk` `PreToolUse` denials) requests by a normalized command family, reusing `safety.rs`'s wrapper-normalization helpers (widened to `pub(crate)`) so `env`/shell wrappers collapse to the same family as the bare command. Each family gets a reusability verdict (false on a long quoted literal payload, or a pipe into `jq`/`grep`/`awk`/`sed`) and a recommendation — a same-day review round (`3c2adf8`) added a separate `is_protected_family` gate so destructive git/publish-release/credential/global-install families are never recommended a standing allow regardless of reusability, plus `env -C`/`--chdir`/`-S`/`--split-string` handling in `unwrap_env_prefix` (the last bails out rather than misparse) and a decorated-pipe-target unwrap so `... | env NO_COLOR=1 jq .` is still recognised as a `jq` filter. `optimize.rs`'s new `permission_noise_findings` folds a noisy audit (>5 total requests) into the standard findings report as one `"permission-noise"` finding. Two regression fixtures (`tests/fixtures/codex-rollout-permission-requests.jsonl`, `tests/fixtures/claude-transcript-permission-requests.jsonl`, the claude one grounded in real transcript shapes inspected on the maintainer's machine) cover the command shapes from the issue's own production evidence. `Cargo.toml` bumped to 2.29.0. See [[Command Safety]]'s "Permission audit" section for the full model.
**Next:** Run the five verification gates and open the PR. The "compile operator-owned managed policy into reusable approvals" acceptance row is now implemented on `feat/133-132-governance-and-compile` (see the entry above) rather than left as a filed gap.

### Cross-harness command permission policy — design approved, spec review pending (2026-08-24, target v2.26.0)
**Status (2026-08-24):** `docs/superpowers/specs/2026-08-24-cross-harness-permissions-design.md` lays out a design (approach "C — keep both models, sharpen the seam") for making read-only work run silently, mutating/destructive commands prompt, currently-denied commands ask instead of dying, and every zirv-wrapped harness behave the same, all from one zirv-owned policy — without the operator configuring Claude or Codex directly. Keeps `safety.rs` (issue #83, harness-neutral command classifier) and `policy.rs` (issue #43, capability-stance model) as separate layers rather than merging them: `policy.rs` governs the whole tool surface coarsely per capability, `safety.rs` classifies shell command strings inside `ShellExec` more finely (gaining destructive-command `ask` built-ins and a SQL statement classifier), and new work concentrates in the projection seam that compiles both into each harness's native launch mechanism.
**Next:** spec review, then implementation against target v2.26.0. Not yet a branch/PR as of this entry.

### Frontend quality workflow — nine-commit draft PR green (`feat/frontend-quality`, 2026-08-21, v2.19.0, PR #78)
**Status (2026-08-21):** Issues #70–#77 plus the focused quality-contract v2 enhancement are implemented as nine dependency-ordered commits in draft PR #78 targeting `main`. Surface modes, product/design thesis and whole-journey reasoning, a 44-rule detector with complete inventory coverage, narrow/intermediate/wide evidence, a mandatory 13-dimension ≥4/5 AI-review gate, and durable task context all remain zero-touch. GitHub CI run 224 passed versioning, build, all 1,945 tests, rustfmt, Linux clippy, and Windows-target clippy.
**Next:** maintainer review; mark PR #78 ready and merge the top stack PR when desired.

## Recently Completed

### 3.15.0 harness batch 3 -- mail heading truncation, safety hardening, verify-on-stop nudge, permission-prompt audit, workdir visibility hint (`release/3.14.0-harness-batch-3`, 2026-09-03, v3.15.0, issues #326/#327/#309/#321/#307; #320 audit half; #316 closed by #325)

**Status (2026-09-03):** Five issues, rebuilt on top of main's own PR #325. #326: a `## ` heading inside a mail message body (a worker's own `## Summary`/`## Findings`) no longer re-closes `parse_markdown`'s header block and truncates the stored payload -- heading recognition now gates on a `header_seen` flag that fires exactly once. #327/#321 item 2: `git push --mirror`/`--prune` and `git checkout -f`/`--force`/`-B` now ask unconditionally; a new segment-wise combinator allows an unsandboxed retry mixing a confined write with a read-only escape segment. #309: a new `[verify_on_stop]` section (narrow-only, default on, cap 2 nudges/session) makes the Stop hook name the exact stale-gate command when this session modified code after the last passing verification run, suppressed for doc-only changes and an active Test/Verify workflow step; `zirv ctx status` gains a `gates: fresh`/`stale` line off the same check. #321/#307/#320: `zirv ctx permissions audit` gains `--session`/`--repo`/`--all-repos` scoping (default: the current repo's own claude project directory) plus a `## Sampled transcripts` section and a `## Permission-prompt log` section grouping Read/Edit-family prompts by directory from `logs/permission-prompts.jsonl`. #307: `zirv test`/`zirv verify --dry-run` are escape-safe on an unsandboxed retry; a Claude delegator gets an `/add-dir` hint when `--workdir` sits outside its own harness visibility.

**Next:** Open the PR against `main`. See [[Ctx Subsystem]], [[Command Safety]], [[Untrusted Configuration]], [[Built-in Commands]], [[Known Issues]], and the 2026-09-03 [[Decision Log]]/[[Work Journal]] entries.

### Issue #329 -- prompt-free posture: sibling-repo write grants, subprocess-env-scrub opt-in, glab parity, agent-default resolution (`fix/prompt-free-posture-329`, 2026-09-03, v3.14.0)
**Status (2026-09-03):** `adapters/claude.rs`'s launch settings now emit the launch repo's linked worktrees AND its sibling git checkouts under the same parent directory (plus each sibling's own linked worktrees, read from `.git/worktrees/*/gitdir` with no process spawn) into both `sandbox.filesystem.allowWrite` and `permissions.additionalDirectories` as literal paths -- never `--add-dir` (would load a sibling's own `.claude/` hooks), never the parent itself, nothing when the parent is a filesystem root or the home directory, capped at 32 siblings/16 worktrees per repo -- superseding the prior "other repositories remain out of scope" doc comment (operator ruling, see the 2026-09-03 [[Decision Log]] entry). `CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1` is no longer set by default (verified against Claude Code 2.1.259: it strips `SSH_AUTH_SOCK`/`GIT_SSH_COMMAND`/`GH_CONFIG_DIR`/etc. from every Bash subprocess and forces the permission mode to `default`) -- now the operator-only `[sandbox] scrub_subprocess_env` (`config.rs`, `REPO_FORBIDDEN`, `ZIRV_CTX_SANDBOX_SCRUB_SUBPROCESS_ENV`). `~/.ssh/known_hosts`/`~/.ssh/config` are re-opened via `sandbox.filesystem.allowRead` (private keys stay denied). `glab` joins `gh` as a prompt-free, sandbox-excluded native command family, and `is_irreversible_distribution_action` gains a matching `glab` arm. `is_read_only_escape_safe` now also accepts a reserved-zirv-escape-safe segment per segment (`is_reserved_zirv_escape_safe_segment`, factored out of `is_reserved_zirv_escape_safe`), so a compound mixing a reserved `zirv ctx` call with a benign read-only filter clears the unsandboxed retry. `zirv ctx permissions audit|compile|propose --agent` now defaults to the harness the command runs under (`ZIRV_CTX_AGENT`, `permissions::resolved_agent`), falling back to codex.
**Next:** Open the PR (#330). See [[Ctx Adapters]], [[Command Safety]], [[Ctx Subsystem]], [[Untrusted Configuration]], and the 2026-09-03 [[Decision Log]]/[[Known Issues]] entries.

### Release 3.12.0 -- writer-permit governance, context lint, cost ledger, speed telemetry, prefix-stability harness, narrowed VCS classifier (`release/3.12.0-harness-batch-2`, 2026-09-02/03, v3.12.0, issues #267/#275/#264/#293/#299/#306)

**Status (2026-09-03):** Six issues closing out a second harness-facing batch. #267: `zirv ctx agent --mode read-only|writing` (default writing) plus `--worktree` (an isolated `git worktree add` sibling) gate a new writer-permit pool (`<state>/permits/writers/`, `supervise.max_writers` default 1, `REPO_FORBIDDEN`), refusing with the first new exit code since #227 (`EXIT_WRITER_BUSY`, 80); auto-spawned review/test/verify workers are classified `WorkerMode::ReadOnly`; `zirv ctx status` gains `writers: N of M slots in use`; both orchestrator prompts gained one sentence naming a designated integrator for shared manifests/lockfiles. #275: `zirv context lint [--json] [--budget] [--fix-plan]` adds five pure findings over the same instructional layers a launch composes (CTX001 budget headroom, CTX002 duplicates, CTX003 contradiction candidates, CTX004 proportionality, CTX005 dedupe leak), discovered as a verify-gate `CheckKind::ContextLint` whenever `.zirv/context/` exists and folded into `zirv context sync --report`; `context.lint_max_pairs` (default 20,000) is `REPO_FORBIDDEN`. #264: `zirv ctx spend` aggregates `delegations.jsonl` by harness/model/task-class/worker, priced by a new pure, operator-only `price.rs` table (`~/.zirv/prices.toml`/`price.table_path` override, both `REPO_FORBIDDEN` alongside `price.stale_after_days`); cost lines land on `zirv ctx status`, `zirv workflow stats`, and the dashboard's new aggregate row (`workers N running · M failed · $x · five_hour P%`). #293: `NormalizedEvent` gains `at_ms` timestamps plus two inert sibling variants (`AssistantFirstText`/`ToolResultTimestamp`); `score::derive_speed_metrics` (turn p50/max latency, TTFT p50, tool-error rate) feeds a `workflow stats` speed line, entirely in `score.rs` -- verified (`timestamps_never_move_a_verdict`) never to move a rot verdict. #299: a prompt-prefix stability test harness (`tests/fixtures/fake-agent.sh`'s `FAKE_AGENT_PROMPT_LOG`, `compile::layers_of`/`emitted_layers`, `compile::test_support::assert_change_confined_to_layer`) asserts a state change perturbs only its own declared prompt layer -- a cache-cost invariant, not just prose. #306: the built-in VCS safety classifier narrows four `ask` rules (`checkout`/`restore`, `clean -f`, `worktree remove --force`, non-interactive `rebase`) to fire only outside agent-scoped local work (a concrete tracked path, a confined scratch worktree), removing their old blanket `Bash(git rebase *)`/`Bash(git clean *)` glob entries from `SHIPPED_POSTURE_ASK`.

**Next:** Open the PR. A same-release residual was closed in-branch: a dashboard pane spawn did not originally enforce the writer-permit pool the headless fork already did (`SpawnRequest.mode` was data-parity only) -- `fulfill_spawn_request` now calls the identical `acquire_writer` gate. One residual left open, not fixed here: `workflow::engine::auto_spawn_decision`'s `AutoSpawn.mode` correctly classifies Review/Test/Verify as read-only (pinned by `auto_spawn_decision_truth_table`) but isn't yet threaded into an actual permit acquisition, since `spawn_auto_worker` launches those as a direct subprocess rather than through `zirv ctx agent`. See [[Ctx Subsystem]], [[Ctx Supervisors]], [[Ctx Adapters]], [[Rot Engine]], [[Usage and Pacing]], [[Workflows]], [[Command Safety]], [[Context Management]], [[Testing Guide]], and the 2026-09-02/03 [[Decision Log]]/[[Work Journal]] entries.

### Release 3.11.0 -- harness batch, five issues (`release/3.11.0-harness-batch`, 2026-09-02, v3.11.0, issues #280/#281/#285/#287/#301)

**Status (2026-09-02):** Five issues closing out handoff continuity, resume trust, verification churn, work-group concurrency, and durable objectives. #280: handoff v3 grows `SECTIONS` 7 -> 11 (Constraints/Blocked/Key-decisions added, Files touched split into Files-read/Files-modified), `DISTILL_PROMPT_VERSION` v2 -> v3, `distill`/`distill_or_structural` take the repo's own previous handoff so a restart chain no longer drops an earlier decision. #281: `resume`/`SessionStart` append a host-verified working-set manifest beside the handoff; `sessions::Record.in_flight` lets a dead-pid record surface a mid-turn crash as one `<zirv_interrupted>` block on the next start. #287: `GateOutcome::Unchanged` stops `--run-checks` from re-running a suite against a byte-identical failing worktree, still burning a step attempt toward `MAX_STEP_ATTEMPTS`. #301: `WorkGroup.reserved_tokens` reserves a child's ceiling atomically at admission (`settle_reservation` replaces `add_spent_tokens`), closing a race where two concurrent admissions could both see the full remaining budget. #285: new `zirv ctx objective set|show|close` plus `--objective` on `exec`/`loop` persists a durable per-repo objective, injected as its own prompt layer (`DEFAULT_PROMPT_VERSION` v9 -> v10), never self-certified complete -- `objective close` refuses without fresh passing verification.

**Next:** Open the PR. Rejected this round: #303 (`codex exec resume <id> "/compact"` -- verified on codex-cli 0.147.0 to send the text as a plain user message and record no compaction, so it cannot stand in for a real compaction trigger). See [[Ctx Subsystem]], [[Ctx Supervisors]], [[Ctx Adapters]], [[Workflows]], [[Context Management]], [[Untrusted Configuration]], [[Built-in Commands]], and the matching 2026-09-02 [[Decision Log]]/[[Work Journal]] entries.

### Wrapper behaviour redesign, round 5 (harness iteration round 3) -- implemented, iteration loop stopped (`feat/wrapper-proportionality`, 2026-09-02, PR #261)
**Status (2026-09-02):** Round 1 (non-Rust half): `.zirv/context/{common,claude,codex}.md` retiered for proportionality (doc/code/PR verify tiers, tier-conditional codex review + vault-keeper, honesty clause moved to common), `CLAUDE.md`/`AGENTS.md` regenerated via `zirv context sync --generate`, and [[Ctx Subsystem]]/[[Ctx Adapters]] updated for `DEFAULT_PROMPT` v3/`HARNESS_PROMPT` v15/the 12-edit/25-turn adoption thresholds. Round 2 (same branch, same day): `DEFAULT_PROMPT` bumped v3 → v4 (read-before-write, debugging discipline, stuck-twice breaker, finish-the-task, no-flattery); `adapters/codex.rs` gained `WORKER_PROMPT`/`SUB_ORCHESTRATOR_PROMPT` for parity with claude; `.zirv/context/{claude,codex}.md` deduped further against the prompt constants; `rot::repetition` is now interleave-aware (an edit-like call resets the streak) and `hook.rs::stop_output` names over-verification explicitly when it drives the verdict; `review`/`reviewer` require a concrete failure scenario per finding and `frontend-craft`/`frontend-review`/`frontend-verify` scale by tier. `Cargo.toml` bumped to 3.9.0. **Round 3 / harness iteration round 1 (2026-09-02, workflow 263cb162, same PR #261):** three fresh audits (cost, speed, intelligence) shipped seven changes — `corrections_in` incremental via `CorrectionCheckpoint`; single-pass session start (`memory::load_both_scopes`, `compile.rs`'s context-file read-once); the workflow-step prompt layer moved after Context and before Memory (`DEFAULT_PROMPT_VERSION` v8 → v9); review package schema 2 → 4 (delta rounds send only new/changed findings plus an `accepted_spec_excerpt`); `zirv verify` reuses fresh `zirv test` evidence for an unchanged fingerprint, plus `workflow advance --run-checks` and `workflow review dispose --apply-recommended`; a same-error rot signal (`Signals.same_error_repeats`, `same_error_weight` default 0 so the score stays inert, but the Stop-hook advisory names it once `same_error_threshold` is crossed); a handoff `Verification` section (`DISTILL_PROMPT_VERSION` v1 → v2). Rejected this round: an idle-turn signal (orchestrator seats never edit, so it would fire on every legitimate delegation-wait turn) and removing `memory.max_injected_bytes` (would hard-fail an operator's existing config rather than degrade). See `docs/superpowers/specs/2026-09-01-wrapper-behaviour-redesign.md`'s "Round 3" section for the full audit findings. **Round 4 / harness iteration round 2 (2026-09-02, workflow af4ed8ec, same PR #261):** four of round 3's own deferred items — `zirv workflow close <id> [--reason]` gives a review-capped workflow an explicit terminal exit (`WorkflowStatus::Closed`, replacing the never-constructed `Cancelled`) and fixes a `resume` bug that recomputed status ahead of the terminal check, silently reviving `Closed`/`Failed` workflows; `VerificationOutcome.status` is now `Passed|Failed|Unknown`, closing round 3's residual findings F1 (a compound command like `cargo test || true` no longer reads as a pass) and F2 (`write_list` normalizes every handoff list item, not just the `Verification` line); `zirv ctx agent --attach-artifact <intent|spec|plan> [--workflow <id>]` appends a repo's accepted artifact to a delegated worker's task prompt, reordered/capped at 8 KiB and labeled untrusted, never in the system prompt; `durable_harvest_prompt` gains a deduped "Repeated tool errors" block from `StructuralContext.tool_errors` at session end. See `docs/superpowers/specs/2026-09-01-wrapper-behaviour-redesign.md`'s "Round 4" section. **Round 5 / harness iteration round 3 (2026-09-02, narrow, one independent review):** `reviewer_argv` now enforces the roster's resolved review model on the reviewer's own launch argv instead of only advising it (`resolve_review_model` widened `pub(crate)`); a settled `Fixed`/`Residual` review finding writes one durable memory entry under its own `review-finding-<id>` bank; a `status.rs` test hermeticity fix (missing `HomeGuard`) closed a machine-config-dependent CI flake root cause. The round's audit rejected size-aware review count, an active-pointer lock, artifact re-injection dedupe, and the codex idle heuristic, each for a concrete structural reason, and the iteration loop stops here — this is the wrapper-redesign work's final round on this branch/PR. See `docs/superpowers/specs/2026-09-01-wrapper-behaviour-redesign.md`'s "Round 5" section.
**Next:** All five rounds are landed on this branch; open/update the PR. Deferred backlog, unstarted, each blocked on a concrete condition rather than "someday": an active-pointer lock covering every read-modify-write workflow-state path at once (needs its own design, not a per-caller fix); a codex-side idle heuristic mirroring the same-error signal's intent (blocked on unverified codex rollout event shapes); artifact re-injection dedupe across workflow steps (worth doing only if repeated re-injection is actually measured, since compose legitimately runs fresh at session start and at every rot restart); size-aware review count within a tier (needs `Classification.reasons` to become structured data instead of prose). Also still open from earlier rounds: routing the reviewer's model by risk tier, harvesting memory-bank candidates from past workflow failures/findings, retiring machine-local memory entries that duplicate `claude.md`, a Work Journal 10-line-cap lint, folding `frontend-craft` overlap further. See `docs/superpowers/specs/2026-09-01-wrapper-behaviour-redesign.md`.

### Release 3.8.0 — bug batch, four issues across two parallel worktree tracks — implemented, PR pending (`release/3.8.0-bug-batch`, 2026-09-01, v3.8.0, issues #251/#252/#253/#255)
**Status (2026-09-01):** Two independent worktree tracks, five commits, merged onto the release branch. #252: `CodexAdapter::extra_writable_root_args` now always names the resolved git common dir as an explicit writable root (previously only when it resolved outside `cwd`, wrongly assuming a main checkout's own `.git` was already covered — codex's own sandbox keeps `.git` read-only inside every writable root it is handed, `cwd` included), and the same roots now also reach a headless `zirv agent` launch (`agent.rs::with_headless_extra_writable_roots`), not only dashboard-spawned panes. #253: `prompt::compose`'s workflow-step layer is now gated on `role == PromptRole::Orchestrator`, so a dispatched Worker/SubOrchestrator no longer silently inherits whatever workflow step happens to be active in the repo at launch time. #251/#255: the frontend Test gate always scans `verification::changed_paths_since_base` (merge-base diff, never a whole-repo fallback); Review/Verify still scan the whole repo but tag every finding `preexisting` against that set, and `zirv workflow advance --accept-preexisting-findings` records an operator waiver that applies for the rest of the workflow (introduced blocking findings always still fail). `infer_work_domain`'s task-text score is capped below the Frontend selection threshold (a real frontend path signal is now required too); a zero-file scan is `not_applicable` (a pass, printed as such) rather than fail-closed missing evidence; new `workflow start --profile`/`workflow reclassify <id> --profile` let an operator force or fix a workflow's methodology overlay without losing completed steps or accepted artifacts; `collect_all`'s full-surface scan now enumerates via `git ls-files` (gitignore-aware) on a git work tree. `Cargo.toml` bumped to 3.8.0.
**Next:** Open the PR (not yet opened as of this entry). Six commits total: a same-day follow-up (`a28013d`) closed the one residual found after the initial `#251`/`#255` fix — `latest_is_fresh_and_passing` now reuses a cached `not_applicable` (zero-file) report as fresh-and-passing instead of re-running the detector at every gate check. See [[Workflows]], [[Ctx Adapters]], [[Ctx Subsystem]], [[Built-in Commands]], and the matching 2026-09-01 [[Decision Log]]/[[Work Journal]] entries.

### Release 3.5.0 — nine-issue batch closed across two parallel tracks — implemented, PR pending (`feature/issue-batch-3-5-0`, 2026-09-01, v3.5.0, issues #222/#230/#235/#236/#241/#242/#243/#244/#245)
**Status (2026-09-01):** Both tracks from the 2026-09-01 spec (`.zirv/work/c5e68417-69e2-4d79-8b76-4a92bf3815fe/`) landed and merged. Track A (workflow lifecycle): #235 bounded reviewer workers (`review_worker_budget_tokens`/`review_worker_max_tool_calls`) plus per-step wall-clock in `workflow status`; #236 `--brainstorm`/`--no-brainstorm` split into `brainstorm`/`write-intent` skills with a headless-worker refusal; #242 `auto_spawn_on_gate` auto-spawns a bounded worker on a Review/Test/Verify gate transition; #244 `zirv ctx hook session-start` plus the published hook-surface gap analysis. Track B (ctx/agent): #230 item 3 capability-degradation warnings reach the delegator (stdout result lines, report-back mail, dashboard ack); #243 new pure `screen.rs` screening (mail, repo context layers, shared memory, transcript ingestion — flag-only, never blocks); #241 the active workflow's task/step feeds compile-time retrieval; #222 an interactive codex `untrusted` approval posture gets a one-time advisory; #245 closed citing the existing `fallback.rs` cross-harness routing (#186). `Cargo.toml` bumped to 3.5.0.
**Next:** Codex cross-review round (`zirv agent codex`) and the standing Linux/Docker round (this diff touches `wrap.rs`/`adapters/codex.rs` argv) still to run before merge, then open the PR. Residuals, not fixed: agent-spawned/auto-spawned worker telemetry's `parent_session_id` stays unpopulated (#244's telemetry-only disposition — a later phase, not invented here); `WorkerBudget`'s existing `HardStop` semantics mean reviewer ceilings stay strictly opt-in, no default numeric ceiling; `auto_spawn_on_gate` stays default-off. See [[Workflows]], [[Ctx Subsystem]], [[Ctx Adapters]], [[Ctx Supervisors]], [[Command Safety]], [[Untrusted Configuration]], [[Usage and Pacing]], [[Rot Engine]], and the matching 2026-09-01 [[Decision Log]]/[[Work Journal]] entries.

### Waiver-blind review packages fixed and `zirv ctx status --diff` shipped — implemented, PR pending (`feature/238-246-review-waiver-status-diff`, 2026-09-01, v3.4.0, issues #238/#246)
**Status (2026-09-01):** Two issues on one branch. #238: `verification::evaluate_against_operator_baseline(report, repo)` is now the one shared seam `latest_is_fresh_and_passing` (the gate) and `review::VerificationEvidence::from_report` (the review package) both call, so a baseline-waived run's review package now carries `passed_with_baseline_waiver`/`waived_failing_tests` (sorted, both skipped from JSON when false/empty) instead of reporting a raw, waiver-blind `passed:false` — the text-mode `verification:` line gets a ` (passed via operator baseline waiver; waived: ...)` suffix, and the reviewer prompt (`build_reviewer_prompt`, now unit-testable) tells the reviewer this is operator-acknowledged, not a regression. Motivated by every independent reviewer seat filing a false Critical "test verification failed" finding on this shape, three times on the #223/#225 gate and again on PR #247. #246: new `zirv ctx status --diff` prints only the report sections whose rendered text changed since this session's own previous `--diff` call, keyed by `ZIRV_CTX_SESSION` against a small per-session `StatusSnapshot` under a new `status-snapshots/` state-dir directory (mirrors `hook.rs`'s `adoption()` layout); first call and no-session-identity both fall back to the full report, a no-change call prints one line. `HARNESS_PROMPT` bumped to v13, its checkpoint bullet now names `zirv ctx status --brief --diff`. `Cargo.toml` bumped to 3.4.0.
**Next:** Run the five verification gates, then open the PR. No Linux/Docker round needed — this diff touches neither `wrap`/`announce`/`pace` nor adapter argv. A codex cross-review round (`zirv agent codex`) is still to run before merge. See [[Workflows]], [[Ctx Subsystem]], [[Context Management]], and the matching 2026-09-01 [[Decision Log]]/[[Work Journal]] entries.

### Workflow adoption (detect/nudge/enforce) and steady-state token reductions — merged as v3.2.0, continued into v3.3.0 (`worktree-issues-223-225` -> `feature/225-240-token-cost-ruflo`, 2026-08-31, issues #223/#225/#240)
**Status (2026-08-31):** Both issues implemented on one branch, v3.2.0. #223: a pure detector (`commands/workflow/adoption.rs`) plus operator-only `[workflow] adoption` policy (`Off < Advise < Nudge < Enforce`, default `Nudge`, `REPO_FORBIDDEN`) delivers per-policy through the existing Stop/Prompt hooks (`systemMessage` advisory, `additionalContext` nudge, or a `zirv agent` delegation refusal under `Enforce`), with new `AdoptionDetected`/`AdoptionRecovered` telemetry backing a `zirv workflow stats` adoption-rate line. #225: new `zirv ctx compile [--measure]` verb and `zirv ctx status --brief` give a reproducible per-layer byte/token baseline (`docs/benchmarks/token-cost.md` §6), the per-turn hook marker line shrank 170→89 bytes, and `zirv workflow stats`/`zirv ctx usage` gained schema-v2 cache-hit lines. Merged to `main` as v3.2.0 (PR #239, commit `53fc6e0`).
**Continued (2026-08-31, `feature/225-240-token-cost-ruflo`, v3.3.0, issues #225/#240):** Issue #225's measurement work continued: `compile.rs`'s canonical-context dedupe skip now leaves a one-line pointer naming the native file it deduped against instead of pure silence in the composed prompt; `.gitattributes` pins `CLAUDE.md -text`/`AGENTS.md -text` so the dedupe's byte-exact check survives an autocrlf-normalizing checkout instead of being silently suppressed; `docs/benchmarks/token-cost.md` §6 replaced its `bytes/4` estimates with real `tiktoken`-measured deltas (dedupe: -25.9% claude / -23.8% codex; per-turn hook trim: -38.1%; `status --brief`: ~-52.7%) plus a real recorded first-turn `cache_creation_input_tokens` corroboration (top-level median 25,742.5 tokens n=78, subagent median 12,219.5 tokens n=712). Issue #240 (new): `docs/design/2026-08-31-ruflo-evaluation.md` evaluated ruvnet/ruflo claim-by-claim against its own repo/docs, rejecting consensus/persona-inflation/the neural layer/the plugin marketplace/federation/the build-provenance witness, and adopting five spikes re-derived in zirv's own idiom as follow-up issues #241-#245. `Cargo.toml` bumped to 3.3.0.
**Next:** PR pending for `feature/225-240-token-cost-ruflo` (closes #225, #240). Run the five verification gates, a Linux/Docker round, and a codex cross-review round (`zirv agent codex`), then open the PR. Issue #246 (a `zirv ctx status --diff` idea filed this session) was implemented on `feature/238-246-review-waiver-status-diff` — see the entry directly above. See [[Ctx Subsystem]], [[Workflows]], [[Usage and Pacing]], [[Untrusted Configuration]], and the matching 2026-08-31 [[Decision Log]]/[[Work Journal]] entries.

### Bug batch — codex capacity/account exhaustion, `zirv agent --workdir`/`--headless`, lenient review ingestion, verify-check env passthrough — implemented, PR pending (`fix/bug-batch-227-228-229-232-233`, 2026-08-31, v3.1.0, issues #227/#228/#229/#232/#233)
**Status (2026-08-31):** One release PR fixing five GitHub issues plus a `main`-failing test. #227 splits codex trouble text into three classes distinct from a usage-window trip — transient `CAPACITY_PATTERNS` restart the same-repo session with capped backoff (`EXIT_CAPACITY_EXHAUSTED`/78 on exhaustion), `ACCOUNT_EXHAUSTED_PATTERNS` is an immediate hard stop (`EXIT_ACCOUNT_EXHAUSTED`/79, no restart spent), and a headless delegation's non-zero exit now mails its structured reason back to the spawning session unconditionally, not only from a dashboard pane on success. #228 adds `--workdir <dir>`/`--headless` to `zirv agent`: `--workdir` is canonicalised and hard-refused unless inside a git repository, feeds the headless launch's sandbox/repo derivation, and (as `SpawnRequest.workdir`, re-validated server-side) overrides a pane spawn's cwd and writable-root policy; any pane-spawn flag beyond `--model`/`--workdir` is now a named hard error pointing at `--headless` instead of a silent demotion. #229/#232 make `zirv workflow review run`'s `ZIRV_REVIEW_RESULT` ingestion lenient per finding (unknown fields ignored, severity/disposition synonyms accepted, an unrecognized value degrades rather than rejects), salvage the reviewer's raw output to a named path on any ingestion failure, exclude `.zirv/work/**` from the package/staleness fingerprint, and reorder package truncation code-first. #233 gives a verification check's child process an explicit, operator-only `check_env_passthrough` allowlist (`SSH_AUTH_SOCK` and friends) instead of an unscrubbed full environment inherit, plus a macOS `launchctl` login-session fallback. A same-batch fix: this repository's own `.zirv/context/common.md` had drifted 25 bytes over its shipped 4096-byte budget, failing a pinned test — trimmed and regenerated.
**Next:** Open the PR (not yet opened as of this entry). Run the standing Linux/Docker round and a codex cross-review round (`zirv agent codex`) before merge, per the machine's own convention for anything touching `wrap`/`exec`/adapter argv. Two residuals recorded, not fixed: `zirv ctx loop` still only recognises the original `LIMIT_HIT_PATTERNS` class, not the two new codex classes (#227); a forged dashboard spawn request can point a fresh worker pane's writes at any repo *inside* the dashboard's workdir roots (#228) — narrowed same-day by a `dash.workdir_roots` confinement fix from "any git repo the dashboard user can reach," but still open, and still needing #179's own PR (unix-socket `SO_PEERCRED` plus pid-ancestry attribution) to close fully.
**Review round (2026-08-31, same branch):** Three follow-up commits. `CAPACITY_PATTERNS` dropped `"503 Service Unavailable"` after review (a non-provider process could print it) — final six-needle list in [[Usage and Pacing]]. `zirv workflow review run`'s own reviewer spawn (`reviewer_argv`) now passes an explicit `--headless`, since this branch's own #228 loud pane-flag refusal would otherwise block the workflow's own reviewer from inside a dashboard — a gotcha for any zirv-internal `zirv agent` caller passing trailing harness flags. The reviewer prompt contract now states the `ZIRV_REVIEW_RESULT` line must be plain stdout text, never a harness findings tool — added after a claude sonnet reviewer used Claude Code's `ReportFindings` tool instead and the #232 raw-output salvage path recovered its findings via `review add`. `check_env_passthrough` semantics were clarified (a presence guarantee, not an isolation boundary) after a codex review finding claiming a trust-boundary violation was dismissed on those grounds. See [[Workflows]], [[Ctx Supervisors]], [[Command Safety]], [[Untrusted Configuration]], [[Known Issues]], and the matching 2026-08-31 [[Decision Log]] entries.

### PR #234 — safe built-in auto-allow and addressed mail from any cwd — merge conflicts resolved (`fix/224-226-bug-batch`, 2026-08-31, v3.0.1, issues #224/#226)
**Status (2026-08-31):** Merged v3.0.0/main into the PR branch and resolved the overlap between main's issue #219 worktree-mail fix and #226. Directed session/role mail now follows its delivery-envelope path from any cwd (including `--peek`, unread counts, and worktrees), while undirected, claim-once, and fan-out mail remain mailbox-scoped; the broader registered-mailbox double scan from #219 was removed to avoid duplicate delivery and cross-mailbox broadcast visibility. Issue #224's reviewed partition remains intact: trusted structured built-ins are auto-allowed, repo scripts/payloads and dangerous posture-pinning forms are not. The release version is 3.0.1, above the v3.0.0 base.
**Verification / next:** All five local gates pass on the resolved tree (`cargo build`; nextest 3259/3259; serial cargo test 3259/3259; fmt; clippy with warnings denied). Commit and push the merge resolution, then confirm PR #234's GitHub checks are green. See [[Command Safety]], [[Ctx Adapters]], [[Ctx Subsystem]], [[Untrusted Configuration]], and the 2026-08-31 [[Decision Log]]/[[Work Journal]] entries.

### Bug batch, now widened to a breaking release — headless argv budget, worktree mail read-side, frontend target root, bracketed-paste ownership, `.zirv/commands/` relayout — implemented, PR pending (`fix/issue-batch-220-219-206-214`, 2026-08-31, v3.0.0, issues #220/#219/#206/#214/#212)
**Status (2026-08-31):** Scope grew mid-batch: the operator folded #212 (a breaking script-directory relayout) into the same PR, so this ships as **v3.0.0** (breaking) instead of v2.40.0. #220/#219/#214/#206 are unchanged from the prior status (see the matching [[Decision Log]]/[[Work Journal]]/[[Known Issues]] entries). **#212** (`feat(scripts)!: move zirv scripts to .zirv/commands/`): scripts now resolve ONLY from `<repo>/.zirv/commands/` then `~/.zirv/commands/` (same local-shadows-global precedence) — a hard cutover, no root fallback, no auto-migrate. `zirv init` creates `.zirv/commands/` (local and global); `zirv create`/`--global` writes new scripts there; `.shortcuts.yaml` and every config file (`ctx.toml`, `.settings.toml`, `verify.toml`) stay at the `.zirv` root; `zirv help` lists scripts from `commands/` and shortcuts from the root. The not-found error now names stray root-level script files (up to 10, config never listed) with a move instruction. This repo's own 11 `.zirv/*.yaml` scripts were `git mv`'d into `.zirv/commands/`. **#179 was pulled back out** (its implementer produced nothing in 90 minutes) and stays open, excluded from this PR — needs its own PR, still requiring unix-socket `SO_PEERCRED` plus pid-ancestry attribution for dash spawn requests; the filesystem intake boundary documented in `dash/mod.rs` still stands unchanged.
**Next:** Open the PR (not yet opened as of this entry). Residuals carried over from before the scope change: no real human paste in Windows Terminal was exercised for #206 in the agent environment that built this fix — one manual paste on the built binary is recommended before merge. New residual from #212: an installed pre-3.0 `zirv` binary can no longer find this repo's scripts (it still looks in the `.zirv` root) — install the 3.0 binary before relying on `zirv <script>` here. #212's own README/Architecture/Concepts/Modules vault updates were done directly by its implementer, not here. #179 (spawn-request auth, security) remains open and excluded, own PR. See [[Ctx Subsystem]]'s "Chat, Agent and Mail" section, [[Ctx Adapters]], [[Ctx Supervisors]]'s "wrap" section, [[Built-in Commands]]'s "AI-native workflow lifecycle" section, [[Known Issues]], and the 2026-08-31 [[Decision Log]]/[[Work Journal]] entries.

### Bug batch — baseline-waivable test gate, codex argv overflow, budget-kill exit-code fix, flaky sessions test — implemented, PR pending (`worktree-fix-bug-batch-213-215-218-203`, 2026-08-30, issues #215/#213/#203/#218)
**Status (2026-08-30):** Four fixes on one worktree/branch, one PR to come. #215 (workflow test/deploy gate baseline): `zirv workflow advance`'s test/verify step, which previously hard-failed on any test failure in the evidence, now passes when a failing report's failing test names are all a subset of a new operator-owned `~/.zirv/test-baseline/<repo_slug>.json` baseline, recorded only by the new `zirv test baseline [--repo <path>] [--check <id>]... [--dry-run] [--json]` command (runs `VerificationMode::All`, persists evidence like any run, records the union of failing `Unit`-check test names); waived names print loudly, an empty/missing baseline reproduces the old strict behavior. #213 (codex argv overflow): `injection_args_for_session` now shrinks an oversized inline-argv-delivered codex composed prompt to a 24KB budget (`INLINE_ARGV_PROMPT_BUDGET_BYTES`) before spawn — stripping memory, then canonical context, then the workflow-step layer, with a tail-truncation backstop — instead of the launch hard-failing with Windows' `os error 206`; claude's file-based delivery is unaffected. #203 (budget-kill exit-code clobber): the mid-poll over-budget `HardStop` check in `exec.rs`'s `supervise_run` now gives one tick of grace before killing a child, so a naturally-exiting child's real exit code survives instead of being overwritten by `EXIT_BUDGET_EXHAUSTED` — a budget verdict now only ever replaces a clean (0) exit. #218 (test-only flake): `sessions.rs`'s `adopt_child_pid` test now tolerates ≤2s clock-boundary drift between two `process_start_secs` readings.
**Next:** Open the PR (not yet opened as of this entry). Issue #209's stale workflow `31794d36-4ba3-4dbc-8fad-1d5890fa127e`, still parked at its test gate in the main checkout since the dash TUI v3 session, is unblockable once this branch's #215 baseline mechanism ships and merges. Deferred issues still open: #206 (multiline-paste input bug), #214, #212, #179 (spawn-request auth, security). See [[Workflows]]'s "Baseline-waivable test gate" section, [[Utilities]]'s "Inline-argv delivery is budgeted" section, [[Ctx Adapters]], [[Ctx Supervisors]]'s "A mid-poll budget kill got one tick of grace" note, [[Known Issues]], and the 2026-08-30 [[Decision Log]]/[[Work Journal]] entries.

### Install experience unified and hardened — implemented, PR pending (`worktree-feat-210-install-experience`, 2026-08-30, v2.39.1, issue #210)
**Status (2026-08-30):** `cd.yaml`'s Linux release asset now cross-compiles to a static `x86_64-unknown-linux-musl` binary (same asset name) instead of the `ubuntu-24.04` glibc build, which linked GLIBC_2.39 and failed to run on Debian 12/Ubuntu 22.04 — verified in Docker before and after the switch. `install.sh` gained an arch guard (non-x86_64 Linux errors with a `cargo install --git` pointer), a `ZIRV_INSTALL_DIR` override, `mkdir -p` for a missing install dir, and a download-failure message naming the failed URL — verified in Docker (default install, pinned version, override, arm64 guard via qemu). README's Installation/Upgrading sections are now canonical per-OS instructions, with a `brew trust glubiz/tap` note (new Homebrew refuses untrusted third-party taps, verified live) and `cargo install --git` replacing the misleading `cargo build` fallback. `Cargo.toml`'s `repository` field fixed (`Glubiz/zirv` → `Glubiz/zirv-dynamic-cli`) and bumped to 2.39.1 (rebased over v2.39.0/#216).
**Next:** Open the PR (not yet opened as of this entry). Known residuals, not fixed here: already-published releases (<= 2.39.0) remain glibc-2.39-linked, so pinning an old version via `install.sh` still fails on older distros; the separate `zirv-generic-frontend` repo's `cli.zirv.io` landing page still advertises a 404 install-script URL and an unpublished `cargo install zirv` (`src/content/channels.ts`), fixed by a separate PR on that repo. See the 2026-08-30 [[Decision Log]]/[[Work Journal]] entries and [[Technology Stack]]'s Package metadata table.
### Dash TUI v3 — fidelity fixes, rot column, footer signal row — implemented, cross-reviewed, PR pending (`feat/209-dash-v3`, 2026-08-30, v2.39.0, issue #209)
**Status (2026-08-30):** Full lifecycle for the v3 dashboard redesign: operator-approved design artifact (`.zirv/work/31794d36-4ba3-4dbc-8fad-1d5890fa127e/dash-v3-mock.html`) reviewed before implementation, landed as `tui::accent()` gaining bold weight, a new read-only `workflow::active_workflow_summary()` accessor, and the main dash change — dim-cyan dialog borders with `title · n` counts, the sidebar's box replaced by a flat divider plus full-width rules mirroring the header, the sidebar's rot column restored (dropped by issue #202), a new focused-pane footer signal row reusing `wrap`'s own status-bar grammar (verdict, usage, mail, workflow position, supervision), and a selected-row REVERSED-over-coloured-glyph bug fix (sidebar and dialog cursor rows both). A codex cross-review round (`zirv agent codex`) returned 5 findings — 4 confirmed and fixed (dead-pane footer fallback unreachable in the live reap loop; footer mail count read the dashboard's own launch identity instead of the focused pane's; width-degradation tiers dropped the workflow segment before the harness label; the supervision segment hardcoded `true` instead of reading `Pane::reachable()`) and 1 refuted (workflow resolution staying keyed to the dashboard's own repo, not per-pane, is correct — see the matching [[Decision Log]] entries). `Cargo.toml` bumped to 2.39.0.
**Next:** Open the PR (not yet opened as of this entry). Two residuals, not fixed here: issue #215 (filed mid-session) — `zirv workflow advance`'s test-step gate demands `zirv test changed` evidence and hard-fails on this host's ~6 documented pre-existing baseline test failures, making the gate structurally unpassable here even with zero new failures; needs a baseline/waiver mechanism, out of scope for a dash-only branch. And the footer's `supervised`/`unsupervised` segment only distinguishes a spawn-time turn-signal bind failure — there is no dash-observable analogue yet of `wrap`'s own in-process `chrome::BarState::degraded` (a supervising loop going bad mid-session), documented as future work in `FooterAliveFacts`'s own doc comment rather than filed as a separate issue. See [[Ctx Supervisors]]'s "The dashboard header, sidebar and footer", [[Workflows]]'s `active_workflow_summary` note, [[Known Issues]]' new #215 entry, and the 2026-08-30 [[Decision Log]]/[[Work Journal]] entries.

### Reporting, reliable agent mail, and permission-feedback loop — three issues complete, release closing out (`release/2.35.0`, 2026-08-28, v2.35.0, issues #176/#177/#178)
**Status (2026-08-28):** All three issues on draft PR #185 are implemented and review-verified. #176 (`zirv report bug|feature`) shipped first, plus a follow-up trim of `.zirv/context/common.md` to keep the shipped context budget green. #177 (reliable mail: UUID delivery envelopes/receipts, explicit `--claim-once`, role addressing, `--reply-to`/threads, TTL/dead letters, sender status, wake markers, cross-harness envelope rendering, `zirv ctx status` queue/unread/flow fields) went through a review round (`b831dc1`) that fixed one critical (envelope fields weren't `header_value`-sanitized, letting a forged `SESSION_ENV`/`AGENT_ENV` inject a fake bullet line into the trusted envelope block) and two important findings (two unidentified claim-once readers could collapse onto the literal `"unknown-reader"` claimant and both win the claim; a crash between `claim_once` and the physical consume-rename left a `Read` receipt whose file never moved, invisible to `expire_deliveries` forever). #178 (`zirv ctx permissions propose`, disabled by default, operator-only enable) went through two review rounds: round 1 landed the base capture/classify/proposal loop; round 2 (`9570d41`) added a persisted per-family reported-evidence watermark (no-op / delta-marked repeat comments instead of re-filing) and `Excluded`/`OutOfScope` classification with printed non-escalating guidance, plus a same-round `family_depth` fix (`glab` was missing, collapsing every glab command to one family); `affb399` closed a residual where `glab api` had no `is_protected_family` arm and could compile into a standing `[safety] allow`.
**Next:** Record exact five-gate verification results in the PR body, inspect CI, and mark PR #185 ready — the plan's own closeout checklist (`docs/superpowers/plans/2026-08-28-release-2.35.0-issues-176-178.md`) has one unchecked item left. See [[Built-in Commands]] (`report`), [[Command Safety]] (`propose`, `escape_allow`), [[Ctx Subsystem]] ("Reliable delivery"), [[Untrusted Configuration]] (`[github].token`, `[permissions].propose_enabled`), and the 2026-08-28 [[Decision Log]]/[[Work Journal]] entries.

### Token-cost reduction Phases 4-6 (work groups, sub-orchestrators, capacity-aware rotation, heavy-operation permits, spawn trust) — merged, PR #171, v2.32.0 (`release/2.32.0`, 2026-08-27, issue #155)
**Status (2026-08-27):** The remaining phases of issue #155's token-cost reduction plan, stacked as five feature branches merged into `release/2.32.0` (Phase 5(b) `WorkGroup`/`zirv ctx group create|status|close`; Phase 5.1 `PromptRole::SubOrchestrator`; Phase 5.5 heavy-operation permits replacing the session-count `max_heavy_workers` gate; Phase 5(c)/5(e) work-group status tree in `zirv ctx status`; Phase 5.3/5.4 spawn-request lineage/role/depth cap plus `--budget-tokens`/`--max-tool-calls` worker budgets; Phase 6(a) `AgentAdapter::context_window_tokens`; Phase 6(b) `rot::token_gates` scaling the token-gate floor/ceiling to the model's real context window; Phase 6(c) `pace::spawn_gate` gating new delegated spawns, never an existing session's own restart, on quota pressure), then an independent sonnet + codex cross-review round (13 findings, fixed across 14 commits, `7b37034..23a445d`) before merge: unverified spawn-request lineage can no longer claim a coordinator (`SubOrchestrator`) role (Worker spawns unaffected); a file-dropped `SpawnRequest.force` can no longer lift the hard `pace.spawn_hard_pct` quota refusal, only the soft warn band; heavy-operation permit acquisition is now atomic (numbered slot files + `create_new`, post-claim verification, a 5s grace-window sweep for crash-orphaned slots) and tracks the spawned heavy child's own pid so a slot survives the parent dying first; a `CtxConfig` load error now falls back to the default permit policy instead of disabling permits outright; the 600s wait-timeout escape hatch now warns loudly instead of silently proceeding ungoverned; `supervise.heavy_command_patterns` is lift-and-union (add-only) like `sandbox.extra_deny`; a worker's token/tool-call budget is now also checked against the final transcript on a clean child exit; `--max-tool-calls` is refused up front for the codex adapter (no verified tool-call events); rot's token gate now resolves the session's real live model (`AgentAdapter::model_hint`) so a `[1m]` claude seat gets its actual 1M window instead of the 200k default (checkpoint schema bumped 1→2); work-group `child_limit` is now enforced at both admission choke points with rollback on every post-admit failure. All five verification gates clean on the merged tree.
**Next:** Follow-ups filed and still open: #169 (worker budgets reset across a restart — a rot restart or nudge relaunch starts the new child's spend from zero rather than carrying the exhausted total forward), #170, #160. The token-cost benchmark rows (`docs/benchmarks/token-cost.md`) for phases 4-6 are still unmeasured — only phases 1-3 have recorded before/after numbers. See [[Ctx Subsystem]]'s new `group` verb section, [[Ctx Supervisors]]'s "Heavy-operation permits"/"Delegated-worker budgets"/coordinator-lineage sections, [[Ctx Adapters]]'s `model_hint`/`context_window_tokens`/`counts_tool_calls`, [[Rot Engine]]'s "Capacity-aware gates" section, [[Usage and Pacing]]'s `pace::spawn_gate` section, [[Untrusted Configuration]]'s `heavy_command_patterns` note, and the 2026-08-27 [[Decision Log]]/[[Work Journal]] entries.

### Repo-portable memories: shared `--repo` remember/forget, credential guard, provenance-labeled recall — implemented, PR into `release/2.32.0` pending (`feat/172-repo-portable-memories`, 2026-08-27, v2.32.0, issue #172)
**Status (2026-08-27):** `zirv ctx remember --repo`/`zirv ctx forget --repo` write to and remove from the repo-committed `.zirv/memory` shared bank (`MemoryScope::Shared`), gated on `memory.enabled && memory.shared_enabled` for the store path; `zirv ctx recall` now always merges the local and repo banks, labeling each entry's provenance (`[local]`/`[repo — repository-owned content, not operator-verified]`, plus a JSON `scope` field). A credential guard (`sensitive_shared_match`: a deny-list plus `review::detect_token_shape`'s reused token-shape regex, run over key/body/tags/paths, not body-only) refuses a credential-shaped shared write outright on both `zirv ctx remember --repo` and `zirv memory remember --shared`, overridable per-surface via `--allow-sensitive`; the guard runs on the entry's original, untruncated body before `max_entry_bytes` truncation on both the direct (`upsert_shared`) and automatic-harvest (`filter_durable_candidates`) ingestion paths — a review-round fix (`3baeab3`) closed a boundary-shear gap where the harvest path used to truncate first, letting a long credential get sheared below `detect_token_shape`'s length gates. Shared writes now truncate to `max_entry_bytes` the same way private writes already did. `MemoryScope::of`/a shared `ScopedEntry` centralize scope-of-bool mapping that used to be duplicated. `Cargo.toml` stays at 2.32.0 — this PR targets the `release/2.32.0` train, not `main`, and CD only tags releases cut from `main`.
**Next:** Open the PR into `release/2.32.0` (not yet opened as of this entry). See [[Ctx Subsystem]], [[Untrusted Configuration]], [[Built-in Commands]], and the 2026-08-27 [[Decision Log]]/[[Work Journal]] entries.

<!-- Cap at ~10 entries; drop the oldest when adding a new one. -->

<!-- Rotated out 2026-09-03: "Token-cost reduction Phase 1+2 -- quick wins plus raw four-category telemetry with delegation lineage -- implemented, PR #157 draft (feat/token-cost-p2-telemetry on feat/token-cost-p1-quick-wins, 2026-08-27, v2.34.0, issue #155)" -- oldest entry, cap held (loosely -- this list already runs well past ~10) after adding the issue #329 (fix/prompt-free-posture-329) entry. -->

<!-- Rotated out 2026-09-02: "Token-cost reduction Phase 3 -- canonical-content hash dedupe -- implemented, not yet pushed (feat/token-cost-p3-context-dedupe, 2026-08-27, v2.35.0, issue #155)" -- oldest entry, cap held at ~10 after adding the release/3.11.0-harness-batch (#280/#281/#285/#287/#301) entry. -->

<!-- Rotated out 2026-09-01: "Token-cost reduction Phase 1 — quick wins: loud truncation, tighter common.md, merged memory tail -- implemented, not yet pushed (feat/token-cost-p1-quick-wins, 2026-08-26, v2.33.0, issue #155)" — oldest entry, cap held at ~10 after adding the 3.8.0 bug-batch (#251/#252/#253/#255) entry. -->

<!-- Rotated out 2026-09-01: "Dashboard discovery fallback + EPERM-blind pid liveness fix -- implemented, PR pending (fix/145-146-dash-discovery-pane-send, 2026-08-26, v2.30.1, issues #145/#146)" — oldest entry, cap held at ~10 after adding the waiver-blind-review/status-diff (#238/#246) entry. -->

<!-- Rotated out 2026-08-31: "Sandbox-escape allowlist + durable interactive-mode pinning -- implemented, PR #154 (fix/147-sandbox-escape-allowlist, 2026-08-26, v2.31.0, issue #147)" — oldest entry, cap held at ~10 after adding the workflow-adoption/token-cost-p225/Ruflo (#223/#225/#240) entry. -->

<!-- Rotated out 2026-08-31: "Codex resume-flag collision and dead-dashboard-timeout fix round -- implemented, not yet pushed (fix/143-144-agent-spawn, 2026-08-26, v2.29.2, issues #143/#144)" — oldest entry, cap held at ~10 after adding the bug-batch (#220/#219/#214/#206) entry. -->

<!-- Rotated out 2026-08-30: "Three-issue safety/adapter fix round -- implemented, not yet pushed (fix/134-136-139-safety-round, 2026-08-25, v2.29.1, issues #134/#136/#139)" — oldest entry, cap held at ~10 after adding the bug-batch (#215/#213/#203/#218) entry. -->

<!-- Rotated out 2026-08-30: "Dashboard spawn requests now accept linked git worktrees of the dashboard's own repo -- implemented (fix/119-worktree-pane-spawn, 2026-08-25, issue #119)" — oldest entry, cap held at ~10 after adding the dash TUI v3 (#209) and install-experience (#210) entries. -->

<!-- Rotated out 2026-08-28: "Zirv-managed global profile migration and optimizer audit -- production-applied (fix/121-123-setup-optimize, 2026-08-25, v2.28.0)" — oldest entry, cap held at ~10 after adding the release/2.35.0 (#176/#177/#178) entry. -->

<!-- Rotated out 2026-08-27: "Two-phase pane injection + report-back reminder sweep, deferred-submit review round -- implemented (fix/codex-pane-messaging, 2026-08-24, v2.25.2, issues #114/#115, PR #116)" — oldest entry, cap held at ~10 after adding the token-cost Phases 4-6 (#171) and repo-portable-memories (#172) entries. -->

<!-- Rotated out 2026-08-27: "Cross-harness low-noise permissions -- implementation plus market hardening (feat/cross-harness-permissions, 2026-08-24, v2.28.0)" — cap held at ~10 after adding the token-cost Phases 4-6 (#171) and repo-portable-memories (#172) entries. -->

<!-- Rotated out 2026-08-27: "Injection-deferral capability, dashboard multi-line overlays, and a `gh` sandbox carve-out -- implemented (fix/118-injection-deferral-and-dash-newlines, 2026-08-25, v2.27.0, issue #118)" — cap held at ~10 after adding the token-cost Phases 4-6 (#171) and repo-portable-memories (#172) entries. -->

<!-- Rotated out 2026-08-27: "Test-suite speedup via nextest + supervise final-drain race fix -- implemented, PR #113 (perf/test-suite-speed, 2026-08-24, v2.25.1)" — oldest entry, cap held at ~10. -->

<!-- Rotated out 2026-08-26: "Guided first-run setup wizard -- implemented, PR not yet opened (feature/first-run-setup, 2026-08-24, v2.25.0)" — oldest entry, cap held at ~10. -->

<!-- Rotated out 2026-08-26: "Production-readiness pass + full zirv AI-setup migration -- implemented, not yet merged (fix/shipped-posture-allows-zirv, 2026-08-23, v2.24.0)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-26: "Close all 13 open issues (#83–#95) plus a malformed-`ctx.toml` fix -- implemented, not yet merged (feat/close-open-issues, 2026-08-23, v2.23.0)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-26: "Memory, context, and workflow subsystems -- merged (2026-08-21, v2.14.0–v2.17.0, PRs #59/#65/#66/#67)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-26: "Dashboard/harness parity fix round -- codex permission parity, pacing coverage, macOS usage polling, dashboard select mode (fix/dashboard-and-harness-parity, 2026-08-22/23, v2.22.0)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-25: "feat/dash-adaptive-poll-help-overlay -- adaptive input-poll wait, Ctrl+A ? help overlay, click-drag selection, Alt+Enter fold fix, mail-loss fix (2026-08-19, v2.13.0, PR #29)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-25: "feat/chat-token-economy -- worker model defaults, PreToolUse seat guard, token-economy prompts, capacity marker, live inter-session messaging (2026-08-18, v2.12.0, PR #28)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-25: "feat/review-model-config -- per-agent code-review model config (2026-08-18, v2.11.0)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-24: "feat/usage-two-window-display -- both usage windows shown, staleness-filtered (2026-08-17/18)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-24: "feat/usage-credits-throttle -- vendor usage monitoring, use_credits gating, pace-to-reset throttle (2026-08-16/17)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-24: "fix/process-lifecycle -- reap child process trees on every teardown path (stacked on feat/harness-roster-prompt, 2026-08-16)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-23: "Derived harness roster + zirv-owned review policy; dashboard sidebar ownership scoping (feat/harness-roster-prompt, 2026-08-16)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-23: "zirv meta-harness (chat entry, coordination, dashboard) — merged (PR #21, 2026-08-14)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-23: "Agent enable/disable settings and Obsidian vault setup — both merged (2026-08-12)" — oldest entry, cap held at 10. -->
