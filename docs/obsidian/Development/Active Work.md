---
last-verified: 2026-08-25
---

# Active Work

> Claude: update at session start and end for seamless handoffs.

Entries use the format:

```
### <title> — <state> (`branch-name`, YYYY-MM-DD)
**Status (date):** what's true right now.
**Next:** the next concrete step.
```

## In Progress

### Cross-harness command permission policy — design approved, spec review pending (2026-08-24, target v2.26.0)
**Status (2026-08-24):** `docs/superpowers/specs/2026-08-24-cross-harness-permissions-design.md` lays out a design (approach "C — keep both models, sharpen the seam") for making read-only work run silently, mutating/destructive commands prompt, currently-denied commands ask instead of dying, and every zirv-wrapped harness behave the same, all from one zirv-owned policy — without the operator configuring Claude or Codex directly. Keeps `safety.rs` (issue #83, harness-neutral command classifier) and `policy.rs` (issue #43, capability-stance model) as separate layers rather than merging them: `policy.rs` governs the whole tool surface coarsely per capability, `safety.rs` classifies shell command strings inside `ShellExec` more finely (gaining destructive-command `ask` built-ins and a SQL statement classifier), and new work concentrates in the projection seam that compiles both into each harness's native launch mechanism.
**Next:** spec review, then implementation against target v2.26.0. Not yet a branch/PR as of this entry.

### Frontend quality workflow — nine-commit draft PR green (`feat/frontend-quality`, 2026-08-21, v2.19.0, PR #78)
**Status (2026-08-21):** Issues #70–#77 plus the focused quality-contract v2 enhancement are implemented as nine dependency-ordered commits in draft PR #78 targeting `main`. Surface modes, product/design thesis and whole-journey reasoning, a 44-rule detector with complete inventory coverage, narrow/intermediate/wide evidence, a mandatory 13-dimension ≥4/5 AI-review gate, and durable task context all remain zero-touch. GitHub CI run 224 passed versioning, build, all 1,945 tests, rustfmt, Linux clippy, and Windows-target clippy.
**Next:** maintainer review; mark PR #78 ready and merge the top stack PR when desired.

## Recently Completed

### Zirv-managed global profile migration and optimizer audit — production-applied (`fix/121-123-setup-optimize`, 2026-08-25, v2.28.0)
**Status (2026-08-25):** Production migration exposed and filed issues #121–#130. The branch adds dry-runnable, backup-backed `zirv setup profile` routing/statusline/global-prompt/context-budget management; removes Zirv's own optional-path false positives including legacy managed wording; captures bounded sanitized model stderr; rejects findings grounded in ambient native memory rather than supplied optimizer surfaces; isolates subprocess tests from the operator profile; and makes the repository commit workflow refuse protected/detached branches before publishing the current branch to its same-named upstream. The verified 2.28.0 binary is installed globally and the dry-run-validated profile is applied: Claude/Fable orchestration, Codex `gpt-5.6-sol` workers, Claude Sonnet routine review with Opus reserved by the managed prompt for high-risk review, sandbox enabled, 8 KiB common-context budget, 5/5 Claude hooks, 4/4 Codex hooks, and tee-wrapped custom statusline. Deterministic and model-backed optimization now report only five informational duplicates in the legacy global `~/CLAUDE.md`; the model pass rejected three ungrounded findings. Shared-memory optimization found no applicable duplicate consolidation and reported one core-regeneration opportunity. All five required gates pass on the final tree: build, nextest 2,518/2,518, serial test 2,518/2,518, rustfmt, and clippy with warnings denied.
**Next:** Open and merge the closing PR; optionally deduplicate the five legacy global `~/CLAUDE.md` instructions by hand from the report.

### Injection-deferral capability, dashboard multi-line overlays, and a `gh` sandbox carve-out — implemented (`fix/118-injection-deferral-and-dash-newlines`, 2026-08-25, v2.27.0, issue #118)
**Status (2026-08-25):** Closes issue #118. `wrap`'s own T13 mail-advisory injection is now adapter-aware via new `Capabilities::defer_injection_submit` (codex: `true`, claude: `false`): a defer adapter gets the advisory text written alone, then its submitting `\r` drained by the pump loop after the shared `INJECTION_SUBMIT_DELAY` (50ms, now defined once in `mod.rs`, re-exported by `dash::pane`) — closing the codex composer paste-fold (#114/PR #116) for wrap's own advisory, not just the dashboard's visible injections. `inject_compact`/`Action::Compact` stays single-burst deliberately: unreachable for codex since `may_inject` requires a turn signal codex's adapter never posts. Separately, on the same branch: the dashboard now negotiates kitty keyboard enhancement (`DISAMBIGUATE_ESCAPE_CODES`, popped on teardown) so Shift+Enter is visible on the wire, and all four zirv-owned overlays (mail compose, spawn, memory, nudge) insert a newline on Shift+Enter/Alt+Enter instead of submitting, rendered via a new `draft_lines` helper; `nudge_overlay_reduce` was extracted for parity/testability with the other three reducers. A `gh` read-only sandbox-bypass carve-out (`is_sandbox_bypass_safe_gh_command`) exempts a single, simple, already-`Allow` read-only `gh` invocation from the mandatory unsandboxed-retry escalation. `read_until`'s wrap test helper is now bounded by a background-thread `ChunkReader` + `recv_timeout`, closing the previously-recorded blocking-read residual.
**Next:** PR review pending. See [[Ctx Adapters]], [[Ctx Supervisors]], [[Command Safety]], [[Known Issues]], and the 2026-08-25 [[Decision Log]]/[[Work Journal]] entries.

### Cross-harness low-noise permissions — implementation plus market hardening (`feat/cross-harness-permissions`, 2026-08-24, v2.28.0)
**Status (2026-08-24):** The approved twelve-task v2.26.0 implementation and v2.27.0 containment/structural pass are complete. The final v2.28.0 round gives `Bash`, native PowerShell and `cmd.exe` one effect classifier: single-ampersand nodes, recovery-aware generated cleanup, remote-versus-loopback HTTP mutation, credential-file access/upload, destructive version-control effects, infrastructure/service destruction, and irreversible registry/package/release operations are normalized across executable paths and `.exe`/`.cmd` wrappers. Everyday, read-only, staging-only, dry-run, download, loopback and novel commands stay silent. Every Claude launch now carries a SHA-256-addressed immutable policy snapshot; the hook keeps the stricter launch/current verdict, fails closed on tampering, and writes a privacy-preserving daily audit containing command hash rather than raw shell text. Landstrip, Arapuca and Microsoft MXC were evaluated and rejected as invisible mandatory dependencies pending a mature, adversarially verified three-OS containment contract. Final foreground verification: build, rustfmt, clippy, and cfg test-binary no-run pass; 2,409/2,414 serial tests pass and the five failures are exactly the established native-Windows `wrap` baseline; all 88 safety tests pass within that run. `cargo nextest run --no-fail-fast` is unavailable because `cargo-nextest` is not installed.
**Next:** Orchestrator review on the PR (Codex review explicitly skipped by operator request). Operator-executed follow-up remains the real Claude TTY hook-contract probe after quota resets plus Linux/Docker and the Docker AI-feature matrix; native Windows Claude's missing kernel sandbox remains explicitly recorded in [[Known Issues]] rather than being misrepresented as containment parity.
### Two-phase pane injection + report-back reminder sweep, deferred-submit review round — implemented (`fix/codex-pane-messaging`, 2026-08-24, v2.25.2, issues #114/#115, PR #116)
**Status (2026-08-24):** Six commits. #114: `inject_visible` used to write an injected line and its submitting `\r` in one `write_all`; codex's TUI paste-burst heuristic folded that `\r` into the pasted text as a newline, so a nudge/mail/report-back message sat typed but unsubmitted until a human pressed Enter. #115: a dashboard-spawned worker's report-back contract was silent and unverified — `compose_worker_prompt` now logs a `"report-back-omitted"` decision-log entry when mail is enabled but the requester is unaddressable, and a new `report_back_reminder_sweep` (same cadence as `mail_sweep`) injects one unconditional, once-only reminder naming the exact `zirv ctx send --to-session <id>` command into an idle worker pane. A same-day review round (F1-F7) redesigned the #114 mechanism: the first cut blocked the dashboard's single UI thread for `INJECTION_SUBMIT_DELAY` (50ms, still a hardcoded const, not a config key) between its two writes, freezing redraw/input for up to ~1.35s across nine panes/three sweeps and risking a double-typed line on a failed second write. It is now a deferred, tick-drained submit (`Pane::pending_submit`/`drain_pending_submits`/`write_submit_cr`, no sleeping); `wrap.rs`'s `Action::Compact` and T13 mail advisory share the identical shape; a restored worker pane's `report_to`/`report_reminder_sent` now survive a dashboard restart (roster persistence). `Cargo.toml` bumped to 2.25.2.
**Next:** neither fix has been verified against a real, running codex-cli TUI yet — pending the Docker matrix. See the 2026-08-24 [[Work Journal]] entry, [[Ctx Supervisors]], [[Known Issues]], and the 2026-08-24 [[Decision Log]] entries.

### Test-suite speedup via nextest + supervise final-drain race fix — implemented, PR #113 (`perf/test-suite-speed`, 2026-08-24, v2.25.1)
**Status (2026-08-24):** Primary local/CI test loop switched from serial `cargo test -- --test-threads=1` (~11–12 min) to `cargo nextest run --no-fail-fast` (~23–54s), one process per test; `.config/nextest.toml` added (including an `exec-nudge-restart` test-group serializing the writer-thread-driven nudge family against host contention), CI installs a pinned `cargo-nextest@0.9.143` via `taiki-e/install-action`, serial `cargo test` kept as a documented compatibility fallback (`.zirv/context/common.md`'s "all five" verify commands). One production fix: `OutputTap::drain_to_eof(budget)` (`supervise.rs`, `FINAL_DRAIN_BUDGET` = 500ms) replaces `try_lines()` at `exec.rs`'s/`run_loop.rs`'s post-`supervise_child` "final drain" — `try_lines` was itself non-blocking with no synchronization against the reader threads reaching EOF, so it could still lose the fast-exit race it existed to close. The rest is test hermeticity surfaced by per-process isolation: a real-`$HOME` leak in three test modules (`context_cli.rs`, `handoff.rs`, `handover.rs`), `review.rs`'s `dash_channel_active` gained an injectable `EnvLookup`, a 24-name `DASH_REQUESTS_ENV` ambient-failure baseline (when the suite runs under a zirv dash session) scrubbed at all four real-spawn sites in `wrap.rs`/`resume.rs` plus a new `wait_bounded` helper so a wedged pty child fails fast, and `fake-codex-agent.sh`'s `--help` capability probe no longer steals a fixture mode line (fixed same-day in `e589cb3`, the sole deterministic failure on CI run 32723969751). `Cargo.toml` bumped to 2.25.1.
**Next:** PR #113 open against `main`. Residual recorded in [[Known Issues]], not fixed: `wrap.rs`'s `read_until` test helper is a genuinely blocking read with no per-read timeout and can stall past its nominal budget under extreme host load. See the 2026-08-24 [[Work Journal]] entry and [[Ctx Supervisors]]/[[Technology Stack]].

### Guided first-run setup wizard — implemented, PR not yet opened (`feature/first-run-setup`, 2026-08-24, v2.25.0)
**Status (2026-08-24):** Three commits (`cd9dd48` feature, `272a173` fix round 1, `0751357` fix round 2). Bare `zirv` and a literal `zirv chat` (no further arguments) now auto-launch a guided setup wizard the first time both stdin and stdout are real terminals, `~/.zirv` has neither `ctx.toml` nor `.settings.toml`, and (for `chat`) the session would not be refused by the nesting guard; `--help`/`--allow-nested`/any other flag never arms it. Collect-then-apply design (`collect_first_run_answers`/`apply_first_run_answers`) so a Ctrl-C abort writes nothing and the wizard simply re-offers next time. Steps, all HOME-layer only: scaffolds `~/.zirv` (new `init::scaffold_global_zirv`/`scaffold_local_zirv`, extracted from `init_zirv_with` for reuse); per-harness enable/disable via new `settings::set_operator_agent_enabled`; default agent + optional chat model; memory enabled/harvest; optional local `.zirv` scaffold; optional hook install (`run_apply` when a local `.zirv` exists, else the two home-scoped install functions directly, so declining the local-`.zirv` step can't have one appear as a side effect of hooks). `zirv setup`'s guided menu gained a re-runnable "Run first-run wizard" entry. Bare-`zirv` help text corrected: chat needs a *local* `.zirv` plus both TTYs, not a global `~/.zirv` too. `Cargo.toml` bumped to 2.25.0. Two review rounds closed 7 Important findings (nesting-guard ordering, hook-install's unrequested `.zirv` side effect, a double-asked harvest question, an abort permanently suppressing the wizard, `--help` arming it, an index-panic risk, a harvest-decline silently not taking effect) plus several minors. All four gates clean; full suite 27 pre-existing failures only, byte-identical to unpatched `main` (this machine's documented `wrap::tests`/env-isolation baseline). See the 2026-08-24 [[Work Journal]] entry and [[Decision Log]].
**Next:** PR not yet opened as of this entry. Residuals in [[Known Issues]], not fixed: the hook-install fallback installs both harnesses' hooks regardless of the per-harness enable answers; a nesting-guard skip is silent rather than hinting at `zirv setup`; `zirv chat --allow-nested` never arms the wizard (accepted consequence of the `argv.len() == 2` gate, not a bug); three `commands::setup` tests assert `cfg.memory.*` without clearing `ZIRV_CTX_MEMORY_HARVEST`.

### Production-readiness pass + full zirv AI-setup migration — implemented, not yet merged (`fix/shipped-posture-allows-zirv`, 2026-08-23, v2.24.0)
**Status (2026-08-23):** Nine issues filed and fixed on this branch, each with tests, fmt/clippy clean: #98 (shipped `dontAsk` posture denied zirv's own prompt-mandated commands — `Bash(zirv *)` plus `cargo fmt`/`clippy` added, invariant test `prompt_mandated_zirv_commands_are_allowed_by_the_shipped_posture`); #99 (orphaned turn-signal endpoints accumulated as "(no record)" rows in `ctx status` — liveness-probed sweep in `sessions::list`, verified live: 37 stale rows gone); #100 (mail to dead sessions inflated unread/pending counts — undeliverable sweep on the status paths, verified live: 5 swept); #101 (`\\?\` verbatim prefix in setup output — shared `display_path` helper); #102 (safety hook `"ask"` under `dontAsk` was a denial overriding the operator's own `permissions.allow` — `permission_mode`-aware fall-through); #103 (`memory init` vault extraction produced heading-long keys and mid-word 768-byte bodies — sentence-boundary bodies at the configured cap, readable keys, fenced blocks stripped); #104 (posture too narrow to work in — rewritten "usable, no prompts, destructive families denied" posture: whole toolchain families, harness dirs, WebFetch/WebSearch, new destructive-family denies; version bumped to 2.24.0); #105 (drift analysis flagged a zirv-managed CLAUDE.md as a duplicate of its own canonical source — managed files excluded from duplicate/precedence analysis); #106 (`glob_match` didn't match a trailing ` *` pattern against its bare prefix, a claude-semantics divergence that let a bare `git push --force` slip the classifier — bare-prefix rule added). On top of the fixes, this session executed the full zirv AI-setup migration on this machine (dry-run before every wet run): `zirv setup apply` (5/5 claude hooks, 4/4 codex hooks, statusline already tee-wrapped, backups under `.zirv/backups/ai-reset`); canonical `.zirv/context/{common,claude,codex}.md` authored (4093/2990/2436 B); `.zirv/memory/` shared bank now 29 hand-authored + 8 vault-extracted entries (5 low-quality ones forgotten, net 32); 10 private entries migrated from the Claude Code auto-memory bank; `zirv context sync --generate --force` replaced the hand-maintained 21 KB `CLAUDE.md` with a 7.3 KB managed render and created `AGENTS.md` — the Obsidian vault stays as human documentation, unaffected. Native sonnet review clean; codex cross-review confirmed one README doc gap (fixed). Full suite on the final tree pending in the orchestrator (expected 7 pre-existing `wrap.rs` failures only, see [[Known Issues]]).
**Next:** PR not yet created as of this entry — the orchestrator creates it after the full suite finishes. Next session should install the 2.24.0 binary (elevated shell, see the chocolatey-install gotcha) and relaunch `zirv chat` so the new posture actually applies; a `zirv ctx optimize` run is also pending. Residual: codex pane workers hit `codex-windows-sandbox-setup.exe missing` for shell commands (pre-recorded residual, see [[Known Issues]]); the Windows `Drop for SignalServer` never stopping the acceptor/drainer threads is documented in [[Known Issues]] from the #99 work.

### Close all 13 open issues (#83–#95) plus a malformed-`ctx.toml` fix — implemented, not yet merged (`feat/close-open-issues`, 2026-08-23, v2.23.0)
**Status (2026-08-23):** Two commits on top of `origin/main` (`2264b55`, `499e65a`). `2264b55` closes issues #85–#95: codex injection-fallback surfaced in status/announcements (#85); codex rollout-derived turn/usage events, `capabilities().events = true` (#86); one-time `zirv setup` memory-harvest offer (#87); risk re-classification fails safe (Measured/Unavailable, one-band escalation) outside git (#88); one-time codex distiller/reviewer sandbox-residual announcement, `--ignore-rules`/`--ignore-user-config` added when the installed CLI documents them (#89); content-based secret detection behind the filename gate (#90); verification-report retention on write, reusing telemetry's key (#91); `launches_through_cmd_shim` trait default derives from `resolve_program` (#92); `zirv setup` wraps an existing custom statusLine (#93); `zirv ctx send --all` fan-out via per-session read markers (#94); backup-run retention on write (#95); plus a malformed repo `ctx.toml` now skips that layer (`config::UnparsableLayer`) instead of aborting every command. `499e65a` adds #83 (`[safety]` deny/ask/allow command-safety policy, `zirv ctx safety check|list|explain`, wired claude `PreToolUse` hook) and #84 (`zirv ctx handover --model|--agent [--dry-run] [--force]`, swaps the orchestrator seat's model/harness in place under the same registry short id, `Ctrl+A o` dashboard picker). Native 2255/2262 (7 pre-existing Windows-only); Linux `commands::ctx` 1968/1968. `Cargo.toml` bumped to 2.23.0. See the 2026-08-23 [[Work Journal]] entry and the 2026-08-23 [[Decision Log]] entries.
**Next:** not yet pushed/PR'd as of this entry; a code-review fix round may still be adjusting `src/` and the Modules pages concurrently. All 13 issues (#83–#95) are implemented on this branch and will close when the PR merges. Residuals recorded in [[Known Issues]]: codex rollout tool-call/tool-result/compaction events remain unmapped (#86 residual half of issue #11); the Windows-codex injection fallback is surfaced, not closed (#85); verification retention shares telemetry's retention key rather than a dedicated one (#91 scope residual); the distiller/reviewer sandbox-residual announcement can only gate on `--quiet`/`ZIRV_CTX_QUIET`, not the full layered `chrome.events` (#89).

### Memory, context, and workflow subsystems — merged (2026-08-21, v2.14.0–v2.17.0, PRs #59/#65/#66/#67)
**Status (2026-08-21):** All three parallel rework tracks are merged into main, each after a consolidated opus review plus two fix rounds, with every finding fixed or recorded as a residual. Memory (issues #31–35, PR #66 collapsing #57/#58/#62/#64/#66, v2.14.0): private state-dir + shared repo-committed `.zirv/memory/` scopes, a key-addressed store, the `zirv memory` command family, a core memory prompt layer injected at all six launch seams, and a deterministic retrieval engine (recall only; session-start injection deferred to #44). Context (issues #39–43, PR #65 collapsing #60/#61/#63/#65 for v2.15.0, PR #67 for v2.16.0): a generic context-surface model with trust metadata, AGENTS.md/native-settings ingest, the canonical `.zirv/context/` layer (analysis-only until #44), deterministic drift detection, and the canonical `EffectivePolicy` model with per-harness capability mapping (dormant until #44/#46). Workflow (issues #47/#49/#50, PR #59, v2.17.0): the provider-neutral workflow subsystem under `src/commands/workflow/` — skills registry, engine, deterministic classification, verification evidence, review packages/findings, artifacts, telemetry. See the 2026-08-21 [[Work Journal]] entry and the five 2026-08-21 [[Decision Log]] entries.
**Next:** **update (2026-08-22, verified via `gh issue list`): the follow-on issues this entry used to list as open are all closed** — memory #36–38, context #44–46, workflow #48/#51–56, and #68 (`get_scoped`) all shipped 2026-08-21/22. See the frontend-quality and AI-setup entries below and [[Known Issues]] for residuals recorded across those review rounds.

<!-- Cap at ~10 entries; drop the oldest when adding a new one. -->

### Dashboard/harness parity fix round — codex permission parity, pacing coverage, macOS usage polling, dashboard select mode (`fix/dashboard-and-harness-parity`, 2026-08-22/23, v2.22.0)
**Status (2026-08-23):** Seven fixes landed in one branch, uncommitted diff reviewed against source: (1) macOS status header usage via a bounded, announced Keychain fallback in `poll.rs` plus `poll::usage_source_hint` for `zirv ctx status`; (2) `Ctrl+A v` dashboard select-mode toggle releasing zirv's own mouse reporting for a pane (e.g. Claude Code) its click-drag selection can never reach; (3) `wrap`'s live mail advisory gained signal-less (codex) parity, and `zirv ctx send`/`HARNESS_PROMPT` v8 now say plainly that an undirected send is claimed by exactly one session; (4) `HARNESS_PROMPT` v6 states cross-harness delegation parity with a vendor-neutrality regression test; (5) new shipped-default launch posture "sandboxed, no prompts" (`AgentAdapter::policy_args`/`default_sandbox_args`, `[sandbox]` config, `REPO_FORBIDDEN` opt-out) for both adapters; (6) pacing gate closed two gaps — fail-open on no/stale data replaced by a bounded `pace.blind_delay_secs` safety delay, and `wrap`/dashboard-pane launches now consult the gate at all (`pace::InteractiveGate`, T10) where before only `exec`/`loop` did; (7) new `zirv setup restore` reversing a `reset`/`apply` backup, sharing one manifest-tracked backup mechanism across all three. Native tests 2153/2160 (7 pre-existing `wrap` failures identical on main); Docker matrix 107/107 (Linux only, stub harnesses — Windows ConPTY/cmd-shim and macOS Keychain remain untested in CI). `Cargo.toml` bumped to 2.22.0. See the 2026-08-23 [[Work Journal]] entry and the five 2026-08-22 [[Decision Log]] entries.
**Next:** not yet committed/pushed as of this entry. Nine new issues filed for residuals: #83 (harness-neutral command safety policy), #84 (handover), #85 (codex injection fallback), #86 (codex rollout events), #87 (harvest in setup), #88 (risk fail-safe), #89 (codex sandbox asymmetry), #90 (content secret detection), #91 (verification retention), #92 (cmd-shim trait defaults), #93 (wrap existing statusLine), #94 (fan-out send), #95 (backup retention).

### `feat/dash-adaptive-poll-help-overlay` — adaptive input-poll wait, `Ctrl+A ?` help overlay, click-drag selection, Alt+Enter fold fix, mail-loss fix (2026-08-19, v2.13.0, PR #29)
**Status (2026-08-19):** Committed (`ddff811`) and PR #29 open: the adaptive `event::poll` wait (10ms within 300ms of the last operator key/mouse event, else the old flat 50ms) and the `Ctrl+A ?`/`h`/`H` help overlay described below.
**Update (2026-08-19):** Three further changes, uncommitted, extending this same PR. (1) Dashboard click-drag text selection: the host terminal now also enables `?1002` (button-drag tracking, `?1003` still off); a left click-drag over a pane whose child doesn't itself want mouse reporting selects text and release copies it via OSC 52 to the host stdout (64 KiB cap, best-effort); a selection is cancelled by scrollback movement, any grid resize, or the selected pane processing new child output. (2) `encode_key`'s Enter arm now sends `ESC CR` on SHIFT **or** ALT, not SHIFT alone — an empirical ConPTY probe found Windows Terminal's own Shift+Enter rewrite folds into an `Enter`+ALT keydown at zirv's console layer, which was the actual root cause of Shift+Enter submitting instead of newlining. (3) Issue #30 mail-loss fix: `build_turn_env` now sets a spawned pane's session identity unconditionally, so a worker pane's report-back reply is always directed rather than accidentally broadcasting to be claimed by the wrong pane; every on-behalf-of mail consumption now logs a `mail-consumed` decision-log entry. `Cargo.toml` bumped to 2.13.0. See [[Work Journal]] (two 2026-08-19 entries) and [[Decision Log]] (three 2026-08-19 entries).
**Next:** PR #29 pending final codex review + push. See [[Ctx Supervisors]] ("Adaptive input-poll wait", "Pane scrolling and mouse reporting", "Click-drag text selection and clipboard copy", "Key encoding") and [[Known Issues]] for the resolved issue #30 gotcha and the extended Shift+Enter/mouse entries.

<!-- Rotated out 2026-08-25: "feat/chat-token-economy -- worker model defaults, PreToolUse seat guard, token-economy prompts, capacity marker, live inter-session messaging (2026-08-18, v2.12.0, PR #28)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-25: "feat/review-model-config -- per-agent code-review model config (2026-08-18, v2.11.0)" — oldest entry, cap held at ~10. -->
<!-- Rotated out 2026-08-24: "feat/usage-two-window-display -- both usage windows shown, staleness-filtered (2026-08-17/18)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-24: "feat/usage-credits-throttle -- vendor usage monitoring, use_credits gating, pace-to-reset throttle (2026-08-16/17)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-24: "fix/process-lifecycle -- reap child process trees on every teardown path (stacked on feat/harness-roster-prompt, 2026-08-16)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-23: "Derived harness roster + zirv-owned review policy; dashboard sidebar ownership scoping (feat/harness-roster-prompt, 2026-08-16)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-23: "zirv meta-harness (chat entry, coordination, dashboard) — merged (PR #21, 2026-08-14)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-23: "Agent enable/disable settings and Obsidian vault setup — both merged (2026-08-12)" — oldest entry, cap held at 10. -->
