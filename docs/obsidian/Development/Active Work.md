---
last-verified: 2026-08-23
---

# Active Work

> Claude: update at session start and end for seamless handoffs.

Entries use the format:

```
### <title> — <state> (`branch-name`, YYYY-MM-DD)
**Status (date):** what's true right now.
**Next:** the next concrete step.
```

## In Progress

### Frontend quality workflow — nine-commit draft PR green (`feat/frontend-quality`, 2026-08-21, v2.19.0, PR #78)
**Status (2026-08-21):** Issues #70–#77 plus the focused quality-contract v2 enhancement are implemented as nine dependency-ordered commits in draft PR #78 targeting `main`. Surface modes, product/design thesis and whole-journey reasoning, a 44-rule detector with complete inventory coverage, narrow/intermediate/wide evidence, a mandatory 13-dimension ≥4/5 AI-review gate, and durable task context all remain zero-touch. GitHub CI run 224 passed versioning, build, all 1,945 tests, rustfmt, Linux clippy, and Windows-target clippy.
**Next:** maintainer review; mark PR #78 ready and merge the top stack PR when desired.

## Recently Completed

### Close all 13 open issues (#83–#95) plus a malformed-`ctx.toml` fix — implemented, not yet merged (`feat/close-open-issues`, 2026-08-23, v2.23.0)
**Status (2026-08-23):** Two commits on top of `origin/main` (`2264b55`, `499e65a`). `2264b55` closes issues #85–#95: codex injection-fallback surfaced in status/announcements (#85); codex rollout-derived turn/usage events, `capabilities().events = true` (#86); one-time `zirv setup` memory-harvest offer (#87); risk re-classification fails safe (Measured/Unavailable, one-band escalation) outside git (#88); one-time codex distiller/reviewer sandbox-residual announcement, `--ignore-rules`/`--ignore-user-config` added when the installed CLI documents them (#89); content-based secret detection behind the filename gate (#90); verification-report retention on write, reusing telemetry's key (#91); `launches_through_cmd_shim` trait default derives from `resolve_program` (#92); `zirv setup` wraps an existing custom statusLine (#93); `zirv ctx send --all` fan-out via per-session read markers (#94); backup-run retention on write (#95); plus a malformed repo `ctx.toml` now skips that layer (`config::UnparsableLayer`) instead of aborting every command. `499e65a` adds #83 (`[safety]` deny/ask/allow command-safety policy, `zirv ctx safety check|list|explain`, wired claude `PreToolUse` hook) and #84 (`zirv ctx handover --model|--agent [--dry-run] [--force]`, swaps the orchestrator seat's model/harness in place under the same registry short id, `Ctrl+A o` dashboard picker). Native 2255/2262 (7 pre-existing Windows-only); Linux `commands::ctx` 1968/1968. `Cargo.toml` bumped to 2.23.0. See the 2026-08-23 [[Work Journal]] entry and the 2026-08-23 [[Decision Log]] entries.
**Next:** not yet pushed/PR'd as of this entry; a code-review fix round may still be adjusting `src/` and the Modules pages concurrently. All 13 issues (#83–#95) are implemented on this branch and will close when the PR merges. Residuals recorded in [[Known Issues]]: codex rollout tool-call/tool-result/compaction events remain unmapped (#86 residual half of issue #11); the Windows-codex injection fallback is surfaced, not closed (#85); verification retention shares telemetry's retention key rather than a dedicated one (#91 scope residual); the distiller/reviewer sandbox-residual announcement can only gate on `--quiet`/`ZIRV_CTX_QUIET`, not the full layered `chrome.events` (#89).

### Memory, context, and workflow subsystems — merged (2026-08-21, v2.14.0–v2.17.0, PRs #59/#65/#66/#67)
**Status (2026-08-21):** All three parallel rework tracks are merged into main, each after a consolidated opus review plus two fix rounds, with every finding fixed or recorded as a residual. Memory (issues #31–35, PR #66 collapsing #57/#58/#62/#64/#66, v2.14.0): private state-dir + shared repo-committed `.zirv/memory/` scopes, a key-addressed store, the `zirv memory` command family, a core memory prompt layer injected at all six launch seams, and a deterministic retrieval engine (recall only; session-start injection deferred to #44). Context (issues #39–43, PR #65 collapsing #60/#61/#63/#65 for v2.15.0, PR #67 for v2.16.0): a generic context-surface model with trust metadata, AGENTS.md/native-settings ingest, the canonical `.zirv/context/` layer (analysis-only until #44), deterministic drift detection, and the canonical `EffectivePolicy` model with per-harness capability mapping (dormant until #44/#46). Workflow (issues #47/#49/#50, PR #59, v2.17.0): the provider-neutral workflow subsystem under `src/commands/workflow/` — skills registry, engine, deterministic classification, verification evidence, review packages/findings, artifacts, telemetry. See the 2026-08-21 [[Work Journal]] entry and the five 2026-08-21 [[Decision Log]] entries.
**Next:** **update (2026-08-22, verified via `gh issue list`): the follow-on issues this entry used to list as open are all closed** — memory #36–38, context #44–46, workflow #48/#51–56, and #68 (`get_scoped`) all shipped 2026-08-21/22. See the frontend-quality and AI-setup entries below and [[Known Issues]] for residuals recorded across those review rounds.

<!-- Cap at ~10 entries; drop the oldest when adding a new one. -->

### Dashboard/harness parity fix round — codex permission parity, pacing coverage, macOS usage polling, dashboard select mode (`fix/dashboard-and-harness-parity`, 2026-08-22/23, v2.22.0)
**Status (2026-08-23):** Seven fixes landed in one branch, uncommitted diff reviewed against source: (1) macOS status header usage via a bounded, announced Keychain fallback in `poll.rs` plus `poll::usage_source_hint` for `zirv ctx status`; (2) `Ctrl+A v` dashboard select-mode toggle releasing zirv's own mouse reporting for a pane (e.g. Claude Code) its click-drag selection can never reach; (3) `wrap`'s live mail advisory gained signal-less (codex) parity, and `zirv ctx send`/`HARNESS_PROMPT` v8 now say plainly that an undirected send is claimed by exactly one session; (4) `HARNESS_PROMPT` v6 states cross-harness delegation parity with a vendor-neutrality regression test; (5) new shipped-default launch posture "sandboxed, no prompts" (`AgentAdapter::policy_args`/`default_sandbox_args`, `[sandbox]` config, `REPO_FORBIDDEN` opt-out) for both adapters; (6) pacing gate closed two gaps — fail-open on no/stale data replaced by a bounded `pace.blind_delay_secs` safety delay, and `wrap`/dashboard-pane launches now consult the gate at all (`pace::InteractiveGate`, T10) where before only `exec`/`loop` did; (7) new `zirv setup restore` reversing a `reset`/`apply` backup, sharing one manifest-tracked backup mechanism across all three. Native tests 2153/2160 (7 pre-existing `wrap` failures identical on main); Docker matrix 107/107 (Linux only, stub harnesses — Windows ConPTY/cmd-shim and macOS Keychain remain untested in CI). `Cargo.toml` bumped to 2.22.0. See the 2026-08-23 [[Work Journal]] entry and the five 2026-08-22 [[Decision Log]] entries.
**Next:** not yet committed/pushed as of this entry. Nine new issues filed for residuals: #83 (harness-neutral command safety policy), #84 (handover), #85 (codex injection fallback), #86 (codex rollout events), #87 (harvest in setup), #88 (risk fail-safe), #89 (codex sandbox asymmetry), #90 (content secret detection), #91 (verification retention), #92 (cmd-shim trait defaults), #93 (wrap existing statusLine), #94 (fan-out send), #95 (backup retention).

### `feat/dash-adaptive-poll-help-overlay` — adaptive input-poll wait, `Ctrl+A ?` help overlay, click-drag selection, Alt+Enter fold fix, mail-loss fix (2026-08-19, v2.13.0, PR #29)
**Status (2026-08-19):** Committed (`ddff811`) and PR #29 open: the adaptive `event::poll` wait (10ms within 300ms of the last operator key/mouse event, else the old flat 50ms) and the `Ctrl+A ?`/`h`/`H` help overlay described below.
**Update (2026-08-19):** Three further changes, uncommitted, extending this same PR. (1) Dashboard click-drag text selection: the host terminal now also enables `?1002` (button-drag tracking, `?1003` still off); a left click-drag over a pane whose child doesn't itself want mouse reporting selects text and release copies it via OSC 52 to the host stdout (64 KiB cap, best-effort); a selection is cancelled by scrollback movement, any grid resize, or the selected pane processing new child output. (2) `encode_key`'s Enter arm now sends `ESC CR` on SHIFT **or** ALT, not SHIFT alone — an empirical ConPTY probe found Windows Terminal's own Shift+Enter rewrite folds into an `Enter`+ALT keydown at zirv's console layer, which was the actual root cause of Shift+Enter submitting instead of newlining. (3) Issue #30 mail-loss fix: `build_turn_env` now sets a spawned pane's session identity unconditionally, so a worker pane's report-back reply is always directed rather than accidentally broadcasting to be claimed by the wrong pane; every on-behalf-of mail consumption now logs a `mail-consumed` decision-log entry. `Cargo.toml` bumped to 2.13.0. See [[Work Journal]] (two 2026-08-19 entries) and [[Decision Log]] (three 2026-08-19 entries).
**Next:** PR #29 pending final codex review + push. See [[Ctx Supervisors]] ("Adaptive input-poll wait", "Pane scrolling and mouse reporting", "Click-drag text selection and clipboard copy", "Key encoding") and [[Known Issues]] for the resolved issue #30 gotcha and the extended Shift+Enter/mouse entries.

### `feat/chat-token-economy` — worker model defaults, PreToolUse seat guard, token-economy prompts, capacity marker, live inter-session messaging (2026-08-18, v2.12.0, PR #28)
**Status (2026-08-18):** Seven commits on `feat/chat-token-economy`, committed, PR #28 open. First four (`059ad3c`, `fd5a709`, `b2f457d`, `1e1ca45`): new `[worker]` config defaults a delegated headless worker to claude's own `"sonnet"` (codex: unmanaged) instead of the operator's interactive seat model; new `zirv ctx hook pretool` (PreToolUse) guard deterministically denies a subagent dispatch that would fork or silently inherit an expensive orchestrator seat, fixed same-day to follow the *effective* launch model and to gate strictly on a real dispatch's own non-empty prompt; `ORCHESTRATOR_PROMPT`/`HARNESS_PROMPT` rewritten (v5) for mandatory explicit model tiers, a fork ban, token-economy briefs, and single-reviewer-effort `/code-review`; new per-agent `capacity = "small"` marker in `.zirv/.settings.toml`, rendered in the harness roster. Three more (`5fc48b3`, `772f039`, `604a8df`) added live inter-session messaging: `zirv ctx inbox` consumes caller-visible mail by default (`--peek` for the old broad read), `zirv ctx status`'s unread count is now caller-visibility-scoped, a signal-less dashboard pane (codex) counts idle after `dash.idle_quiet_ms` of pty-output quiet (turning on mail/nudge delivery into codex panes for the first time), the dashboard's mail sweep now advises attached orchestrator panes with a one-line notice, and `wrap`'s pump live-types the same one-line advisory into the wrapped session at a verified-idle boundary — bodies and consumption both stay off every one of these channels. `Cargo.toml` bumped to 2.12.0. See [[Work Journal]] (two 2026-08-18 entries) and [[Decision Log]] (four 2026-08-18 entries).
**Update (2026-08-19):** three further commits graft the non-redundant half of a parallel implementation — the role-gated adapter layer (`worker_system_prompt`, claude's `WORKER_PROMPT`), the Orchestrator/Worker split of the operator's own user-layer file, the bare `wrap` verb's role fix, a lone `--model` pin surviving the dashboard spawn-request path, `.zirv/claude.yaml` rule 8 (no `/code-review` from the seat — it forks onto the seat model), and a deterministic phase boundary for the one CI-failing `wrap` pty test. See the 2026-08-19 [[Work Journal]] and [[Decision Log]] entries.
**Next:** PR #28 open. Three follow-ups live outside this repo, not yet done: the operator must wire `zirv ctx hook pretool` manually into `~/.claude/settings.json` (matcher `Agent|Task`) for the guard to run at all, a codex `capacity = "small"` marker (if wanted) needs setting in the operator's home `.zirv/.settings.toml`, and an operator with standing *worker* instructions in `~/.zirv/system-prompt.md` must now copy them into `~/.zirv/system-prompt.worker.md` (see [[Known Issues]]). Also in [[Known Issues]]: the `~/.codex/config.toml` model-pin 400 gotcha, the resolved codex-pane idleness trap, the residual that `wrap`'s own live mail advisory still never fires for a signal-less adapter, the macOS `wrap` pty-test wedge, and the intermittent `exec` nudge timeouts. Residual not done: dashboard worker panes still get no adapter layer at all, so `WORKER_PROMPT` never reaches them.

### `feat/review-model-config` — per-agent code-review model config (2026-08-18, v2.11.0)
**Status (2026-08-18):** Two commits (`7da7ed5`, `e8454d3`) on `feat/review-model-config`, committed. New `[review]` section in `ctx.toml` (`review.claude`/`review.codex`, `REPO_FORBIDDEN` as a whole table) lets an operator pin the code-review model per harness; unconfigured, `AgentAdapter::review_model_below(seat)` picks one tier below the orchestrator seat on each adapter's own verified ladder. `harness_prompt_lines` appends a roster line naming every enabled harness's resolved choice and the routing rule; claude's `ORCHESTRATOR_PROMPT` review bullet now points at it. The fix-up commit made the roster line honest at the floor tier and for an operator-configured equal, and matched seats case-insensitively. `Cargo.toml` bumped to 2.11.0. See [[Work Journal]] and [[Decision Log]].
**Next:** PR #27 open (merge conflicts vs the merged PR #26 resolved 2026-08-18). The version-ordering dependency is satisfied: PR #26 (2.10.0) merged first; this branch carries 2.11.0. Two residuals recorded in [[Known Issues]], not fixed: the codex review ladder is sourced from a codex-cli 0.146.0 capture, not re-verified against npm's 0.105.0; the equals-seat check compares exact resolved-model strings only, so a differently-spelled-but-equal-tier configured model keeps the strict never-clause wording.

### `feat/usage-two-window-display` — both usage windows shown, staleness-filtered (2026-08-17/18)
**Status (2026-08-18):** Merged to main as PR #26 (v2.10.0). Per-agent usage now shows both the 5h and 7d windows (`5h X% wk Y%`) instead of the worse-of-both collapse, at the dash header, `wrap`'s status bar, and `zirv ctx status`; a new `window::available(windows, now)` drops a window whose `resets_at` has passed or that has outlived its own span before any of those three render it. `zirv ctx usage` deliberately stays raw/unfiltered — the diagnostic surface explaining pacing. Both refresh gates (`poll::maybe_poll`, `window::refresh_codex_usage`) now treat a display-dropped slot as staleness (`window::freshest_available_observation`) so a rolled-over window refreshes promptly instead of sitting blank up to `collector_max_age_secs`. `Cargo.toml` bumped 2.9.0 → 2.10.0. See [[Work Journal]] and [[Decision Log]] (2026-08-18 entry) for the mechanism.
**Next:** none — merged. Two residuals recorded in [[Known Issues]], not fixed: pace's hard-park path deliberately admits a rolled-over-but-recently-observed reading (a different, pre-existing staleness rule from `available`'s own), and `zirv ctx usage` prints a bare unix epoch for a passed `resets_at` with no "already reset" wording.

### `feat/usage-credits-throttle` — vendor usage monitoring, use_credits gating, pace-to-reset throttle (2026-08-16/17)
**Status (2026-08-17):** Nine-task plan complete and committed on `feat/usage-credits-throttle`, docs/version/gates done (Task 8). `pace.rs` gained a `Slow` pace-to-reset decision (soft-throttle band between new `soft_percent`/`max_percent`) and `PaceGate{use_credits, poller}` (`use_credits` skips proactive throttle/pause only, never the vendor-reported limit park). `window.rs` gained a passive codex rollout-file collector; `poll.rs` (new) is an active HTTP poll fallback (`ureq`, this crate's first HTTP dependency) behind a staleness/interval floor. Task 7 put per-harness usage back in the dashboard header; Task 9 delivered session-conventions v2 to codex workers via a task-prompt fallback. `Cargo.toml` bumped 2.8.0 → 2.9.0. See [[Work Journal]] and [[Decision Log]] (four new entries) for the mechanism.
**Next:** post-plan review round pending (native `/code-review` plus a `zirv agent codex` review worker per session conventions, triage, fix, re-review, hard stop after 2 fix rounds) — not yet run as of this entry. Then push and open the PR. Three residuals recorded in [[Known Issues]], not fixed: the codex ChatGPT-backend poll endpoint ships unverified (no readable token to test against), the codex rollout collector is pinned to codex-cli 0.105.0's shape only, and the Anthropic OAuth usage endpoint is unofficial and may drift.

### `fix/process-lifecycle` — reap child process trees on every teardown path (stacked on `feat/harness-roster-prompt`, 2026-08-16)
**Status (2026-08-16):** Two commits (`c843891`, `222b24f`), stacked on `feat/harness-roster-prompt`, pending final re-review + PR. Three Windows lifecycle layers landed: (P1) `supervise::kill_tree` now runs at every pty teardown seam (`wrap::quit_child`, `dash::pane::Pane::finish_shutdown`, the distiller's timeout escalation), not just `exec`/`loop`. (P2) a cross-platform supervised-pid registry swept by the Windows console-close handler on terminal events only. (P3) kill-on-close Job Objects (`ChildGuard`/`JobGuard`, new `windows-sys` `Win32_System_JobObjects` feature) as the kernel backstop for a crash or `taskkill /F` against zirv itself. The review round (`222b24f`) added the roster-restore liveness fail-safe (`partition_live`/`short_is_live`, deferred rather than dropped) and parked `wrap`'s registry record on zirv's own pid during a restart's kill→respawn window. See [[Work Journal]] and [[Decision Log]] (supersedes the 2026-08-14 Job-Object rejection).
**Next:** final re-review, then open the PR (stacked on the still-open `feat/harness-roster-prompt` PR). Five residuals recorded in [[Known Issues]], not fixed: a dropped `ChildGuard` kills its child on Windows (a new invariant every future caller must hold); Job-Object assignment races a shim's own grandchild; the distiller's `kill_tree` escalation has no dedicated test; a live session's roster entry never ages out while it stays alive; a held-back roster candidate is still lost if the dashboard never reaches `on_quit`.

### Derived harness roster + zirv-owned review policy; dashboard sidebar ownership scoping (`feat/harness-roster-prompt`, 2026-08-16)
**Status (2026-08-16):** Three commits (`a5bb389`, `183d45f`, `a2536a5`), all committed; `fix/process-lifecycle` above now stacks on top. `a5bb389`: a new derived prompt layer (`PromptSource::Harnesses`, `adapters::harness_prompt_lines`) tells an Orchestrator session which harnesses it can `zirv agent` right now, gated by `prompt.harnesses`; `HARNESS_PROMPT` (v3→v4) gained self-initiative guidance and a single zirv-owned, bounded cross-harness review policy that claude's `ORCHESTRATOR_PROMPT`/`.zirv/claude.yaml` stay self-contained per role and layer on top of. `183d45f`: the dashboard sidebar's view-only rows are scoped to sessions this dashboard owns (`sessions::Record.owner_pid`), reversing `ac40418`. `a2536a5` (the review round): hardened the roster (a real presence check past `ready()`'s fail-open, self-exclusion, `agent_bin` scoped to the adapter it names) and moved `owner_pid` stamping into `SessionGuard::register` itself so every registration path is attributed uniformly. See [[Work Journal]] (2026-08-16) and [[Decision Log]] for both decisions.
**Next:** open a PR against main (still open as of 2026-08-16). Two residuals recorded in [[Known Issues]], not fixed: the roster-restore liveness gap (now itself superseded by `fix/process-lifecycle`'s own fail-safe and residuals above), and raw-pid ownership missing a pane child's own in-process headless fallback.

<!-- Rotated out 2026-08-23: "zirv meta-harness (chat entry, coordination, dashboard) — merged (PR #21, 2026-08-14)" — oldest entry, cap held at 10. -->
<!-- Rotated out 2026-08-23: "Agent enable/disable settings and Obsidian vault setup — both merged (2026-08-12)" — oldest entry, cap held at 10. -->
